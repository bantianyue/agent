#!/usr/bin/env python3
"""
MOSS-TTS Local v1.5 瓶颈优化文章
"""

import json, os, sys

# 获取文章目录（兼容 write-article-data.py 的 exec 调用）
_article_dir = sys.argv[1] if len(sys.argv) > 1 else os.getcwd()

DATA = {
    "title": "如何移除 MOSS-TTS Local v1.5 的最大性能瓶颈",

    # 要点速览：必须为 [{key, body}] 列表，不可为字符串
    "summary": [
        {"key": "核心发现", "body": "MOSS-TTS v1.5 的 92 层 Transformer 声码器成为非流式推理最大瓶颈"},
        {"key": "关键数据", "body": "QPS 提升 48.64%，p99 延迟下降 35.03%，RTF 降低 33.82%"},
        {"key": "方法创新", "body": "采用 SGLang 打包注意力路径，兼容本地因果注意力规则"},
        {"key": "质量保障", "body": "WER 2.02%、说话人相似度 65.1、UTMOS 3.97，全部达标"},
        {"key": "零侵入", "body": "无需更换编码器或重训模型，仅封装注意力计算路径"},
    ],

    "lead": [
        "**TTS 推理不止是自回归语言模型循环**，一个请求需要经过参考音频编码、编码帧生成、波形重建三个阶段。优化某一阶段后，往往会暴露另一处新的瓶颈。",
        "本文介绍如何在不更换声码器或改动模型架构的前提下，将 MOSS-TTS Local v1.5 端到端 QPS 提升近 49%。",
    ],

    "sections": [
        {
            "type": "h2",
            "title": "优化对象：MOSS-TTS 三阶段管线",
            "paras": [
                "MOSS-TTS Local v1.5 是本地 48kHz 立体声零样本 TTS 模型，支持 31 种语言、长音频生成、时长控制和显式停顿。其推理管线分为三个阶段：",
                "1. **参考编码**：文本分词，参考音频通过 MOSS-Audio-Tokenizer-v2 转为离散残差量化码，提供音色条件信息；",
                "2. **自回归生成**：36 层 Qwen3-4B 主干逐帧生成音频，随后 1 层本地 Transformer 决定继续/停止，并顺序采样 12 个音频码本；",
                "3. **声码器解码**：生成的编码通过 MOSS-Audio-Tokenizer-v2 解码器转为波形音频。",
                "自回归阶段输出形状为 [T, 13]（T 为音频帧数），但这些只是压缩表示，真正的音频波形还需要声码器处理完整序列后才能生成。对于非流式推理，自回归解码结束并不意味着请求完成——声码器的完整计算仍在其后运行。",
            ],
            "figs": [
                {"src": "fig1.png", "caption": "MOSS-TTS v1.5 架构与三阶段推理管线"},
            ],
        },
        {
            "type": "h2",
            "title": "前期已完成的优化工作",
            "paras": [
                "在聚焦声码器之前，其他阶段已经完成了大量优化：",
                "参考编码阶段引入了批处理和内容寻址缓存，重复使用的参考音色无需重新运行约十亿参数的音频编码器。自回归引擎采用 SGLang 调度和 CUDA Graphs，为重载解码步骤回放捕获的 GPU 操作序列。持久化 GPU 状态减少了逐帧簿记，GPU 端基数哈希查找消除了 GPU 与 CPU 之间的一个同步点。",
                "解码循环还加入了一步前瞻：主机解析上一步时，可启动下一步 GPU 解码。帧启动状态池化避免在 Python 中重建活跃行状态，种子张量采样器编译进 CUDA Graph 捕获边界。",
                "流式路径则走了另一条优化路线：持久化解码会话允许音频增量输出，CUDA Graph 加速了大量小型流式解码器调用。跨阶段内存预算为声码器预留了足够 GPU 容量，而不是将自回归引擎视为唯一驻留模型。",
                "当再次分析非流式路径时，预处理和自回归的明显热点已不再原始，这使得另一阶段的开销成为主导。",
            ],
        },
        {
            "type": "h2",
            "title": "为什么 MOSS 的声码器与众不同",
            "paras": [
                "常见 TTS 推理假设是：自回归 LM 主干做大部分昂贵工作，最终声码器只是相对轻量的解码器。这个假设更符合 Boson AI 的 Higgs Audio v3 TTS。",
                "Higgs Audio v3 同样使用 Qwen3 规模的自回归主干生成音频编码令牌，但其波形解码器是基于 DAC 的卷积网络，而不是独立的大 Transformer 模型。MOSS 则部署了 MOSS-Audio-Tokenizer-v2 的解码器部分——一个约十亿参数、带滑动窗口注意力的因果 Transformer 独立模型。",
                "这彻底改变了优化面。Higgs 解码器围绕卷积和上采样组织，没有需要打包的 Transformer 注意力路径。而在 MOSS 中，**波形重建本身包含深度的局部注意力堆栈**，序列布局、填充、注意力内核语义直接影响声码器性能。",
                "MOSS 声码器在 Transformer 阶段和时间上采样阶段之间交替。从 L 个编码帧开始，Transformer 阶段在六种分辨率下运行：125、250、400、400、400、400 个位置。注意力始终保持局部，其分数计算随序列长度乘以有界窗口而不是随完整序列长度的平方缩放。",
            ],
            "figs": [
                {"src": "fig2.png", "caption": "六阶段 Transformer 加五次上采样的声码器架构"},
            ],
        },
        {
            "type": "h3",
            "title": "计算量分析：776L 层-令牌积",
            "paras": [
                "五次 patch 和上采样阶段在 Transformer 阶段之间将时间分辨率翻倍。六个 Transformer 阶段共包含：32 + 12×5 = 92 个 Transformer 层。",
                "每个阶段处理的序列长度不同：第一层 L，后续分别为 2L、4L、8L、16L、32L。总计算量可通过「层数 × 序列长度」的层-令牌积衡量：32L + 12×(2L+4L+8L+16L+32L) = **776L**。",
                "这还只是注意力部分，不包括投影和前馈网络的开销。第六个 Transformer 阶段之后，还有一个大小为 240 的最终 patch 阶段将扩展特征转为波形采样。这就是为什么自回归模型完成编码帧生成后，声码器仍然开销巨大的原因。",
            ],
        },
        {
            "type": "h2",
            "title": "从性能分析到封装实现",
            "paras": [
                "我们原本预期自回归引擎占据剩余的非流式时间大头。但性能分析指向了 Transformer 密集的声码器。一旦将这个结果映射回架构，原因就很清楚：声码器在将序列从 L 扩展到 32L 的同时运行了 92 个 Transformer 层。这给出了明确的实现目标：这六个 Transformer 阶段内部的注意力。",
                "**我们不需要更换声码器或重训权重**。时间上采样阶段、学习的投影、归一化、残差缩放、前馈块和波形路径都不是性能分析识别的瓶颈，因此可以完全照常运行。",
                "修改仅限于数据到达注意力的方式。密集批处理包含对每个短于最长序列的音频序列的填充。封装器移除这些填充行，将有效帧打包在一起，同时保留每个请求的边界和位置，对打包后的表示运行注意力，然后将结果恢复为原始编码期望的密集布局。",
            ],
            "figs": [
                {"src": "fig3.png", "caption": "打包注意力路径的实现架构：移除填充→打包→注意力→解包"},
            ],
        },
        {
            "type": "h3",
            "title": "有效帧打包机制",
            "paras": [
                "投影后的 Transformer 接收形状为 [batch, max_time, hidden] 的密集批特征张量。由于音频请求很少长度相同，max_time 包含对每个短于最长请求的填充，这些行不是编码帧，也不需要通过 Transformer 堆栈。",
                "SGLang 的变长 FlashAttention 路径期望不同的表示：只收集有效前缀并连接成一个张量，形状为 [sum(input_lengths), hidden]。",
                "伴随打包张量的是 cu_seqlens——一个 int32 累积长度数组，告诉内核每个请求的开始和结束位置。对于三个长度为 L0、L1、L2 的请求，它是：[0, L0, L0+L1, L0+L1+L2]。",
                "每个打包行还携带位置 ID。打包在内存中连接请求，但不能将它们变成一个长序列：每个请求的位置仍然从零重新开始。",
                "Transformer 堆栈完成后，有效行被散射回 [batch, max_time, hidden]，该密集张量被传递到原始输出投影。对于没有填充的单个请求的常见情况，收集和散射被完全跳过，直接使用 reshape。",
            ],
        },
        {
            "type": "h3",
            "title": "局部因果注意力到 FA3 的翻译",
            "paras": [
                "MOSS 使用因果局部窗口。在原始掩码中，当满足 0 ≤ query_position - key_position < context 时，键可见。以第一阶段 context=125 为例，每个查询可以看到自身和最多 124 个先前位置。FlashAttention 通过左右键数描述窗口，等效参数是：window_size = (context - 1, 0)。这个 -1 虽小但很重要：传入 (context, 0) 会在左侧多允许一个键。",
                "FA3 还有第二个特定细节：其局部窗口行为与 128 令牌查询分块绑定。当打包序列跨越这些边界之一时，将完整请求视为一个普通变长序列不会为每个查询重现 MOSS 掩码。",
                "解决方案是为请求构建局部注意力计划。每个序列被分成最多 128 令牌的查询分块，每个分块接收包含它可能需要的历史的源 K/V 区间，然后在该查询/KV 对内部应用局部因果掩码。",
                "这些区间会重叠，因此实现为 Q 段和收集的 K/V 段构建单独的累积长度。如果所需的 K/V 布局已经连续，则直接使用。否则，所需行被一次性收集到内核期望的布局中。",
                "分块不会成为单独的内核启动，Q 段、收集的 K/V 段、cu_seqlens_q 和 cu_seqlens_k 被传入每层一个变长 FlashAttention 调用。计划本身为投影 Transformer 前向传递构建一次，并在所有层中重用，因为它们的序列几何完全相同。",
            ],
        },
        {
            "type": "h2",
            "title": "基准测试与质量结果",
            "paras": [
                "将 Transformer 注意力迁移到打包路径后，不仅声码器本身变快，端到端指标的提升幅度更令人惊讶。基准测试在 H100 上运行 1,088 个样本的 SeedTTS 纯生成工作负载：",
                "**QPS 从 4.965 提升到 7.380，增幅 48.64%**；服务输出吞吐量从每秒 273.2 帧提升到 406.0 帧。",
                "**平均延迟从 1.608 秒降至 1.082 秒**，尾部延迟同样显著：p95 下降 30.14%，p99 下降 35.03%。",
                "**实时因子（处理时间/生成音频时长，越低越好）从 0.3847 降至 0.2546，下降 33.82%**。这些测量覆盖完整打包路径，未分离移除填充工作的增益与 FA3 内核本身的增益。",
                "质量评估也全部达标：1,088 样本的语料库 WER 为 2.02%（阈值 3.21%），50 样本的说话人相似度为 65.1（阈值 63.7），1,088 样本的 UTMOS 为 3.97（阈值 3.84）。",
                "最终，优化不需要新的声码器。性能分析给出了精确目标：投影 Transformer 路径。一旦有效帧被打包且 MOSS 的局部注意力被正确翻译，SGLang 的变长内核就能更高效地执行昂贵部分，这个狭窄改动就足以将端到端 QPS 提升近 49%。",
            ],
        },
    ],

    "conclusion": [
        "**这项工作展示了性能分析驱动的深度优化的威力**：不是更换模型架构，而是深入识别真实瓶颈，然后用最小侵入的封装将核心计算迁移到更高效的执行路径上。",
        "48.64% 的端到端 QPS 提升来自于只改动注意力计算的包装层——原有模型权重、架构、质量指标全部保持不变。这种「零侵入」优化模式为其他大模型推理加速提供了可复用的范式。",
        "SGLang Omni 的 TTS 栈工作涵盖模型集成、神经音频编码、调度、GPU 内核和基准测试，欢迎在 SGLang Omni 代码库参与贡献和技术讨论。",
    ],

    "reference_url": "https://www.linkedin.com/pulse/how-we-removed-moss-tts-local-v15s-biggest-bottleneck-palanisamy-yfbzf/?trackingId=TeejpGPJTlCqvL5WKoMLjA%3D%3D",
}

# 当被 exec 调用时，write-article-data.py 会取 DATA 变量
# 也可以直接运行本脚本做语法检查：python article_data_build.py .
if __name__ == "__main__":
    print("✅ DATA 字典结构正常")
    print(f"   summary: {len(DATA['summary'])} 条")
    print(f"   sections: {len(DATA['sections'])} 节")
    print(f"   参考链接: {DATA['reference_url'][:60]}...")
