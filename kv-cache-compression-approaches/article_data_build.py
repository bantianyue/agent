#!/usr/bin/env python3
# -*- coding: utf-8 -*-
import json, os, sys

_article_dir = sys.argv[1] if len(sys.argv) > 1 else os.getcwd()

TBL = {
    "head": ["", "量化", "Token 驱逐", "跨头共享", "低秩压缩"],
    "rows": [
        ["省内存方式", "每个数字用更少比特", "少存一些 token", "每层更少的 Key/Value", "每个 token 更小的向量"],
        ["需要重训", "否", "否", "是", "是"],
        ["是否丢 token", "否", "是", "否", "否"],
        ["典型压缩比", "2 到 4 倍", "固定缓存大小", "4 到 8 倍", "10 倍以上"],
        ["质量损失", "很小", "取决于任务", "很小", "几乎没有"],
        ["代表方法", "KIVI", "H2O、StreamingLLM", "GQA、MQA", "MLA（DeepSeek）"],
    ],
}

CODE1 = '''__CODE__python::layers = 32
heads = 32
head_dim = 128
bytes_per_number = 2

# Key and Value, so multiply by 2
per_token = 2 * layers * heads * head_dim * bytes_per_number
print(per_token / (1024 * 1024))  # 0.5 MB'''

CODE2 = '''__CODE__python::values = [0.31, -0.72, 0.05, 0.98]
scale = max(abs(v) for v in values) / 127

quantized = [round(v / scale) for v in values]
print(quantized)  # [40, -93, 6, 127]'''

DATA = {
    "summary": [
        {"key": "为什么压", "body": "以 Llama 2 量级的配置算，一个 token 的 KV cache 就要 0.5 MB：4000 token 对话占 2 GB，10 万 token 文档占 50 GB，还会随用户数线性叠加"},
        {"key": "四种方法", "body": "量化（更少比特）、Token 驱逐（丢掉不重要的 token）、跨头共享（MQA/GQA）、低秩压缩（MLA 存潜向量）"},
        {"key": "怎么选", "body": "已有模型想快速省内存用量化；超长或无止境输入用带 Attention Sink 的驱逐；要精确回忆别用驱逐；训练新模型直接上 GQA/MLA，追求极致就叠加使用"},
    ],
    "lead": [
        "KV cache 是模型用来「记住」对话的内存：每写一个新 token，它都要回看此前所有 token 的 Key 和 Value。问题是这份缓存会随对话变长和并发用户变多而快速膨胀，直接吃掉 GPU 显存，还拖慢生成速度。",
        "这篇文章把 KV cache 压缩的四类方法讲清楚：量化、Token 驱逐、跨头共享 K/V、低秩压缩，并给出各自的压缩比、质量代价与适用场景。",
    ],
    "sections": [
        {
            "type": "h2",
            "title": "先看 LLM 怎么写文本",
            "paras": [
                "LLM（大语言模型）是能读文本、写文本的 AI 模型，ChatGPT、Claude、Gemini 都基于它。模型通过阅读互联网上的海量文本学习，这个过程叫训练；训练完成后我们提问，它写出回答。",
                "最要紧的一点是：模型不会一次写完整个回答，而是一小片一小片地写，这一小片叫 token（可以粗略理解为一个词）。比如问「法国的首都是哪里」，它会先写「The」，然后回看目前所有内容写下「capital」，再回看一遍写「of」，接着「France」「is」「Paris」。",
                "也就是说，每写一个新 token，模型都得回看此前所有 token。这个回看动作，就由 Attention 完成。",
            ],
        },
        {
            "type": "h2",
            "title": "什么是 Attention",
            "paras": [
                "Attention 是模型判断「哪些历史 token 对写下一个 token 重要」的方式，简单说就是回看并挑出重点。比如模型在写「The dog was tired, so it went to sleep.」时遇到「it」，它需要知道「it」指的是「the dog」，于是会给「dog」更高的注意力，其它词则更低。",
                "那它是怎么做到的？每个 token 会被转换成三样东西：Query（当前 token 在找什么）、Key（每个历史 token 能提供什么，像标签）、Value（每个历史 token 实际携带的信息）。这三样都只是一串数字，在 AI 里叫作向量。",
                "可以用图书馆打比方：我们脑子里的问题是 Query，每本书书脊上的标题是 Key，书里的内容是 Value。我们拿问题和每本书的标题比对，标题匹配得好的书获得更多注意力，然后阅读那些书的内容并汇总所学。Attention 做的正是这件事：新 token 的 Query 与此前每个 token 的 Key 比对，按匹配程度把 Value 组合起来传给下一步。",
                "还有一点：模型不是只做一次 Attention。它有很多层叠在一起，每层又有多个注意力头，每个头像一位独立的读者，有的关注语法、有的关注人名、有的负责判断「it」指代谁。",
            ],
        },
        {
            "type": "h2",
            "title": "什么是 KV Cache",
            "paras": [
                "问题来了：每写一个新 token，模型都需要此前所有 token 的 Key 和 Value。假设已经写了 100 个 token、正在写第 101 个，它需要前 100 个 token 的 Key 和 Value；如果不做任何记忆，模型会把 100 个 token 的 Key 和 Value 重新算一遍，到第 102 个 token 时再把 101 个重算一遍，如此反复。",
                "这是巨大的浪费：一个 token 的 Key 和 Value 一旦算出来就不会再变，为什么要反复计算？于是 KV Cache 出场了。Cache 就是一块存储，把算过的东西存下来避免重算；KV Cache 就是模型存放此前所有 token 的 Key 与 Value 的内存。有了它，写第 101 个 token 时只需计算最新 token 的 Key 和 Value 并追加到缓存，其余全部从缓存里读。",
                "但这里有个陷阱：这份缓存会随每一个 token 增长，而且长得非常快。",
            ],
        },
        {
            "type": "h2",
            "title": "为什么 KV Cache 会变得巨大",
            "paras": [
                "先补两个小知识：bit 是计算机最小的内存单位，非 0 即 1；8 个 bit 是 1 个 byte。",
                "取一个接近 Llama 2 真实配置的模型：32 层、每层 32 个注意力头、每个头的 Key 向量与 Value 向量各有 128 个数字、每个数字用 16 bit（即 2 byte）存储。先算一个 token 要占多少缓存：一个层的一个头存 128 个 Key 数字加 128 个 Value 数字，共 256 个；32 个头就是 8192 个；32 层就是 262144 个数字；每个数字 2 byte，于是一个 token 需要 524288 byte，也就是 0.5 MB。",
                CODE1,
                "这里把层数、头数、每个头的大小与每个数字的字节数相乘，最前面乘 2 是因为 Key 和 Value 都要存。",
                "0.5 MB 一个 token 听起来不多，但把全局算一下：用户进行 4000 token 的对话，就是 4000 乘 0.5 MB，等于 2 GB；用户粘贴一份 10 万 token 的长文档，就是 50 GB，而这只是一个用户的缓存。模型跑在 GPU 上，GPU 显存通常是 24 GB 到 80 GB，其中很大一部分还要留给模型本身；如果同一块 GPU 上有 100 个用户同时对话、每人一份缓存，所需内存远超任何 GPU 的容量。",
                "而且不止是内存问题：每生成一个新 token，模型都要把整份缓存从内存里读一遍，缓存越大读取越久，生成就越慢。",
                "解决这个问题有两条路：一条是别再浪费已有内存（比如 PagedAttention 的做法），另一条是把缓存本身变小，也就是下面要讲的 KV Cache 压缩。",
            ],
            "fig_after": {
                "0": [{"src": "fig01.png", "caption": ""}],
            },
        },
        {
            "type": "h2",
            "title": "什么是 KV Cache 压缩",
            "paras": [
                "KV Cache 压缩是一组让缓存变小、同时尽量保持模型输出质量不变的技术。说白了，就是用更少的空间存下同样的对话记忆。",
                "做法有几种：有些可以直接用在已有模型上，有些必须在训练之前就设计进模型里。下面从最简单的开始，逐一说明。",
            ],
        },
        {
            "type": "h2",
            "title": "方法一：量化",
            "paras": [
                "量化的意思是：缓存里的每个数字用更少的比特来存。举个例子，一个人的身高可以写成 175.4837 cm，也可以写成 175 cm；后者占用更少，对多数用途也够用。量化做的就是这件事：正常情况下 KV cache 里每个数字用 16 bit 存储，量化后可以存成 8 bit 甚至 4 bit。",
                "举个具体例子。假设 Key 向量里有这么几个数字：",
                "__CODE__python::[0.31, -0.72, 0.05, 0.98]",
                "要压到更少的比特，先找出最大的值 0.98，然后把每个数字缩放到 −127 到 127 这个能用 8 bit 表示的小整数范围内。",
                CODE2,
                "这里把每个数字除以缩放系数再四舍五入到最近的整数，于是每个数字都变成了能用 8 bit 而不是 16 bit 表示的小整数；只需要额外单独存下这个缩放系数，需要时就能近似还原原来的数值。",
                "从 16 bit 到 8 bit，缓存变成原来的一半；从 16 bit 到 4 bit，缓存变成原来的四分之一。",
                "优点：添加起来非常简单，不丢弃任何 token，质量损失很小。缺点：低于 4 bit 之后舍入误差会变大，模型回答开始变差，所以能压到什么程度是有下限的。",
                "还有一点值得注意：Key 和 Value 的表现并不一样。在 Key 中，少数位置总是携带特别大的数值，普通的四舍五入处理不好它们，因此像 KIVI 这类更好的方法会对 Key 和 Value 采用略有不同的量化方式，把误差压小。不必纠结细节，只要记住：为了效果最好，Key 和 Value 要区别对待。",
                "这个方法的问题是：即便用 4 bit，缓存仍然会随每个 token 增长，超长对话下依然太大。",
            ],
        },
        {
            "type": "h2",
            "title": "方法二：Token 驱逐",
            "paras": [
                "Token 驱逐的意思是：把不重要的 token 的 Key 和 Value 丢掉。Eviction 就是「踢出去」，这里是把不重要的 token 从缓存里踢出去。",
                "打个比方：读一本 500 页的小说，我们不会记住每一句话，而是记住重要的角色、重要的事件，以及刚读过的最后几页，其余的都忘掉，但依然完全理解这个故事。同样的道理，并不是每个历史 token 对写下一个 token 都同等重要：模型计算 Attention 时，大部分注意力集中在少数 token 上，其余 token 几乎得不到注意力。既然如此，为什么还要把它们留在缓存里？",
                "那怎么知道哪些 token 重要？答案就在 Attention 本身：模型每写一个新 token，都会算出每个历史 token 获得了多少注意力。我们可以为每个 token 累计这个数值，累计最高的就是重要 token，也就是所谓「heavy hitters」（高频命中者），这正是 H2O（Heavy Hitter Oracle）技术做的事。",
                "规则因此很简单：固定一个预算，比如 1000 个 token；永远保留最近的 token，因为最近的上下文几乎总是重要的；在更早的 token 里只保留 heavy hitters；其余的丢掉。这样无论对话多长，缓存都不会超过 1000 个 token。",
                "但这里有个很有意思的发现：文本最开头的几个 token 总是获得大量注意力，无论它们本身是什么，哪怕第一个 token 只是一个没有意义的起始标记。为什么？因为 Attention 就像把 100% 分配给所有历史 token，总和必须永远是 100%；当没有任何历史 token 真正有用时，模型也得把这份注意力放到某处，于是它学会了倾倒给第一个 token——第一个 token 就像一个「水槽」，接收那些不需要的注意力，这就是 Attention Sink。",
                "如果把这个 token 丢掉，模型输出会变得毫无意义，这是 StreamingLLM 技术发现的。于是最终规则是：永远保留最前面几个 token、永远保留最近的 token，heavy hitters 从中间选。",
                "优点：缓存大小固定，与对话长度无关，这是唯一真正解决「无上限增长」问题的方法。缺点：token 一旦丢掉就永远没了；如果用户之后问到被驱逐 token 里的内容，模型就记不起来，这是真实的信息损失。它适合聊天、摘要一类任务，但不适合需要精确回忆的任务，比如「找出第 40 页提到的电话号码」。",
            ],
        },
        {
            "type": "h2",
            "title": "方法三：跨注意力头共享 Key 与 Value",
            "paras": [
                "前面几种都是在模型训练完成之后压缩缓存，这个方法不同：它在训练之前就改变模型本身的设计。",
                "回想一下：每一层有很多注意力头，每个头都有自己的 Key 和 Value；在例子中，32 个头意味着每层有 32 套 Key 和 Value。问题是，这 32 个头真的都需要各自独立的 Key 和 Value 吗？答案是不需要。研究发现很多头可以共享同一套 Key 和 Value 而效果依然很好，只有 Query 需要每个头各自保留。",
                "再用图书馆打比方：32 个学生各自带着不同的问题（Query）走进图书馆，但图书馆不需要为每本书准备 32 份副本，所有学生看的是同一个书架、同样的书名与内容，区别只在各自的问题。",
                "具体有两种做法。多查询注意力（MQA）：所有头共享一套 Key 和 Value，例子中 32 个头共用 1 套，缓存缩小 32 倍。分组查询注意力（GQA）：把头分组，每组共享一套 Key 和 Value，例子中 32 个头分成 8 组就是 8 套而不是 32 套，缓存缩小 4 倍。MQA 压得更狠但会损失一些质量，GQA 是折中方案，拿到了大部分内存节省而几乎没有质量损失，这也是 Llama 3、Mistral、Gemma 等现代模型普遍采用 GQA 的原因。",
                "优点：不丢弃任何 token、没有舍入误差；节省是内置在模型里的，运行时不需要额外工作。缺点：必须在训练模型之前决定，无法在不重训的前提下用于已有模型；而且缓存仍会随每个 token 增长，只是速度变慢。",
            ],
        },
        {
            "type": "h2",
            "title": "方法四：低秩压缩",
            "paras": [
                "低秩压缩的意思是：只存 Key 和 Value 的一个小型压缩版本，需要时再展开还原。低秩意味着同样的信息可以用更少的数字承载。",
                "打个比方：要给朋友发一张大照片，可以先把照片打包成压缩文件发过去，朋友想看时再解压；压缩文件占空间更小，照片本身几乎不变。这里模型不再为每个 token 存完整的 Key 和 Value，而是存一个称为「潜向量」（latent vector）的小向量——latent 就是「隐藏」的意思，它是 Key 与 Value 的一种隐藏压缩形式；需要 Key 和 Value 时，模型用训练中学到的一小组数字把潜向量展开回来。",
                "看具体数字：例子中每个头为 Key 存 128 个数字、为 Value 存 128 个数字，32 个头就是每层每 token 8192 个数字。用低秩压缩后，模型每层每 token 只存一个比如 512 个数字的潜向量，小了 16 倍。这正是多头潜在注意力（MLA）做的事，它由 DeepSeek 在 DeepSeek-V2 中提出，DeepSeek-V3 也在使用。",
                "你可能会问：先压缩再展开，难道不会损失质量？这正是这个方法的美妙之处——模型从一开始就带着这种压缩一起训练，于是它学会只把有用的信息放进潜向量，重要的东西并不会丢失。",
                "优点：内存节省巨大，不丢弃任何 token，质量与完整 Attention 相当、某些情况下甚至更好。缺点：和 GQA 一样必须在训练前就设计进模型，无法用于已有模型；而且模型需要一点额外计算来展开潜向量。",
                "DeepSeek-V4 把这一思路推得更远：把连续多个 token 合并成一条压缩后的缓存条目。",
            ],
        },
        {
            "type": "h2",
            "title": "四种方法对比",
            "paras": [
                "还有一点值得注意：这些方法并不是互相竞争，而是可以叠加使用。一个模型可以在设计上采用 MLA，再对潜向量做量化，然后再叠加 token 驱逐。下表汇总了它们的差异。",
            ],
            "table": TBL,
        },
        {
            "type": "h2",
            "title": "什么时候用哪一种",
            "paras": [
                "按使用场景来选：如果在跑一个已有模型、想快速省内存，用量化，它最容易加。如果有超长对话、或者文本源源不断没有尽头，用带 Attention Sink 的 Token 驱逐。如果需要精确回忆长文档里的每一处内容，不要用 Token 驱逐，用量化，或者用内置 GQA 或 MLA 的模型。如果要训练一个新模型，从第一天起就在设计里加入 GQA 或 MLA。如果想要最大程度的节省，就把它们组合起来：先选一个带 MLA 或 GQA 的模型，再对缓存做量化，最后在任务允许的情况下做驱逐。",
            ],
        },
    ],
    "conclusion": [
        "**四种方法其实对应四种不同的「省法」：省比特、省 token、省每层的 Key/Value、省每个 token 的向量。** 量化和驱逐可以后加，跨头共享与低秩压缩必须在训练前就设计进模型；前者简单但要留意低比特的舍入误差，后者收益更大但无法惠及已有模型。",
        "真正实用的一点是它们可以叠加：先用 GQA 或 MLA 把基础占用降下来，再量化缓存，最后在任务允许时做 token 驱逐。唯一要小心的是 Token 驱逐会永久丢信息——聊天和摘要没问题，需要「精确回忆起第 40 页的电话号码」这类任务就别用它。",
    ],
    "reference_url": "https://outcomeschool.com/blog/kv-cache-compression",
    "title": "KV Cache 压缩四件套：量化、Token 驱逐、跨头共享与低秩压缩",
}

out_path = os.path.join(_article_dir, "article_data.json")
with open(out_path, "w", encoding="utf-8") as f:
    json.dump(DATA, f, ensure_ascii=False, indent=2)
print("OK wrote", out_path, len(DATA.get("sections", [])), "sections")
