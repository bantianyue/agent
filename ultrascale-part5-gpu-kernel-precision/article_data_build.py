#!/usr/bin/env python3
# -*- coding: utf-8 -*-
# article_data_build.py — The Ultra-Scale Playbook 第5篇（钻进 GPU 内部：内核、内存、融合与低精度）
# 命令式组装 top-level dict -> json.dump 到 article_data.json
import json, os, sys

_article_dir = sys.argv[1] if len(sys.argv) > 1 else os.getcwd()

DATA = {
    "summary": [
        {"key": "从 PyTorch 到 CUDA 的一段渐变", "body": "想写新算子或加快某个函数，从 torch.compile 起步最快：一行装饰器就换来接近 C 的实现，还能导出它生成的 Triton 内核作起点。再往上就是 Triton 与 CUDA，语言越底层越灵活，也越能顶着设备峰值跑。"},
        {"key": "四条传统手艺", "body": "内存合并：让整束 warp 的 32 个线程去抓一串连续地址，吞吐能抬高约十倍。切片（tiling）：把 A、B 的块先搬进高速共享内存，供块内所有线程复用。线程粗化：让一根加粗线程替几根瘦线程多干几份活，把反复触发的共享内存访问压下去。再配一条纪律：同一个 warp 内的线程绝对不要各走各的分支。"},
        {"key": "能省一次往返就省一次", "body": "把一连串逐点运算打包进单一内核（融合内核）能大幅减少与全局内存的来回报到，FlashAttention 正是这套理念的集大成者：它把 S 与 P 这两张大矩阵彻底不落回 HBM，直接在 SRAM 里算，慢了可不止一点。"},
        {"key": "位数不是越低越好", "body": "混合精度并非全面降位：主权重、梯度与统计量往往得留在 FP32。闪点在于把算子切到 FP16 乃至 FP8 上去跑：速度翻倍，代价是溢出、下溢与稳定性。DeepSeek-V3 用按分片归一化范围的方法，第一次把 FP8 大规模预训练跑通。",
        },
    ],

    "lead": [
        "这一路我们从调度与布线的高度看怎么把模型摊上数百乃至上千张 GPU：哪儿摆张量并行、哪儿走流水、通信又怎么趁计算的空档悄悄流过。可所有这些讨论都默认了一件事，每一“次”矩阵乘本身都能按硬件希望的那样又快又省地完成。真正的最后一公里，藏在每块加速芯片的内部：当一个 kernel 被派到某颗流式处理器上时，线程到底怎么排队、数据怎么从最慢的显存一点点挪到最快的寄存器。这篇就把灯关掉、直接钻进 GPU 那张蓝图里走一遍：先弄清一颗现代加速芯片的计算与存储那层层嵌套的组织方式，再从 PyTorch 一路写到 CUDA，看为了一点点把内存访问捋顺，社区攒下了哪些技巧；随后讲清楚把一串算子熔成单个内核之后，如何点亮像 FlashAttention 这样的工程杰作；最后落到谁都绕不开的数字表示上，把 FP16、BF16 直到 FP8 的混合精度训练的取舍一次讲透。",
        "和前几篇一致，原文里那些博客配图与工具截图若信息量有限、或属于交互式示意，我们都用文字转述它们实际想要表达的结论，不伪造任何静态帧；正文七张配图全部取自原文真实矢量图与截图，未作杜撰。公式、数据点与那一长串对比表尽量原样保留，方便与前几篇、以及后续收尾篇对照。",
    ],

    "sections": [
        {
            "type": "h2",
            "title": "一块现代 GPU，内存与算力都层层嵌套",
            "paras": [
                "在这本书之前的章节里，模型算得动靠的是我们在各种加速器之间挪运算，眼里只有粗略的内存总量和高层的调度；可那些做在“每一分运算到底怎么落地”的更底层优化，全都跳过了。这里补上这一课。GPU 的结构高度分层：算力这一侧，它由一整排称作流式多处理器（SM）的计算单元组成，每个 SM 又管着一组流处理器也就是内核中说的核。以一张 H100 为例，它有 132 个 SM、每 SM 128 核，拢共 16896 个核，每个核还能同时扛下多条线程。内存一侧同样讲究：寄存器最小且只归当前线程私有；共享内存与 L1 缓存为同一 SM 内跑的线程共用；往上还有全体 SM 共享的 L2 缓存；最外层就是显存里那块最大的全局内存，也就是宣传单上写着 80 GB 的 H100 那玩意，也是访问最慢的一位。GPU 的本事，正来自顺着这套分层把尽量多的活并行铺满那些核。",
                "在 GPU 核上执行的一段代码叫内核（kernel），可以用 CUDA 或 Triton 这类高层语言来写，再被编译成名为 PTX 的低层汇编供 NVIDIA 芯片执行。真正跑起来还要一份跑在 CPU 上的主机代码去准备数据分配、把数据与代码送上设备。内核的调度规矩值得记住三层：先把线程以 32 为一组绑成 warp，同一 warp 内的线程锁步地执行同一条指令、只是各算各的那份数据；warp 再聚成大小更可调的块（如 256），每块整个交给一个 SM；一个 SM 能同时并跑好几块，但资源一紧张，后到的块得排队等派。记住我这一串规模与分配的约束共同决定了怎么用才最贴硬件。多数时候你不必钻到这层精度，社区里现成的内核尽可直接复用，所以下面我们只拉开一扇起步的小窗。",],
        },
        {
            "type": "h2",
            "title": "想提升性能，先学会借现成的手，再学自己动刀",
            "paras": [
                "手头想加一个还没有现成内核的新算子，或是嫌某个 PyTorch 函数太慢，从头写高吞吐的 CUDA 内核既费经验又陡得很。更顺的起点是我早就聊过的 torch.compile：它动态捕捉你的运算，把那段代码实时化作 Triton 里更底层的优质内核，你只需在函数头顶加一行 @torch.compile 装饰器。为了讲得具体，我们拿激活函数 ELU 做例子，它的定义是输入为负时取 e 的 x 次方减一，为证与非负时直接透传 x。先用朴素的 PyTorch 实现一遍，再罩上装饰器，编译版与非编译版的表现差距极大，哪怕只是多了一行注解。下面的对比图里，横轴是参与计算的列数 N，纵轴是耗时或吞吐：编译版本在相当广的规模范围内把未编译版本甩开一大截，这正是 torch.compile 动态优化的威力。",
            ],
            "fig_after": {
                "0": [{"src": "torch-compile-triton.png",
                       "caption": "ELU 前向的耗时对比：加了 @torch.compile 之后，随列数 N 变化的计算耗时远低于朴素的 PyTorch 实现，动态生成底层内核带来可观的收益"}],
            },
        },
        {
            "type": "h3",
            "title": "把它自动生成的内核顺手捡起来当起跑线",
            "paras": [
                "要是编译带来的提速仍不够，就该考虑亲手上 Triton 内核了。起手式很简单：先把 torch.compile 生成的那份 Triton 代码抄出来当底稿。如何办到？设上环境变量 TORCH_LOGS 为 output_code，再跑一遍带装饰器的脚本，控制台就会吐出对应当前算子的 Triton 内核。长这样的一段逻辑里，tl.program_id(0) 给出一份全局唯一的块号，由块号算出本块要处理的片段起点 block_start，再借 tl.arange 构造这块范围内的索引，用一根 valid_mask 掩掉越界的尾块，安全地 tl.load 装入数据，套上 ELU 的判别式与指数函数，最后 tl.store 写回。为了好读，你尽可把这套生成的代码改一改变量名、补上注释，或者干脆请个大模型来代劳，审一眼逻辑对得上就行。",
                "有意思的是，对我们这个例子，独立拿去用 triton.testing.Benchmark 一测，这份单独拆出来的内核在小尺寸下甚至比 @torch.compile 还略胜一筹，只不过这多半是「包含编译时长」与「纯运行计时」算法的产物。记住这招：从一开始写，而要从自动生成的内核起步，把力气花在优化它身上，能替你省下大把时间。",
                "再往下，Triton 也并非无所不能。语言层面对共享内存的摆放、以及 SM 内部的调度这类底层细节够不着，你只能在块与块跨 SM 的调度上使力。要拿到最深层的控制，就得直接写 CUDA，那里底层一切触手可及。从上往下排成一个望远镜：PyTorch 最容易但最慢；torch.compile 容易又快，却不够灵活；Triton 更难一些、更快也更活；CUDA 最难，但只要你写对了，它就是最快、也最能随心所欲的那一位。下面我们拣几条在 CUDA 里最常见也最划算的手法来讲。",],
        },
        {
            "type": "h3",
            "title": "内存合并：先让一束线程去抓一串连着的地址",
            "paras": [
                "头一条是优化内存访问路径。全局内存在上面的记忆谱里又慢带宽又低，几乎是绝大多数应用的头号瓶颈，把它怎么访问理顺，收益立竿见影。硬件一侧，全局内存由 DRAM 实现，而 DRAM 有个天生的脾气：你一旦去读某块地址，它并不只给那一格，而是把那一格连同它后面一连串的相邻单元用片内多路传感器并行读出，再整体以一段突发方式快速交给处理器。内存合并正是吃透了这个突发特性：设法让同一个 warp 内那 32 条线程去访问彼此紧挨的内存位置。比如线程 0 读地址 M、线程 1 读 M 加一、线程 2 读 M 加二……硬件就能把这一束请求并成一次大的、高效的 DRAM 突发访问，而不是一趟趟各自单独取。",
                "拿矩阵乘示范最清楚。最简单的朴素实现是让每根线程算输出矩阵里的单个元素：对着 A 的整行与 B 的整列各做一次内积。把它丢给 ncu 这类剖析器，立刻能看到两项毛病：内存吞吐偏低，且有大片未合并访问。原因在于同一块的 (0,0) 与 (1,0) 两根线程（它们落在同一个 warp 里）会在迭代里都去读 B 的某一整列、却各读 A 的不同行。矩阵按行主序存（一行的元素在地址上紧紧相连），于是线程 (0,0) 读 A_{0,0}、线程 (1,0) 读 A_{1,0}，这两个地址隔着老远，每一轮迭代都不对齐，内存访问无从合并。",
                "改法是把坐标的算法重新捋一遍：改用一维而非二维的块，让那束落在同 warp 的线程（threadIdx.x 差得不多）共享同一个行号、各自错开列号。这样它们读的是 A 的同一行、B 的不同列，天然满足行主序的连续布局，内存访问随即合并。再拿 ncu 一测：未合并访问的告警没了，GPU 的内存吞吐抬升了差不多十倍，内核的执行时间也同步降到一个量级以下。几行坐标改写的魔力，可见一斑。",],
            "fig_after": {
                "1": [{"src": "tiling.png",
                       "caption": "分布式矩阵乘的 tiling 示意（改编自通用计算科普）：每轮迭代由块内线程合作把 A 的 BLOCK_M x BLOCK_K 片与 B 的 BLOCK_K x BLOCK_N 片一次搬进共享内存，再在片内反复复用做运算"}],
            出价：几行坐标改写的魔力，可见一斑。",],
                    },
                    {
                        "type": "h3",
                        "title": "再进阶：tiling、线程粗化，还有一个大原则",
                        "paras": [
                            "第二招叫切片或平铺（tiling），核心是用上那份小而快的块内共享内存。在矩阵乘里，块内每根线程都要碰 A 与 B 两边的数据，若都独立去全局内存取，同块线程取到的必然是大量彼此重叠的片段，冗余读取惊人。tiling 反过来做：每次迭代让块内所有线程合作，把 A 的一块与 B 的一块一次搬进共享内存，随后整块线程就在那份共享数据上反复算；中间结果攒进累加矩阵，算完当前片再推进，直到把 A、B 各片都过完。而把 threadIdx.x 派作列号，能让同 warp 顺带把访存也合并了。照它的实现跑下来，内存吞吐升到每秒约 410 GB，内核耗时又缩了近 43%，总吞吐摸到约每秒 6.6 万亿次浮点运算（TFLOPs）。下面这张示意图把这个切片复用的节奏画得很直观：每次循环先合作把一块 A、一块 B 搬进共享内存，再在片内算部分和。",
                            "第三招叫线程粗化（thread coarsening）：纯靠 tiling 还不够究竟。若去细数 warp 各状态的停滞周期数，会看到醒目的 MIO 队列停滞：那些走共享内存访问、特殊数学指令、动态分支的指令把队列堵满了。官方剖析文档把它读作「warp 正等着 MIO 指令队列别太满」，而共享内存访问若是主因，最直接的药是改用更少却更宽的访存请求，也就是把好几根线程并成一根粗化线程，让这根更粗的线程多管几份输出元素，共享内存访问次数随之骤降。",
                            "动手改写或打磨自写内核时，还有最后一条不可忘的大原则：把控制流发散（control divergence）压到最低。一颗 SM 按单指令多数据的模型跑，同一时刻给整束 warp 取一条指令、全体同步执行；发散就是 warp 里不同线程走了不同的去路（比如 if 分支把一批人引到别的代码块），硬件只得把几条路径串着跑，另一边线程干等，糟蹋算力。所以内核设计要尽量让同 warp 线程总走同一条路径：削减无关分支、用保证同路径的数据结构、或者用预判（predication）一类招数兜底。",
                        ],
                    },
                    {
        {
            "type": "h2",
            "title": "融合内核：能一把算完的事，绝不来回倒腾",
            "paras": [
                "前面反复提过 GPU 与 CPU 的运作可以异步：主机代码能不等结果就非阻塞地把活源源不断派给 GPU。异步的妙处在于把通信和计算叠到一起，而它还能往更普遍处延伸一步，也就是尽量别让数据和执行来回在主机与 GPU 内核之间传。一串内核若每一步都把中间结果送回全局内存、再由下一个内核重新读回，其实大多是无谓往返。更聪明的做法是让 GPU 尽可能自治：把一串相邻的逐点运算一口气打包进同一个内核去一气跑完，这就是「融合内核」。它在逐点类运算上尤其得心应手，因为这些运算对每个输入 token 各自独立，压根没有把算好的值先放回全局内存、再搬进 SM 内存重开一炉的必要；把值都留在手边，直到这一串算完再用才是对的。Transformer 里能塞进这种融合的地方遍地都是，凡是接二连三出现逐点运算的地方都算，比如 LayerNorm 那一整串规整化与缩放。下面配图用三角形做个简化的比喻：左侧是逐个算子来回倒腾原图又读回的笨办法，右侧则是把这一连串操作一把做掉的融合版。理解了它身上这套理念，才算攒足去见那尊内核工程杰作的审美的资格，下一节的主角闪亮登场。",
            ],
            "fig_after": {
                "0": [{"src": "fused_kernels2.png",
                       "caption": "融合内核示意（源自 Horace He 的图解）：与其把每个算子的中间结果送回全局内存再读回，不如把一排相连的逐点运算合成单次执行一步到位"}],
            },
        },
        {
            "type": "h2",
            "title": "FlashAttention 1 到 3：把注意力的每一步都留在 SRAM 里",
            "paras": [
                "FlashAttention 是 Tri Dao 等人为注意力机制提交的定制 CUDA 内核，目的既快又省内存，思路是把 GPU 那多层次存储物尽其用，别去过分依赖最慢的那一层全局内存。值得先提醒一句，这片全局内存有个颇为反直觉的官方爱称，叫「高带宽内存」HBM，名字虽响亮，带宽与 SM 上高速的静态随机存取存储（SRAM）相比仍是慢得不是一点半点。朴素版本的注意力要我先把查询、键、值使劲算出来，还得把打分矩阵 S 与分母的归一化矩阵 P 整个地落到 HBM 里再读回；而 S 与 P 在长上下文下是模型里数一数二的大激活张量，单这一条就足够把注意力堵得死死的。",
                "FlashAttention 的做法是反其道而行：它把 S 切成一小块一小块，正好塞得进 SM 那块小得多的 SRAM，甚至干脆不再把整张 S 落回内存，只保存算 softmax 归一化所需的那些统计量，于是部分输出 O 能在 SRAM 里一步就地算完，不用在中途来回搬。这样不仅省了共享内存的心，还一举解开了「要把模型里最大激活矩阵之一（长上下文时正是注意力矩阵）整张实体化」的那个内存瓶颈。下图是 FlashAttention 论文的经典示意图：左半是朴素做法，Q、K、V 与大块 S、P 在 HBM 里不断往返；右半是 FlashAttention 的片式计法，仅把 O 分块存回 HBM，所有过渡全在片上完成。",
                "因命中要害而又快而准，FlashAttention 今天几乎成了所有 Transformer 的默认注意力实现：它既免去了把 S 实体化的内存负担，也清理掉注意力朴素 O(S 平方) 代价里相当一块的影响。也正因如此，那些旨在近似注意力的线性注意力与亚二次方各路奇招，几乎都被这精确又快的实现挤到了一旁。",
                "继第一版之后，同一团队又相继放出 FlashAttention 2 与 3。后两版与第一版比，改得已不是注意力机制本身，而是更精心地贴着 GPU 的底细打磨：一是尽量把非矩阵乘的杂活压到最低，二是为 FlashAttention 2 把负载仔细按 warp 与线程块切开，并在 FlashAttention 3 里专门为最新 Hopper（H100）架构优化对 FP8 与张量核的支持。要说代价，FlashAttention 对能被加速的注意力模式有些挑食，不是每种模板都能用；若你既要快又想顺带保灵活，不妨看走同样路线的 FlexAttention。把它来回端详之后就能明白，正是把硬件内部那点存储/算力的身段读得透透的，一笔看似不可能的提速才有了来路。",
            ],
            "fig_after": {
                "0": [{"src": "flashattn.png",
                       "caption": "朴素注意力的内存流动：打分矩阵 S 与归一化矩阵 P 被整张实体化并写入 HBM，再读回做后续计算，绕不开全局内存慢速的往返"},
                       ],
                "1": [{"src": "flashattn2.png",
                       "caption": "FlashAttention 的片式算法（取自 FlashAttention 论文）：S、P 不落 HBM，把关键统计量留在片上、在 SRAM 内分块把部分输出 O 就地算好再写回"}],
            },
        },
        {
            "type": "h2",
            "title": "混合精度训练：低位有红利，也有它的价",
            "paras": [
                "前面几乎每一页都念过低位数格式对存激活、参数与优化器状态的内存压力，现在该把格式细节打开摊白，把它们的取舍、优势与局限一次说清。所谓混合精度训练，说白了就是训练时混着用好几种精度。PyTorch 张量默认是单精度浮点也称 FP32：每个数占 32 位 4 字节，这 32 位又切成三份，符号位用一位决定正负，尾数（mantissa）决定一个数有几位有效数字，指数（exponent）管这个数的数量级。这跟科学计数法是一回事。既然位数可以减少，麻烦只在于从哪儿省：是省尾数还是省指数。",
                "把常用格式摆到同一张表上，字节分配一目了然：FP32 用 32 位、1 符号 8 指数 23 尾数；FP16 用 16 位、1 符号 5 指数 10 尾数；BF16 同为 16 位、1 符号却多留了 8 位指数、只剩 7 位尾数，那开头的 b 来自 Google Brain，取 brain 的首字母。回到 8 位，又因怎么留位分出两种：e4m3（4 指数 3 尾数）与 e5m2（5 指数 2 尾数），好让人按需挑更贴手的那款。以可表数量级说，FP32 横跨约 80 个数量级，FP16 为顾尾数砍掉一大截范围；BF16 保住了 FP32 那么多指数、把全体数量级完整搬了下来，代价是尾数更稀；两只 8 位格式范围更窄，其中 e5m2 保住了 FP16 的范围，e4m3 还要再收一档。",
                "为何有的格式范围保得住、有的保不住？把 1 到 2 之间均匀抽一万个点，各自舍入到最近的可表数去，就能把分辨率摊到纸上。BF16 靠牺牲精度换来了 FP32 般的范围，FP16 则窄而密；到 8 位更惨烈，区间 1 到 2 里 e4m3 只落得下 7 个数、e5m2 只剩 3 个。测分辨率常用一把尺叫 epsilon，指 1.00 以后第一个能表示的数：对 FP32 它约在上界 1.19 乘 10 的负七次方（图中粗略写作 10 的负四次方），FP16 约 10 的负三次方，BF16 又比它再高十倍不止。下面这图把这层意思一次画清：横轴每个格式下 1 到 2 里那一点点的分布有多密，越稀说明一个格式越常在相邻数量级间「跳格」。",
                "那么混合精度到底怎么个混法？答案是：我们没法把 FP32 全撤掉，通常要在某些环节保留满精度，这正是它被叫做「混合」的原因。接下去，先从 16 位看起，再判断能否一路探低到 8 位。",
            ],
            "fig_after": {
                "2": [{"src": "mixedprecision_2.png",
                       "caption": "数值分辨率示意：在 1 到 2 之间取大量样本各舍入成每个格式最近的可表数，可见 BF16 保住范围但点更疏、FP16 点数更密而范围更窄，FP8 的 e4m3 与 e5m2 点数稀少"}],
            },
        },
        {
            "type": "h3",
            "title": "FP16 与 BF16 训练：来自论文的三道秘方",
            "paras": [
                "把所有张量与运算一股脑切成 FP16，训练多半会直接发散。当初发表混合精度训练的论文为此凑足三招，好让低精度配得上 FP32 的精度。头一招是保留一份 FP32 版的权重：训练中会有权重被降得很小、一舍入就归零；但即便权重本身没贴近零，只要更新步长远小于权重自身数量级，两者相减时尾数也会先一步被抹空，一旦归零后面再没有梯度信号，就一辈子是零了。第二招是 loss scaling：梯度天生的身材普遍不足 1，是下溢（underflow）的常客。简单有效的对策，是在反向传播之前先把损失值放大若干倍，反向结束后再把梯度缩回来；因为缩放早在后续裁剪或优化步骤之前就被挪开，并不影响训练。第三招是累加：某些 16 位下的求平均、求和会把中间过程再做下溢或上溢，于是改在浮点 32 里把中间结果攒着，只把最终结果降回 16 位。有这三招就稳住了训练，还白赚低精度算术那种又快一步的吞吐。",
                "可你既然读到这里，只怕又会捧着杯子问一句：能不能比 16 位再快、再低，探到 8 位？答案是：或许可以。",
            ],
        },
        {
            "type": "h3",
            "title": "FP8 预训练：另一片战场，稳定是最大的坎",
            "paras": [
                "就算把通信与计算的每一丝缝隙全部叠满，总归要被硬件那张低层的理论 FLOPS 大顶拦住，这时数字精度就成了分水岭。以 H100 为例，FP8 的矩阵乘（GEMM）理论吞吐是 BF16 的两倍，把低精度拿来接着压榨吞吐的路径自然越发诱人。近期的研究，从 FP8-LM、torchao 到 DeepSeek-V3，都展示了 FP8 训练在大模型上的潜力；但 FP8 预训练横着一个极难驯服的硬骨头：稳定性。精度一降、数值动荡常把损失曲线拉得分崩离析，想追平高精度训练的准度并不容易；而且固定模型规模下学习率抬得越高、这种动荡越是猛烈，这让 FP8 预训练格外挑食。典型 FP8 训练那张分叉的发散损失曲线，几乎成了失败的标配开场白。",
                "公开报道中第一个成功做到超大规模 FP8 混合精度训练的，是 DeepSeek-V3。作者们逐算子复盘前向（Fprop）以及反向那两段，激活的回传（Dgrad）与权重的回传（Wgrad）全都拆开细究；与 BF16 混合精度一个路数，部分聚合与主权重留了高精度，真正的大头运算则放 FP8 上跑。由高往低切换、可表范围又收窄时，你得先动手把激活值的范围规整齐，比如取它的绝对最大值做标度。DeepSeek-V3 更另起一套量化方案：范围按分片一层层归一，输入与激活用 1 乘 128 的分片、权重与标度元素用 128 乘 128，好让个别离群值不至于把归一化带偏太狠。它另有一串为压内存与通信而生的巧思，全落在那份技术报告第 3.3 节。我们把几套主流的 FP8 方案的账一并排到一张对照表里（按权重、梯度、优化器状态的字节数各占多少），一眼能看出 Transformer Engine 与 FP8-LM 的 O3、DeepSeek-V3 与 nanotron 各藏了什么内存身段，Reduction 往往在 16-25% 到 50% 之间浮动。",
                "还需为读者把这张几套名声在外的低精度路线按 GEMM 精度、主权重、各状态所占内存一节节排开。底线是：到了 2025 年初，FP8 仍算一件实验中的手艺，方法还在快步辗转；鉴于收益一目了然，它早晚会站稳脚跟、接替 BF16 混合精度成为默认。想要一份开源实现可去翻 nanotron 相关 PR；再把目光投向明天一点点，Blackwell 这一代 NVIDIA 新品已公开宣布支持 FP4 训练，提速有望更进一步，只是毫无疑问又会带来新一轮训练稳定性的难题。",
            ],
            "fig_after": {
                "1": [{"src": "fp8_diagram.png",
                       "caption": "DeepSeek-V3 的逐分片量化示意：范围按输入 1x128、权重 128x128 的分片分别归一，让个别离群的激活不至于绑架整段标度"}],
            },
            "table": {
                "head": ["方案", "GEMM 精度", "主权重", "梯度累加", "模型权重", "梯度", "优化器状态", "每参内存"],
                "rows": [
                    ["BF16+FP32 基线", "bfloat16", "fp32", "fp32", "bf16", "bf16", "fp32+fp32", "4+4+2+2+4+4=20 字节"],
                    ["去掉梯累 FP32", "bf16", "fp32", "无", "bf16", "bf16", "fp32+fp32", "4+2+2+4+4=16 字节"],
                    ["Transformer Engine", "fp8", "无", "无", "fp32", "fp32", "fp32+fp32", "4+4+4+4=16 字节（省20%）"],
                    ["FP8-LM O3级", "fp8", "fp16", "fp16", "fp8", "fp8", "fp8+fp16", "2+2+1+1+1+2=9 字节（省55%）"],
                    ["DeepSeek-V3", "fp8", "fp32", "fp32", "fp8", "bf16", "bf16+bf16", "4+4+1+2+2+2=15 字节（省25%）"],
                    ["nanotron FP8", "fp8", "bf16", "fp32", "fp8", "fp8", "fp8+fp8", "2+4+1+1+1+1=10 字节（省50%）"],
                ],
            },
        },
        {
            "type": "h2",
            "title": "小结：从集群的骨架，落回一枚芯片的魂",
            "paras": [
                "这个章节至此把低层那层推进到顶点：为了把训练跑得更快，我们先后做过模型代码层的改动、亲手为若干算子定制内核，最终又学到一连串彻底不挑模型、谁都能用、且早已成为行规的标准手艺，也就是混合精度训练。它们围绕两条主线互相咬合：一是把内存访问捋到跟硬件存储分层严丝合缝，让 DRAM 突发、共享内存复用、SRAM residency 各得其所；二是靠更抠门地把位数花出去，用更快的低精度算术换取吞吐，同时用三条小药方和满精度的底座把数值稳定性稳稳托住。FP8 甚至 FP4 的下一站就在眼前：更快是真的，但每一份提速都要拿训练稳定性去换，新的平衡练起来总得重来一遍。这也正好接上我们这一整本书的主线：从单卡缓缓站起，到几千张卡的浩荡行列，环环相扣的优化从来没有止于某一条指令，总要落到一整套工程手感的磨砺上。",
            ],
        },
    ],

    "conclusion": [
        "这一篇算我们技术漫游里最深的一层：离开「把模型摊上几百上千张卡」的调度高度，一头扎进单枚芯片的腹地。先是看清现代 GPU 那层层嵌套的计算与存储组织方式，随后从一行 @torch.compile 装饰器起步，走完 PyTorch、torch.compile、Triton 直到 CUDA 的进阶之路，把内存合并、切片、线程粗化与控制流发散四条正经手艺一一试过，学会了写高效内核最重要的纪律，也明白了为什么要尽可能把连串的逐点运算熔成单一内核。真正的高潮在 FlashAttention 身上：它是把存储分级读得最透的样本，把它那套「别把 S 与 P 实体化、就地分块算 softmax」的招式看明白，许多看似贴着火线到底的提速便有了来由。",
        "把视线再落到浮点表示这座曾经的「默认雷区」上，从二进制里符号、指数与尾数的牵制，一路辨清 FP16、BF16 与两类 FP8 身上「范围、分辨率与稳定性」三者的取舍：BF16 保得住数量级而牺牲精度，混合精度这道题因此总得留一块 fp32 的底座给主权重、梯度与统计量。16 位那三条来自原论文的秘方值得被背下来，FP8 里 DeepSeek-V3 那份逐片量化更替整片规模化 FP8 预训练钉下了第一根标杆。这套手艺并不孤立于我们前几篇聊过的分布式框架，恰恰相反，它说明无论把并行铺得多开、通信藏得多妙，最终都得由每个内核在每块芯片上的每一次取数与每一次运算去兑现。打完这层，我们只需再花一篇做收尾的盘点与眺望，就能替这条从单卡到集群的漫漫长路合上账本。",
    ],

    "reference_url": "https://huggingface.co/spaces/nanotron/ultrascale-playbook",
    "title": "钻进 GPU 内部：从一行 @torch.compile 到 FlashAttention，再到 FP8 混合精度的斤斤与两两",
}

out_path = os.path.join(_article_dir, "article_data.json")
with open(out_path, "w", encoding="utf-8") as f:
    json.dump(DATA, f, ensure_ascii=False, indent=2)
print("OK wrote article_data.json chars=", len(json.dumps(DATA, ensure_ascii=False)), "sections=", len(DATA["sections"]))
