# -*- coding: utf-8 -*-
import os,json
D=os.path.dirname(os.path.abspath(__file__))
S=[]
def h2(t): o={"type":"h2","title":t,"paras":[]};S.append(o);return o
def t(o,*p): o["paras"]+=list(p)

h2("开场")
# noted below
s=h2("1. 数字与精度：硬件怎么存一个数，存不下时会发生什么")
t(s,"作者说：我一开始也把这章当琐碎知识跳过，直到花两天调一个量化后乱输出的模型，根因恰恰在这里。所以请早点学。")
t(s,"1.1 一个浮点数怎么搭起来。浮点数用三段存：一个符号位、若干指数位、若干尾数位。指数位决定你能表示多大的数/多小到哪；尾数位决定你留住几位有效数字。每种格式只是把可用 bit 按不同比例分给这两件事。理解了这个取舍，就能预测任何一点格式会怎么表现。")
t(s,"1.2 你会碰上的格式。FP32：8 个指数位加 23 个尾数位，可靠默认，但慢、且吃显存。FP16：内存减半，但只有 5 个指数位，能表示的数值范围小，值更容易溢出成无穷或缩成零。BF16：保留 FP32 的 8 个指数位、让出的是尾数位，范围和 FP32 一样但精度更低；现代多数工作默认它，因为丢精度只是加点噪，丢范围会产生一路传染的无穷。FP8 有两个变体 E4M3 与 E5M2，名字本身就告诉你位数拆分，E4M3 即 4 指数 3 尾数。INT8、INT4 是整数，可表示值之间的间距是固定而非相对的，要表示小数需要单独一个 scale factor。")
t(s,"1.3 舍入与后果。每次算术都会把结果舍到最接近的可表示值，引入小误差。长串运算里这些误差累积，这也是为什么长向量在 FP16 下求和会漂离正确答案、快的 kernel 通常即便输入低精度也把 running total 留在 FP32。还要正确理解溢出与下溢：FP16 里单个异常大的激活值就能超过上限变成无穷，之后一切都被毁掉。")
t(s,"1.4 epsilon 与消除性相消。Machine epsilon 是 1.0 附近两个可表示数的最小间距，它告诉你两个 float 得靠多近，比较才不至于失去意义。灾难性消除发生于两个几乎相等的数相减，前几位对消、剩下的基本是舍入误差。它出现在归一化层与方差计算里，教科书公式常在减两块大的相近量，数值稳定的版本会重排算术躲开。")
t(s,"1.5 稳定 softmax。softmax 要对每个输入取指数，input 到 90 左右指数就在 FP32 下溢出。标准修法是先把最大输入值从全体中减去再取指数，数学上答案一样，因为软分配对整体平移不变。这个技巧的一般化是 log-sum-exp，值得真正学会而不是只背 softmax 的修法，它是稳定算对数概率的基础；FlashAttention 里的 online softmax 也是这个思想的变体。")
s=h2("2. 量化数学：把权存进更少的位")
t(s,"量化是你大部分显存与带宽节省的来源，核心是一次线性映射，比论文名好懂得多。")
t(s,"2.1 映射。把一段 float 值域映射到一小批整数，两个数定义它：scale 决定每个整数步覆盖多少真实值，zero point 决定哪个整数对应真正的零。量化是除以 scale、加 zero point、再舍入；反量化则反过来。几乎每种量化方法，本质都是“怎么把这两个数选好”的不同策略。")
t(s,"2.2 对称与非对称。对称量化把 zero point 固定在零、以它居中范围，算术更省，适合权重这种本来就大致居中的数据。非对称让 zero point 可以移动，更能配歪斜分布，例如 ReLU 后的激活永不为负，对称会把一半整数浪费在不会出现的取值上。")
t(s,"2.3 粒度。可以整张 tensor 用一个 scale，也可以每通道一个，或每固定大小的一组值一个，例如 64 或 128。粒度越细精度越好，因为张量某处几个极值不再拖高其它一切；代价是你要存、要处理更多 scale factor。INT4 一般要靠 group 级 scale 才够准到可用。")
t(s,"2.4 怎么选范围。量化前必须先定从哪段 float 范围映射。最省选择是观测到的最小/最大，但单个极值会把范围拉长、留给你在乎的值分辨率很少。Percentile clipping 刻意切掉尾部；更仔细的做法是在候选范围上搜索、留下误差最小者，误差可用平方误差或对原分布的 KL 散度衡量。权重范围因为权重固定能精确量;激活范围依赖输入，要拿一小批校准数据过一遍模型记下所见。")
t(s,"2.5 离群值。Transformer 里极少数激活通道携带比其它高很多倍的值。你若懂得为什么一个大值会毁掉整张张量对其它值的有效分辨率，各种离群处理法的动机就显而易见了：它们要么把离群通道单独拎出来用更高精度，要么把困难在权重与激活之间移动，要么重缩放通道使范围更均匀。")
t(s,"2.6 粗略讲曲率方法。更强的权重量化会利用“输出对哪些权最敏感”的信息，用 Hessian 近似。模型几乎不依赖的权可以随便舍进，重度依赖的权则会被补偿。你用这些方法不必从头推更新规则，但明白 Hessian 被当敏感度估计，就懂它为何比裸舍入强一大截。")
s=h2("3. 面向算力的线性代数：同样那些运算，按代价计算而非按含义")
t(s,"走过训练路线图的人已知矩乘是什么意思，而推理需要知道它要花多少钱。")
t(s,"3.1 数一次 matmul 的花费。m×k 乘 k×n 大约要 2×m×n×k 次浮点运算，因为每个输出元素是一次长为 k 的点积、每步一乘一加。另外你还要把两个输入矩阵从内存读出、把输出写回。要对模型里的形状把手算这两项估算练到顺手。能在 profile 之前就说“这层该大概花多久”，是整个领域最有用的一个习惯。")
t(s,"3.2 矩阵×矩阵对矩阵×向量。矩乘每个载入字节做了很多算术，因为每个载入值被许多输出元素复用；矩阵×向量则每个字节只做一点算术，因为每个权重载一次、用完即弃。对单请求逐 token 生成基本属于后一种情形，这一事实解释了为什么服务语言模型这么难的一大半。")
t(s,"3.3 算术强度。arithmetic intensity 等于运算数除以搬移的字节数，是你第一该查的指标。强度高时芯片的算力卡你；强度低时显存带宽卡你，计算单元空等数据。多数推理负载属于后者，所以大量优化在想怎么少搬字节而非少做算术。")
t(s,"3.4 张量在内存里的排布。需要懂 row-major 与 column-major、stride、contiguous 是什么意思;也要懂为什么 transpose 矩阵会在时间上真花成本(数学上无变化)，以及为什么形状要 pad 到特定倍数才好让硬件的矩阵单元派上用场。")
t(s,"3.5 Tiling。把大 matmul 切成能进片上快内存的小块，并在逐出前尽量复用每块。涉及的数学是“cache 塞得下多少、每块被复用几次”这样的算术，它是你会读到的每个快 kernel 背后的核心思路。")
t(s,"3.6 低秩结构与稀疏。矩阵的 rank 告诉它真正含多少独立信息。奇异值分解(SVD)能用更低秩的版本近似一张矩阵、少占内存，这是低秩压缩与低秩 adapter 的基础。adapter 是在基座权重上叠加的一次低秩更新，所以懂 rank 能明白为什么把它合进基座模型是精确等价的、而同时服务许多 adapter 需要另一套做法。稀疏方面实务要懂：像每四格里两个非零这类结构化 pattern 有硬件支持、真能加速；非结构化稀疏常常即便算术更少反而更慢，因为索引开销盖过了省下的量。")
s=h2("4. 性能建模：在实测前先估运行时间")
t(s,"这章与机器学习最少相关，却是作者用最多的一部分。")
t(s,"4.1 roofline 模型。把可达成性能对算术强度作图会得两条界：一条斜线代表显存带宽，强度低时卡你;一条水平线代表峰值算力，强度高时卡你。你跑的每个 kernel 落在这两条天花板之一下面。学会把一个负载放上这张图，立刻知道该改进存还是改进算术，能免掉花一周去优化一个从来不是瓶颈的东西。")
t(s,"4.2 带宽受限估算。受内存限制时，运行时间约等于要搬的字节除以设备内存带宽。由此能做的估算：70 亿参数模型以 FP16 存约 14GB；为产出单个输出 token 你要把每个权读一遍，用 14GB 除以 GPU 内存带宽，就得到什么都还没算时的每 token 时间下限。换成 4bit 权再除一遍，下限约降四倍，这也是为什么单请求生成里量化即使不动算术也能提速。")
t(s,"4.3 延迟与吞吐。延迟是单个请求花多久；吞吐是一秒完成多少请求。把 batch 加大会改善吞吐、恶化单请求延迟，所以你是在一条曲线上选点，而非找一个两头都最好的设置。")
t(s,"4.4 Little 定律。L=ρ…表述：系统内平均请求数等于到达率乘每个请求平均停留时长。这是行内公式，答一个非常实用的问题：要达到某个吞吐，需要同时在途多少请求。一旦知道这个数，就知道要替 KV cache 预留多少内存。")
t(s,"4.5 排队行为。不需要多少正规排队论，但要内化一条结论：利用率逼近满载时，等待时间是无界增长而非线性增长。把服务器从 90% 挪到 95%，排队延迟恶化远大于那 5% 的差距，这也是容量规划总要留 headroom 的原因。")
t(s,"4.6 Amdahl 定律。优化流水某一级能拿到的加速，受限于该级占总时间多大部分；开工前算一下，能省下你对只占 3% 运行时间的东西猛使劲。")
t(s,"4.7 跨设备切分。模型拆到多张 GPU 时设备间要交换中间结果。要对 all-reduce、all-gather 这类集合通信花多少有个感觉，并且懂它随设备数与消息大小怎么涨。值得理解的失败情形是：拆了模型反而更慢——你把通信加进来想减的算力，本来就不是卡你的东西。")
s=h2("5. Transformer 推理算术：把上面全用到真实负载上")
t(s,"5.1 行为相反的两个阶段。处理输入 prompt 是一次性进行、每个 token 并行算，每载入字节有大量算术，这段通常算力受限、每 token 成本低。生成 output 是一次一个 token，每一步为产出单个 token 要把整套权重读一遍，每字节的算术很少、是内存受限，每 token 成本高得多。几乎每个 serving 优化都存在，正是因为这两阶段对硬件想要的不一样。")
t(s,"5.2 KV cache 怎么定大小。这个计算决定你能同时服务多少用户，值得背在手边。总量取决于：层数、key 与 value 的 head 数、每个 head 的维度、每存储元素的字节数、序列长度、batch 大小。全乘起来，再乘一个因子 2 因为 key 和 value 你都要存。做完有两件事要注意：它随序列长度与 batch 都是线性涨，且你量化权重也不会让 KV cache 缩小。长上下文服务里 KV cache 是比权重更大的问题。")
t(s,"5.3 attention 何时开始称王。attention score 计算随序列长度平方增长，feed-forward 层随它线性增长。短序列时二次项小到可忽略、feed-forward 主导成本;存在一个交叉长度，过了它 attention 变为主开销。值得大致知道这个点落在你自己常碰的模型形状的哪里。")
t(s,"5.4 减少 key/value head 数。GQA 与 MQA 保留全部 query head，但让多个组共享 key/value head。拿 5.2 的 KV 计算式换小号的 key/value head 数重算一次，省下的内存直接就从算术里掉出来。")
t(s,"5.5 分页与碎片。与其为每个请求按最长可能序列长度预留一整段连续内存，不如随序列增长以小定长块来分配 KV cache。相关量是三样：每个序列里最后那个半满块浪费多少空间、你预留的内存里有多大比例放着真数据、以及共享同一 prompt 前缀的两个请求能互相共享多少内存。")
t(s,"5.6 随时间批处理。静态 batching 攒一批请求一起跑、等全部结束。当请求生成的 token 数不同，短的一早结束、槽位躺到最长那个收工。Continuous batching 在旧请求完成时把新请求填进空槽。它的数学是关于槽位随时间保持多满、以及你能收回多少那段空置容量。")
s=h2("6. 面向解码的概率：sampling 设置对输出分布做了什么")
t(s,"6.1 softmax 与 temperature。softmax 把原始分数变成概率分布；temperature 在 softmax 前先除分数。低于 1 会拉开第一与后面差距、把概率集中到更少 token；高于 1 把分布摊平；趋近 0 时收敛到永远抽最高分。把它想成重塑一个分布、而不是一个笼统的“创意度”旋钮，会更好推演输出质量为何变了。")
t(s,"6.2 切尾。top-k 留概率最前的 k 个 token；top-p(也叫 nucleus)留加起来够到 p 的最小一组 token；min-p 留概率高于最高 token 概率某一比例的 token。三者都是丢弃一部分分布、再对剩余重归一化使之和为 1；区别在于怎么决定在哪里切，尤其切法是否随模型当步的信心自适应。")
t(s,"6.3 熵与困惑度。熵衡量某步分布有多散，是模型多不确定的一个合理信号，也是自适应解码策略的有用输入。Perplexity 是数据集上平均负对数概率的指数，是大家检查量化或优化改动有没有损伤模型时报告的标准数字。")
t(s,"6.4 KL 散度。KL 衡量一个概率分布离另一个多远。实务上用它对比：同一批输入下，优化后模型与原始模型的输出分布。散度小意味着你的提速是无损白拿的；大意味着即便 benchmark 分数碰巧像，你其实改了模型行为。")
t(s,"6.5 投机解码。一个小而快的模型先提出领先几步的若干 token，大模型一次前向全部验证。验证用 rejection sampling，安排成你最终接受的 token 恰好遵循大模型自己会产出的那个分布。这个保证才是最妙的地方，值得读推导。预期加速是一道计算而不是猜：取决于草稿 token 多常被接受、每步提多少个、两个模型的成本比。不把这笔账算清，很容易造出比原方案还慢的东西。")
t(s,"6.6 可复现性。浮点加法不满足结合律，同一串数换顺序加会有略不同的结果。改 batch 会改变 kernel 内部求和的顺序、改 logits 末几位、偶尔改到采到哪个 token。于是同一 prompt 同一 seed 在不同 batch 下可能给出不同输出，这来自算术本身，不是 serving 代码的 bug。")
s=h2("7. 面向基准的统计学：量速度时不骗自己")
t(s,"多数你看到的已发布推理 benchmark，都会栽在下面某一种坑上。")
t(s,"7.1 用分位数而非平均。报中位数、95 分位、99 分位。平均把那些跑糟的请求藏起来，而用户体验的是慢尾而不是平均。另记得估 p99 需要很多样本；只跑了 50 个请求的 p99 就一两个数据点，不该被当作一次测量引用。")
t(s,"7.2 分阶段报告。首 token 时间(TTFT)量用户等第一个字出现多久，由 prompt 处理与排队时间主导；每输出 token 时间量之后的流式速度，由内存带宽主导。还要端到端延迟和总体吞吐。因为 prompt 处理与生成瓶颈不同，单一 tokens/s 数字能把几乎一切藏住，所以要分开报。")
t(s,"7.3 噪声。要有足够基础统计分辨“改进”与“次间波动”：测量上要标准差不-confidence interval，好让 2% 的提升别在相同 run 之间噪声本就 5% 时被宣布成胜利。还要丢掉前几轮 run：最初调用含编译、内存分配、冷 cache，掺进平均会让一切显得更差更噪。")
t(s,"7.4 负载生成。不管前面的请求有没有完成都按固定速率发请求，能暴露系统跟不上时的样子、包括队列怎么堆积。换成保持固定 worker 数、每个等响应再发下一个，反而会把它藏掉，因为慢响应会自动拖慢你的发送速率。要特别查的一个著名坑是 coordinated omission：如果你的负载生成端在等一个慢响应时被阻塞，它恰恰在系统挣扎的那段没有发送，最坏延迟根本没被记到。这悄悄废掉大量已发布数字。")
s=h2("8. 离散数学与位级活：读真实现后才会冒出来的小东西")
t(s,"8.1 位操作。把两个 4bit 值塞进一个字节，再掩码移位取回来。写过一次就简单，没写过之前一直乱，所以写一次吧。")
t(s,"8.2 2 的幂与对齐。块大小常是 16/64/128，原因与内存事务大小、矩阵单元形状有关。要有向上取整除法、up pad 到倍数、和对齐要求的感觉。算术无关紧要，性能后果不是。")
t(s,"8.3 哈希与 cache 命中率。判断两个请求是否共享前缀一般要哈希整块 token。要能推理命中率、以及给定命中率对你的有效吞吐做什么，加上对存共享前缀的树结构有基础理解。")
t(s,"8.4 复杂度记号。够用它发现某个 scheduler 或淘汰策略在做随活动请求数平方增长的工作。你不写证明，只检查在并发一千请求时它是否还能撑得住。")
s=h2("我是怎么学的")
t(s,"作者：上面每一条我都没按那个顺序学，你多半也不会。这是我现在会推荐给从头开始的人的路子。")
t(s,"1. 先把框架拿到手。Horace He 的《Making Deep Learning Go Brrrr From First Principles》把所有东西归成 compute-bound、memory-bound、overhead-bound。最先读它，这个分类是你组织其余内容的骨架。然后开着计算器把 kipply 的《Transformer Inference Arithmetic》过一遍，它有真公式真数字，自己动手算过才是“认识”与“知道”的差别。")
t(s,"2. 学硬件在干什么。Kirk 与 Hwu 的《Programming Massively Parallel Processors》是标准 GPU 编程书；即便你从不写生产 kernel，讲内存层次与 tiling 的章节会改变你对代价的思考。GPU MODE 讲座系列(原 CUDA MODE)免费，展示真人推理真 kernel，这是书里难得到的。计算机体系结构用 Hennessy/Patterson 的 A Quantitative Approach，别通读——讲内存层次与性能测量的章节正是 roofline 推理与 Amdahl 定律出处，要的就这些。")
t(s,"3. 浮点。Goldberg 的《What Every Computer Scientist Should Know About Floating-Point Arithmetic》很老却仍是这个话题最好的东西，文密，分两遍读别试图一遍吸收。然后故意去弄坏东西：写能让 FP16 溢出的小脚本、演示长求和误差累积的脚本、让朴素 softmax 产生无穷的脚本。作者说那十五分钟比阅读教会他更多。")
t(s,"4. 量化。读一篇高效推理量化的综述文收词汇，再读你实际所用方法背后的原论文;scale 与 zero point 一扎实，这些论文大多一顿饭能读完。推荐的练习：对单个权重矩阵亲手实现 quantize/dequantize、测重建误差，然后把每张 tensor 一个 scale 换成每通道一个、看误差掉下来。那个下午比读论文有用。")
t(s,"5. Serving 系统。读 FlashAttention 论文是为 tiling 与 online softmax 的思路，不是 CUDA 细节——它做的事大多是减少内存流量。vLLM 那篇 PagedAttention 论文把“为什么限制你能服务多少用户的不是裸算力而是 KV cache 内存管理”讲得最清。投机解码的论文值得为 rejection sampling 论证一读，那是一段真的漂亮的概率。")
t(s,"6. 测量。Brendan Gregg 的《Systems Performance》与机器学习毫无关系，这正是它有用的原因：教你诚实测量、认真对待尾延迟、躲开标准基准错误。另外单独去查 coordinated omission 随便读读，它改变此后作者读每个延迟数字的方式。")
s=h2("让它真正长进记忆的循环")
t(s,"作者：阅读带我走了一段然后不再奏效。真正有用的是这个。1. 挑一个模型，手算它的权重内存、每 token KV cache、以及在你硬件上每输出 token 的理论最小时间。2. 跑起来量同样的量。3. 想明白你的预测为何错了。第三步才发生学习。每次估计与测量的差距都来自某个你还不知道的东西，去追那个东西比当初那次计算教得更多。")
s=h2("可以放下的东西")
t(s,"基础起点的优化理论用不上：收敛保证、凸性、鞍点分析是训练的事，而推理里权重早已固定。同理可跳过多数贝叶斯统计、以及 Little 定律与 4.5 利用率警告之外的全部正规排队论。手推反向传播是好背景，但这里你不会用它。看着无聊却不可省略的四样：浮点行为、算术强度与 roofline 推理、KV cache 定价、带分位数的诚实测量，它们反复出现。其余列表是你被具体问题逼着时才值得补的深度。")
t(s,"最后一件作者花了很久才接受：训练路线图主要是微积分与概率，而这一份主要是数字节与估时间。你若带着第一种数学预期进场、觉得自己误入了一门系统课，那感觉是对的，这个领域确实如此。")
_ = S.pop(0)  # remove placeholder
# Insert intro section first
intro=h2("一篇文章把 AI 推理工程要的数学讲清")
t(intro,"作者：这篇文章我会拆开讲做 AI 推理工程所必需的数学，也会分享我亲身用过的确切路线图与资料。直接开始。以下各章为全文的完整中文直译，未做概括。")
S.insert(0,intro)
d={"title":"做 AI 推理工程该学哪些数学：一份完整路线图（全文直译）",
 "reference_url":"https://x.com/TheVixhal/status/2097008871231672595",
 "summary":[
  {"key":"这篇是什么","body":"X 上的一篇成体系长文《The Math Needed for AI Inference Engineering (Complete Roadmap)》：作者拆出做推理工程真正要用的数学，共 8 章加学习路线。按用户要求全文 100% 保留、中文直译、不做摘要，本文即其完整直译。"},
 ],
 "lead":["下面这篇 x 文章的中文全文直译，按原文顺序逐章保留：数字与精度、量化数学、面向算力的线性代数、性能建模、Transformer 推理算术、面向解码的概率、面向基准的统计学、离散与位级，以及作者自己的学习路径与可省内容。","因直译保真需要,章节标题尽可能贴近原文；公式与要点、资源一律保留原意。"],
 "sections":S,
 "conclusion":["原文以一句类比收尾：训练路线图主要是微积分与概率，而这一份主要是数字节与估时间。若你带着前者入场却误入一门系统课，就对了——这个领域真就是这样。"]}
json.dump(d,open(os.path.join(D,'article_data.json'),'w',encoding='utf-8'),ensure_ascii=False,indent=2)
tot=sum(len(p) for sd in S for p in sd['paras'])+len(d['summary'][0]['body'])+len(d['lead'][0])
print('sections',len(S),'total chars~',tot)
