# -*- coding: utf-8 -*-
"""SwitchYard 路由基准 build"""
import json, os, sys

DATA = {
  "title": "你的 agent 有多少调用真的需要前沿模型？SwitchYard 路由只花 7% 的调用扛 68% 的账单",
  "lead": [
    "agent 会发很多 LLM 调用，而多数团队把每一次都发给同一个模型。NVIDIA NeMo Switchyard 是一个开源的模型路由库，能在 agent 工作流各步骤间自动化模型选择——不需要前沿模型处理的活，就不发到前沿模型上去。",
    "LangChain 用 Deep Agents 评估套件跑了一遍 Switchyard，测出路由器发往前沿模型的调用只有 7%，剩下 93% 由一个 30B 参数模型处理。在 NeMo 3.5 Lightning 与 Claude Opus 4.8 之间路由，总成本比纯用 Opus 降了 74%，同时保留约 93% 的准确率。下面给出实测数据，以及一个告诉你何时该在自己工作负载上用同一策略的公式。"
  ],
  "summary": [
    {
      "key": "核心结论",
      "body": "并非每一回合都需要最好的模型：实测只有 7% 的调用需要，而这 7% 扛了 68% 的账单。路由把 93% 的调用导向廉价模型。"
    },
    {
      "key": "权衡公允",
      "body": "路由省了 74% 成本、代价是约 6 个百分点的准确率。它值不值，取决于廉价模型与前沿模型之间的价格差是否足够宽、能覆盖 judge 模型的固定开销。"
    },
    {
      "key": "决策公式",
      "body": "最小卸载比例 = judge 成本 / (贵模型成本 − 廉价模型成本)。两个模型价格越接近，公式要求卸载越多（可能超 100%），路由就划不来，除非你本地托管廉价模型。"
    }
  ],
  "sections": [
    {
      "type": "h2",
      "title": "问题的根源：把一切发给一个模型",
      "paras": [
        "过去把每一次调用发给同一个模型是合理的默认项——那时的模型更便宜、能力更接近。但这点已经变了：前沿模型更贵也更强大，而开放权重模型已经快到、便宜到能承接 agent 真实工作的一部分，尽管不是全部。与此同时，一句『读一下这个文件』和一句『查清楚这个测试为什么失败』仍以相同的每 token 价格发给同一个模型，尽管前者微不足道、后者并非如此。",
        "多少回合真正需要那个贵模型，决定了路由是否划算。LangChain 已有针对 Deep Agents 的基准套件，这次只是把基准的指向从『一个模型』换成『一个路由器』。"
      ],
      "fig_after": {}
    },
    {
      "type": "h2",
      "title": "SwitchYard 是什么",
      "paras": [
        "Switchyard（NVIDIA-NeMo 开源）是 NVIDIA 的模型路由库，它基于你给每个步骤配置的策略，把每条 agent 查询自动路由到封闭与开放模型的任意组合。你可以把它当作 agent 指向的一个 proxy 来跑，也可以作为 agent 进程里的 middleware。",
        "它提供两种路由思路（第三种在研究中），在延迟与准确率之间做不同取舍。LLM classifier：一个小的 judge 模型参与路由决策，有三种模式——capability 每次按能力选目标、escalation 每个任务从便宜模型起步、连续坏回合后把会话升级到大模型、custom 由你自定义；它有更高准确率潜力，代价是多一次对小模型调用。Stage router（启发式）：按工作流阶段路由，读错误模式、推理模式与 token 数，不加额外模型调用、延迟几乎为零，但只在你的流量产生它要找的信号时才触发（本篇未基准测试）。Prefill-activation MLP：读 prefill 时的模型内部激活来路由，处于研究阶段而非生产就绪，是 NVIDIA 正在探索的方向。",
        "LangChain 基准测的是 LLM classifier 的 escalation 模式：每个任务先从更便宜的模型起步，一个小 judge 模型读每个完整回合并投票判断 agent 是否在正轨上，连续两次负面判定就把该任务此后改路由到贵模型。这个『单行道』减少路由成本，因为任务一旦升级，judge 就不必再在每个回合上运行。"
      ],
      "fig_after": {
        "2": [
          {
            "src": "fig01.png",
            "caption": "图 1：escalation 路由——两次 'strike' 就把一个任务移到贵模型，并持续到会话结束。"
          }
        ]
      }
    },
    {
      "type": "h2",
      "title": "我们测到了什么",
      "paras": [
        "Deep Agents 评估套件有 145 个多步 agent 任务，平均每个任务 6.3 次模型调用，工具操作映射到真实生产负载：政策约束下的客服对话、on-call 事故排查、跨消息/问题跟踪/邮件的多步工作流自动化；评估覆盖工具使用、多步检索、文件系统操作与长上下文总结。",
        "一个重要范围说明：这些评估在受控场景里跑，好处是能直接归因失败原因，坏处是套件已经饱和——三类工作量准确率都很高，30B 参数模型与前沿模型之间只有 8 个点差异，这给路由发挥价值的空间比更难的负载更小。把所有成本按缓存输入价计（发票显示的就是这个）。"
      ],
      "fig_after": {}
    },
    {
      "type": "h3",
      "title": "实测数据",
      "paras": [
        "三臂对比：纯 Opus 4.8 是 86.0% 准确率、每次运行 $11.45；路由后（Opus + NeMo 3.5 Lightning）80.0%、$3.00；纯 NeMo Lightlion 77.7%、$0.72。调用去向与钱流向的错位是核心发现：NeMo 3.5 Lightning 处理了 93% 的模型调用却只占 10.4% 的花费，Opus 处理 7% 的调用却花了 68.4%。前沿模型被用得远少于单模型假设，而最后 6 个点的准确率，每个完成任务要多花 3.5 倍。",
        "judge 还占了剩余 21.2% 的花费——它在任务升级前每个回合都跑，且不像前沿模型能从 prompt 缓存受益，所以在路由臂里成了仅次于 Opus 的第二大开销项。想砍路由成本，judge 模型比升级率更值得优化。"
      ],
      "fig_after": {
        "1": [
          {
            "src": "fig02.png",
            "caption": "图 2：路由臂中模型调用占比与花费占比的对比。调用数不含 judge（它只在每个弱层回合触发一次）。"
          }
        ]
      }
    },
    {
      "type": "h3",
      "title": "结论要点",
      "paras": [
        "准确率代价是真实的：路由便宜 74%、准确率低 6 点；单次运行波动约 2.7 点，所以 6 点差距明显在噪声之外。价值来自把流量从前沿模型移开——把简单活发给廉价模型才是省钱所在；而当一个任务因廉价模型吃力而升级到 Opus 时，只比纯跑廉价模型好 2.3 点，小于运行自身波动，所以不能说路由在这里胜过了廉价模型。成本跨运行稳定：Opus 三次运行只波动 1.5%，74% 这个数字立在稳定基线上。"
      ],
      "fig_after": {}
    },
    {
      "type": "h2",
      "title": "按范围预算，别按单个数字",
      "paras": [
        "五次运行里，前沿流量从 4.1% 到 9.1%，均值 6.9%，最重的一次升级次数是最轻的两倍多。一次 Opus 调用约 $0.0324，NeMo 3.5 Lightning 约 $0.00037（约 87 倍差价），于是这个波动把成本范围推到 $2.16 到 $3.61——五轮之间啥都没变，只是路由器决定升级哪些回合，账单就动了 67%。",
        "这就是你接手一个路由器时的取舍：它降低平均花费、同时拉宽围绕它的波动范围。要按这个范围的上限做预算。想收窄它，strike 次数是最有杠杆的设置（配置里的 confirmations，决定 judge 在移任务到前沿模型前要看到多少次坏回合）；降低它就会更频繁升级，而那些是贵调用。judge 本身是另一个杠杆——它占路由花费 21.2%，一个更便宜的 judge，或能跳过明显正常的回合，直接就是从账单里减钱。"
      ],
      "fig_after": {}
    },
    {
      "type": "h2",
      "title": "明显的异议：为什么还需要路由器？",
      "paras": [
        "NeMo 3.5 Lightning 单独 77.7%、$0.72 一次；纯 Opus 86.0%、$11.45；路由 80.0%、$3.00。为什么不用模型路由器？路由只比廉价模型高 2.3 点、贵 4.2 倍——这个差距小于运行自身 2.7 点的波动，所以不能说路由在此胜过了廉价模型。必须坦白：廉价模型表现很好，这套件上只有 8 点把 NeMo 3.5 Lightning 与前沿模型分开。",
        "但对比里带着事后视角：我们知道这 145 个任务的结果。生产里你不知道刚来的请求是易是难，而把廉价模型用在一切上，意味着在难任务上也只能接受它的答案。路由是『不必猜』的代价，也同时压低了你的成本上限——我们最差的一次路由运行 $3.61，约为纯 Opus 的三分之一。",
        "所以如果你的首要目标是成本最低、流量又像这个负载，NeMo 3.5 Lightning 单独是更好的选择。路由是给那些需要在难请求上具备前沿能力、又无法预先判断哪些请求难的团队的。"
      ],
      "fig_after": {}
    },
    {
      "type": "h2",
      "title": "这个权衡在你的负载上是否划得来",
      "paras": [
        "相对于纯前沿模型，路由器只在『你发给更小模型的回合占比超过这个门槛』时降低总成本：最小卸载比例 = judge 成本 / (贵模型成本 − 廉价模型成本)。judge 是每次运行的固定税——它在任务升级前每个回合都跑，无论最后有没有升级。所以问题从来不是『我的廉价模型够不够好』，而是『我的两个模型间的价格差有没有宽到能付得起 judge』。",
        "以这组配对为例：judge 每次运行 $0.64，价格差 $10.73，所以只需卸载 5.9% 的回合，而实际卸载了 93%，比门槛高 16 倍。差距一边倒时公式有点像走形式；它在价差较窄、答案不那么明显时更有用。",
        "公式也能把路由排除在外：如果两个模型价格接近，每次卸载省的钱很少，公式要求你卸载超过 100% 的回合——这是不可能的，任何 judge 配置都救不了。例外是本地托管的廉价模型：在 DGX Spark 之类的设备上自跑，推理成本近乎为零，价差被重新拉宽到足以让路由划算。公式不能告诉你这个路由器在你的流量上会不会做对选择，只能告诉你『对的选择』值不值得付费——用它来从成本上排除路由，用自己的负载来把它确认回来。"
      ],
      "fig_after": {}
    },
    {
      "type": "h2",
      "title": "何时不该用路由",
      "paras": [
        "两种情形别用：对延迟敏感——judge 是每回合第二个模型调用，约 700ms，而 stage router 几乎为零；工作负载太短——escalation 需要多回合轨迹才有东西可读。",
        "LangChain 还测了 middleware 版本：路由在进程内进行，无需单独服务，任何 LangChain 聊天模型都可作为路由目标，工具绑定、回调、追踪与结构化输出都继续工作，每个响应在 response_metadata[\"switchyard\"] 里携带路由轨迹。数字来自服务器端 escalation 模式；middleware 是实验性的、尚未发布为包。他们下一步想测：更便宜或本地托管的 judge（占 21.2%）、更难的工作负载（路由在模型差距更大处更值得证明）、以及 confirmations=1。"
      ],
      "fig_after": {}
    }
  ],
  "conclusion": [
    "这篇对『agent 该不该全用最贵模型』给了一个可量化的答案：多数调用根本不需要前沿模型。在 LangChain 的压力测试里，仅 7% 的调用真正需要最强模型，而它们扛了 68% 的账单；用一个 30B 模型处理其余 93%，成本降 74%、准确率只掉 6 个点。",
    "但它真正的价值是把『值不值』变成一道算术：最小卸载比例 = judge 成本 ÷（贵−便宜）。两个模型价格差越大越划算，价差接近则不如本地托管廉价模型。路由不是免费的——judge 每次要多一次调用、还拉宽了账单的波动范围。最终决策本质上是在『为确定性付钱』和『赌请求的难度』之间选择：如果你不能提前分辨难易，路由器就是那个让你不必猜、还把成本天花板压低三分之二的选择。"
  ],
  "reference_url": "https://www.langchain.com/blog/switchyard-agent-routing-benchmark"
}

with open("article_data.json", "w", encoding="utf-8") as f:
    json.dump(DATA, f, ensure_ascii=False, indent=2)
print(f"✅ 写入 article_data.json")