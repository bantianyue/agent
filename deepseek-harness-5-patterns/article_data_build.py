# -*- coding: utf-8 -*-
"""DeepSeek Harness 5 Patterns 编译 build"""
import json, os, sys

DATA = {
 "title": "DeepSeek 开源 Agent Harness 的五个设计模式：为什么它 48 小时破 10 万星",
 "lead": [
  "DeepSeek 几天前开源了它的 agent harness，成为 GitHub 史上最快破 10 万星的仓库——只用了 48 小时。这让作者很好奇里面到底有什么，于是在 Mac 上装下来跑了一整个周末、通读了源码。",
  "结论是：你不需要迁移到它也能获得价值。代码库里有五个设计决策让他印象深刻——都是他以前遇到过、但从没见过处理得这么好的问题。本文逐一展开，细节足够你在任何技术栈上复用。"
 ],
 "summary": [
  {
   "key": "模式1",
   "body": "从日志派生模型上下文而非维护消息数组——append-only 事件日志是唯一真相源，每次请求从日志重建，附 log-reconstruction desync 硬校验。"
  },
  {
   "key": "模式2",
   "body": "用提醒打断循环而非硬阻断——计数连续相同工具调用，3/5/8 次注入越来越强硬的提醒，永不硬拦，模型保留最终决定权。"
  },
  {
   "key": "模式3+4+5",
   "body": "告诉模型它没看到什么（截断+均匀采样+可访问）；代码执行无法绕过权限层；杀掉上下文保留工作区（多轮重启+五字段 handoff）。"
  }
 ],
 "sections": [
  {
   "type": "h2",
   "title": "背景：为什么值得深挖这个 harness",
   "paras": [
    "作者说：DeepSeek 开源了它的 agent harness，几天内就成了 GitHub 史上最快破 10 万星的仓库，48 小时达成。这让他好奇里面到底有什么，于是装上、跑起来、读源码，花了一整个周末。",
    "你不必切换到它也能获得价值。代码库里有五个设计决策让他特别在意——都是他以前遇到过、但从没见过处理得好的问题。他会逐一展开，细节足够你在自己的技术栈上直接使用。"
   ],
   "fig_after": {
    "0": [
     {
      "src": "fig01.jpg",
      "caption": "图：DeepSeek agent harness 概览——快照与界面。"
     }
    ]
   }
  },
  {
   "type": "h2",
   "title": "模式 1：从日志派生上下文，而不是维护它",
   "paras": [
    "如果你构建过 agent，大概撞过这个：发给模型的上下文和你日志里记录的上下文对不上，于是你在用一份从未发生过的事件的记录调试一次失败运行。根因是同一信息存在两个地方——发给模型的消息数组、以及关于它的遥测，两者没有约束必须一致，时间一长就分叉。",
    "DeepSeek Harness 只有一个地方：session 是 append-only 事件日志，每次模型请求的上下文都从日志重新计算。架构文档里有条硬规则：任何进入模型请求的东西必须能从日志重建，想把新东西放到模型面前，必须先定义新事件类型。",
    "所以没有任何办法在不被记录的情况下往模型视野里塞东西——因为记录就是视野的来源。代码里有个检查拦截每个出去的 LLM 请求，逐字段与从日志的重新派生对比：消息、系统提示、工具 schema、temperature，全部。任何不匹配就抛一个叫 log-reconstruction desync 的错误。",
    "理解了这点，它们的 trace UI 也就说得通了：轨迹面板展示每次调用、每个 token 数、每个工具结果，且不需要任何额外插桩——它读的是系统要运转就必然存在的数据。要在自己栈里落地：反转写入顺序，先追加事件、再从追加构建请求，并在 dev 加那个相等性检查。大约二十行代码，把静默漂移变成看得见的测试失败。"
   ],
   "fig_after": {
    "0": [
     {
      "src": "fig02.jpg",
      "caption": "图：模式 1——append-only 事件日志派生模型上下文。"
     }
    ]
   }
  },
  {
   "type": "h2",
   "title": "模式 2：用提醒打断循环，而不是硬阻断",
   "paras": [
    "每个跑过 agent 的人都见过它卡在反复调用同一个工具、同一组参数，一路烧 token。两种常见处理方式都不好：硬阻断重复调用，会误伤那些本应重试的合法场景；什么都不做，循环会一直跑到上下文塞满。",
    "它们的解法是一个小插件：计数对同一工具、相同参数的连续调用，计数到 3、然后 5、然后 8 时，往上下文里注入越来越强硬的提醒——别再重复了，重读上次结果，换方法或收尾。它从不阻断任何东西，模型始终保留最终决定权。",
    "细节很讲究：同一参数换顺序仍算重复；两次相同 grep 之间的一次 todo 更新不会重置计数（否则任何小调用都会掩盖循环）；被拒绝的调用也算——agent 反复重试一个一直被拒的调用，是最常见的循环。每个 agent 有独立计数器，子 agent 卡循环不会触发主 agent 的告警。",
    "这个在任何一个能观察工具调用的 harness 里一个下午就能搭出来。在 Claude Code 上，光 hooks 就够了。"
   ],
   "fig_after": {
    "0": [
     {
      "src": "fig03.jpg",
      "caption": "图：模式 2——连续调用计数 + 递增提醒注入。"
     }
    ]
   }
  },
  {
   "type": "h2",
   "title": "模式 3：告诉模型它没看到什么",
   "paras": [
    "当你的搜索工具从 4000 个结果里返回前 100 个、却不说明截断时，模型就以为只有 100 个文件，之后的每个决策都建立在一个错误数字上。它们的文件搜索同样限结果数，但方式不同：超限时，从整个项目里均匀挑选 100 个而非前 100 个，模型得到公平样本而非按字母序排到最前的；结果明确声明被截断了；完整列表保存到文件并给出路径，模型需要时可以去读其余部分。",
    "当沙箱拦截一个命令时，它们的 bash 工具会告诉模型：这是策略拒绝，不是命令的 bug，不要再换方式重试。没有这句话，模型会花六轮试图绕过一条你故意写的规则。",
    "给自己一小时审计工具：每个工具是否在截断时披露了？模型能否触达被截掉的部分？错误信息是否讲清楚了重试有没有用？"
   ],
   "fig_after": {
    "0": [
     {
      "src": "fig04.jpg",
      "caption": "图：模式 3——截断时均匀采样 + 明确披露 + 完整列表可访问。"
     }
    ]
   }
  },
  {
   "type": "h2",
   "title": "模式 4：无法绕过权限层的代码执行",
   "paras": [
    "现在大家都在收敛到同一个模式：与其每轮一次工具调用，不如让模型写一个循环调用你工具的程序。五十次文件读取变成一次往返，只有程序返回的摘要进上下文。",
    "Code mode 就是它们对这个模式的实现：模型拿到唯一的工具 run_code，其它所有工具都变成程序内部可调用的函数。程序里每个调用仍走完整权限管线，没有任何捷径；每个调用单独记录，所以你能看到程序做了什么，即使模型只看到一个结果；被拒绝的调用在程序内表现为可捕获的错误，而不是挂起或静默 null。",
    "如果你自己构建这类系统、或评估声称支持它的框架：先检查权限部分。跳过它，模型就有一条绕过你自己规则的路。"
   ],
   "fig_after": {
    "0": [
     {
      "src": "fig05.jpg",
      "caption": "图：模式 4——run_code 单工具入口，内部调用仍走权限管线。"
     }
    ]
   }
  },
  {
   "type": "h2",
   "title": "模式 5：杀掉上下文，保留工作区",
   "paras": [
    "长跑 agent 会把上下文堆到上下文本身成为问题。某个点你必须重新开始，并指望笔记够用。DeepSeek Harness 把重启做进了系统：重启循环围绕固定目标按轮次工作，每轮从零对话历史的全新 agent 开始。",
    "轮次之间，新 agent 拿到共享工作区的文件加一个五字段交接：status、summary、evidence、next steps、blocker。transcript 不跨轮次，所以每轮都从满推理能力开始，而不是跋涉过上一轮的残余。",
    "退出规则也很讲究：agent 至少要试三轮才能声明自己被卡住；只有人才能创建或修改目标，所以子 agent 不能发明自己的目标。如果你跑隔夜 agent：先写好交接 schema，设好大小上限，决定哪些状态活在文件里、哪些随上下文死掉。然后重启就只是系统运行方式的一部分。"
   ],
   "fig_after": {
    "0": [
     {
      "src": "fig06.jpg",
      "caption": "图：模式 5——多轮重启，每轮全新 agent + 五字段 handoff。"
     }
    ]
   }
  },
  {
   "type": "h2",
   "title": "自己去验证：15 分钟可查",
   "paras": [
    "作者说，他描述的每一件事大约十五分钟都能验证：给它一个你熟悉仓库里的任务，打开 Trajectory 标签读一遍运行——你会看到每个请求、每个工具调用、token 数、以及从解码时间分离出来的首 token 时间。也可以把任何 session 导出成 zip，自己读原始事件日志。",
    "想直接测插件部分：打开 ~/.dsh/profiles/web/cordis.patch.yml，加三行禁用 id 为 ui-sidebar 的那行，刷新，侧边栏消失；删掉那三行，它又回来了——连 UI 都是配置文件里的行。想更深入，看 GitHub 源码，从解决你手头问题的那个开始。"
   ],
   "fig_after": {
    "0": [
     {
      "src": "fig07.jpg",
      "caption": "图：验证——轨迹面板展示每个请求/工具调用/token 数。"
     }
    ]
   }
  }
 ],
 "conclusion": [
  "这五个模式共享一条暗线：把 agent 的状态管理从「隐式内存」变成「显式可重建」。上下文从日志派生、循环用提醒而非阻断、截断明确披露、代码执行不绕过权限、重启靠工作区而非对话历史——每一条都是把脆弱性换成可验证性。",
  "对想造更可靠 agent 的人，最有迁移价值的是模式 1 的日志派生上下文与模式 5 的五字段交接：前者根治「记录与事实分叉」，后者让长跑任务能干净重启。两者都不需要换框架，直接重构你自己的状态流就能落地。"
 ],
 "reference_url": "https://x.com/Saboo_Shubham_/status/2089177978265870817"
}

with open("article_data.json", "w", encoding="utf-8") as f:
    json.dump(DATA, f, ensure_ascii=False, indent=1)
print(f"✅ 写入 article_data.json")