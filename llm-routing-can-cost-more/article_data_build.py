#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""article_data_build.py — LLM Routing 100%保留重建 (Akshay X Article)"""
import json, os, sys
_article_dir = sys.argv[1] if len(sys.argv) > 1 else os.getcwd()

try:
    _cur = os.getcwd()
except Exception:
    _cur = _article_dir

DATA = {
    "summary": [
        {"key": "核心现象", "body": "一次编码 agent 会话里有难易悬殊的任务，写死单一模型让简单任务也按贵模型计费"},
        {"key": "真实代价", "body": "Uber 约 5000 工程师自 2025-12 起用 Claude Code，到 2026-04 已花光全年 AI 预算"},
        {"key": "关键结论", "body": "路由能省约 85% 成本，但 agent 每轮切模型会让前缀缓存作废：必须按会话钉住同一模型"},
    ],

    "lead": [
        "很多人认识的路由长这个样子：分类器先读请求，容易的活交给便宜模型，剩下的交给前沿模型。这个版本人人都听过，但它恰好在生产中会失效。",
        "一次编码 agent 会话往往会在同一场里做五类性质不同的任务：分析代码库、写新函数、根据测试输出修 bug、解释方法、检索文档。难度天差地别，可系统一旦写死单一模型，它们便按同一费率计费。",
        "本文由此展开：先透过 Uber 的真实账本看固定单一模型造成的浪费，再拆解朴素路由埋进 agent 循环后反而更烧钱的四种原因，最后看一种把路由收进基础设施、并借会话固定保住前缀缓存红利的做法。文中的图、代码块与关键算式均按原文完整保留。",
    ],

    "sections": [
        {
            "type": "h2",
            "title": "开头：最了解的路由，恰是会在生产里碎掉的那个",
            "paras": [
                "一个人人都知道的路由版本是这样：一个分类器先读请求，把容易的交给便宜模型，剩下的难活才轮到前沿模型。这个方案听着顺理成章，可在真实 agent 生产里，它往往就是碎了的那一版。",
                "一次编码 agent 会话会同时做五类难度悬殊的事，但写死一个模型后它们被按同一个价格计费——成本就是这样在不知不觉中被抬高。",
            ],
            "fig_after": {
                "1": [
                    {"src": "fig01.png", "caption": "原文开篇配图"}
                ]
            },
        },
        {
            "type": "h2",
            "title": "Uber 的账本：5 千工程师烧光年度 AI 预算",
            "paras": [
                "Uber 在 2025 年 12 月把 Claude Code 推广给约 5000 名工程师，到 2026 年 4 月全年 AI 预算就花光了。重度用户一个月跑 500 到 2000 美元，有高管在单次两小时会话里花掉 1200 美元。没有哪里坏了——工具被用在了它本来的地方。",
                "Uber 的做法是给每个工具设 1500 美元/月封顶：这靠限产能控成本，只治标。治本还得靠计价对齐——也就是让每个请求用得上、也真正配得上的模型。",
                "真正关键的一点：朴素地把路由搭好并埋进 agent 循环，也可能比不路由花得更贵。全文花了很长篇幅讲的就是它为什么发生、以及正确的修法是什么样。",
            ],
            "fig_after": {
                "2": [
                    {"src": "fig02.png", "caption": "原文配图"}
                ]
            },
        },
        {
            "type": "h2",
            "title": "路由的价值：用研究数据说话",
            "paras": [
                "把请求发给最能办好事、又最便宜的模型，听起来正确，它也确实经得起验证：分类、抽取、格式化、短摘要这类任务，用便宜约一个量级的模型（约 1/10 价）即可产出几乎同样结果，前沿模型被解放出来留给真正要它出力的活。",
                "学术层面这条早被证明。RouteLLM 发表于 ICLR 2025，基于 Chatbot Arena 的人类偏好训练，在标准 benchmark 评测：其矩阵分解路由器在 MT Bench 达到 GPT-4 Turbo 约 95% 质量，而只有 14% 的查询被发给强模型——换算约省 85% 成本。",
                "路由器还能泛化到它没训练过的模型配对，说明这套方法并不锚定某一台配置。账按你自己的流量算也成立：70% 的请求走 0.10 美元/百万 token，30% 走 3 美元/百万 token，混合单价约 0.97 美元/百万，低于统一走 3 美元。",
                "结论上，2026 年一篇 arXiv 动态路由综述指出：设计良好的路由能借各模型的专长超过单一最强模型。技术没得疑问，问题在于路由层本身就不易落地，这正是多数团队宁可写死单模型的原因。",
            ],
        },
        {
            "type": "h2",
            "title": "自建路由失败的四种姿势",
            "paras": [
                "路由概念虽直白（识别意图、转发合适模型），实现却有一叠在生产中互相放大的失败模式。这里把四种常见情景拆细：",
            ],
        },
        {
            "type": "olist-b",
            "title": "你会为一次意图付两次推理的钱",
            "paras": [
                "最直觉的 DIY 是在前面放一个小 LLM 当分类器——这样每个请求都要先花一次分类调用、再花一次真实推理。即便分类器便宜，也在每个请求头上压了一笔固定税：只有分类器显著便宜于模型档位价差、且准到不拖累路由判断时，才能谈省字。",
            ],
        },
        {
            "type": "olist-b",
            "title": "通用大模型并不擅长当路由器",
            "paras": [
                "指令本身信号弱（\u201cfix this\u201d\u201cmake it faster\u201d），意图只埋在完整对话历史里。通用模型从没被训练成\u201c对照路由定义归因\u201d的判别器，它们是在做一件没练过的活；编码类负载最容易在这种地方翻车。",
            ],
        },
        {
            "type": "olist-b",
            "title": "路由逻辑会腐烂",
            "paras": [
                "加模型、改任务名、调价……每一次都意味着在毫无评估护栏的前提下手改路由代码。没有任何机制提醒你某次路由改动悄悄劣化了产出；往往要等线上用户报 bug，你才后知后觉。",
            ],
        },
        {
            "type": "olist-b",
            "title": "切换模型会丢缓存",
            "paras": [
                "这对 agent 格外致命。提供商对重复 prompt 前缀缓存注意力：同 token 序列命中同一模型即复用缓存、按折扣价计费；一旦中途切到另一个模型，对方对这段对话毫无缓存，每条 token 都要按全价重算。这是 DIY 路由最容易漏掉的账。",
                "由此可见，四者的病灶同根——路由逻辑长在应用这一层，就得自担分类器、逻辑、维护与缓存行为；把路由收进基础设施，正是针对此症的手术。",
            ],
            "fig_after": {
                "1": [
                    {"src": "fig03.png", "caption": "原文配图：路由长在自己的应用里时的四类失效"}
                ]
            },
        },
        {
            "type": "h2",
            "title": "把路由收进基础设施层",
            "paras": [
                "DigitalOcean Inference Router 的思路是把上述\u201c自己造\u201d的成本整体吸收进基础设施：你只要用自然语言描述要做哪些任务、允许每个任务由哪些模型服务，余下每次请求该落到哪个模型，由平台在请求时替你决定。",
                "底层引擎是开源 AI 原生代理 Plano，决策分两阶段进行：先解析意图，再在候选池中按策略排序。",
            ],
            "fig_after": {
                "1": [
                    {"src": "fig04.png", "caption": "原文配图：路由下沉基础设施后的整体结构"}
                ]
            },
        },
        {
            "type": "h2",
            "title": "Phase 1：意图解析",
            "paras": [
                "小模型读整段对话、匹配到你写的任务描述、返回路由决策。看似\u201c双重推理\u201d，但差异在谁在做分类——它不是通用模型，而是专门为了这一个动作训练的小模型：Katanemo 的 Arch-Router，只有 1.5B。",
                "小专用模型在准确率上胜过 Claude 3.7 Sonnet，运行还快 28 倍。路由不需要写散文、无需工具调用、也不用复杂推理，能力面足够小，以致小模型能完整覆盖；它跑在代理内部而非一次单独付费调用，每请求只多约 200ms 延迟，而不是账单上多一行。",
            ],
            "fig_after": {
                "1": [
                    {"src": "fig05.png", "caption": "原文配图：Arch-Router 意图识别能力的说明"}
                ]
            },
        },
        {
            "type": "h3",
            "title": "现在的生产模型：Plano-Orchestrator",
            "paras": [
                "今天跑在生产的是 Plano-Orchestrator，专门为\u201c更难\u201d的会话模式调过：含糊的追问、中途跑题、以及根本不该路由的消息。它在整套准确率上略胜 GPT-5.1 与 Claude Sonnet 4.5，尤其在最含糊的编码类意图上拉开最大。",
                "代码是意图最含糊的场域：\u201cfix this\u201d/\u201ctry again\u201d 要回到前文才有意义，一个在同类模式下受过训的模型，读起来强过通用模型被临时叫来当分类器。",
            ],
        },
        {
            "type": "h2",
            "title": "Phase 2：给候选池排序",
            "paras": [
                "任务确认后候选池收窄到至多 3 个模型，第二名要在这几个候选里定谁上场。你也可以把池子排序、永远取队首——但这在长期会失灵：提供商延迟在一日内随负载波动 2 到 3 倍（凌晨最快的模型到下午多半变最慢），价格、延迟、配额都在漂移，上个月写的配置文件描述的已是另一番景象。",
                "因此排序引擎实时拉取 DigitalOcean 定价 API 的成本、以及 Prometheus 的延迟，再按任务策略排序：Cost Efficiency 看 token 成本，Speed Optimization 看首个 token 时间，Manual Ranking 只按你给定的顺序不重排，Optimal 用 DO 自带基准排序。后台循环不断刷新并写入内存缓存，使请求时的读取几乎零成本。",
                "每个路由器也带一份 fallback 清单：一旦选中的模型掉线/被限流/不可用，就按策略顺延到下一个候选；都不行时再落入你自己配置的兜底模型。",
                "下图为路由配置界面相关截图，供对照原文操作。",
            ],
            "fig_after": {
                "1": [
                    {"src": "fig06.png", "caption": "原文配图：路由设置界面示意（配置阶段）"}
                ]
            },
        },
        {
            "type": "h2",
            "title": "模型亲和：agent 需要另一套路由逻辑",
            "paras": [
                "前面两步都把请求当作独立单元，但 agent 不这么运作——如今的严肃工作几乎都是多轮的。一个编码 agent 读文件、调工具、对结果推理、写码、自查：每步都轴在前面的所有步骤上，这正是循环能跑起来的机制。",
                "这套机制会改变路由的经济核算：下面是在讲前缀缓存与逐轮换路由为什么互相打架时用的配图。",
            ],
            "fig_after": {
                "1": [
                    {"src": "fig07.png", "caption": "原文配图：前缀缓存与会话内多次调用示意"}
                ]
            },
        },
        {
            "type": "h3",
            "title": "前缀缓存的红利与它怎么被切碎",
            "paras": [
                "LLM API 是无状态的：提供商不记得上一轮，agent 每轮要把整段历史重发一遍。靠\u201c基于前缀的 KV 缓存\u201d兜底：请求以模型处理过的一段 token 开头，则复用其缓存注意力、不用重算；命中部分只按约 10% 的正常输入价计费。",
                "agent 循环本质是\u201c重复前缀\u201d：15 轮编码会话到后期约 90% 发送内容是早已处理过的文本——这本是最适合命中缓存的场景。可若每轮都按意图重路由，第 3 轮代码生成到了模型 A、第 4 轮修 bug 又切到模型 B，B 那段对话毫无缓存，于是 5 万 token 全部按全价输入价重算；轮次越往后，代价越大。",
            ],
        },
        {
            "type": "h3",
            "title": "三处同时崩坏 + 会话固定的修法",
            "paras": [
                "一句话总结逐轮切模型的三重代价：**成本**——维持模型亲和能在 90% 缓存命中场景上省下 45% 到 80% 输入 token，切一次模型归零；**行为一致性**——各模型的输出风格和工具调用格式差异会搅乱 agent 的解析；**连贯性**——一段推理脉络被一个连总结格式都不同的模型接走。",
                "正确的修法是会话固定：对某次会话的第一个请求做一次全新路由决策，随后把这一场会话的所有请求都钉在同一个模型上。DO 以 X-Model-Affinity + 会话 ID 做到：带同一 affinity ID 的第二个请求会跳过路由、直接返回 pinned:true 并套用同一模型——这样只付一次路由开销，之后每一轮继续吃满前缀缓存。",
            ],
        },
    ],

    "conclusion": [
        "固定单一模型最“省心”却在偷偷烧钱；而只把路由做对一半、没接好 agent 的会话上下文，又会比不路由更贵。真正的分水岭藏在缓存里——只要路由能在一通会话的第一轮做出决定、并把整场钉死在一个模型上，前缀缓存就从“白白浪费的大头”变成“每一轮都在帮你打折的资产”。",
        "给做 agent 基建和成本治理的团队一个判断：省 token 的关键，不在于模型选得多么“恰到好处”，而在你干预得有多克制——别打断 provider 已经为这场对话攒下的前缀缓存。再叠加 62 倍（DeepSeek V4 Flash 与 Claude Opus 5 之间）的输入价差、一天内 2 到 3 倍的延迟波动这些现实，路由排序必须读实时指标而非压着上个月的配置表硬推理，才谈得上持续成立。",
    ],

    "reference_url": "https://x.com/akshay_pachaar/status/2096601734072402054",
    "title": "LLM 路由做不好，可能比不做更烧钱",
}

# ---- embed the single original bash code block as a __CODE__ para ----
code = '''curl --location 'https://inference.do-ai.run/v1/chat/completions' \\
  -H \"Authorization: Bearer $MODEL_ACCESS_KEY\" \\
  -H \"X-Model-Affinity: session-001\" \\
  -d '{
    "model": "router:test-router",
    "messages": [
      {"role": "user", "content": "Write a Python function for binary search"}
    ]
  }'
'''

# Insert the code right after the "会话固定" explanatory section, before building steps:
code_sec = {
    "type": "h2",
    "title": "贴一段原文的会话固定调用代码",
    "paras": [
        "下面这段代码来自原文，请求一个配置好的路由模型 router:test-router，并带上 X-Model-Affinity: session-001，让这一场会话始终沿用同一个下游模型。",
        "__CODE__bash::" + code,
    ],
}

# find insertion index: after the section titled "三处同时崩坏 + 会话固定的修法"
idxs = [i for i, s in enumerate(DATA["sections"]) if "会话固定的修法" in s["title"]]
DATA["sections"] = DATA["sections"][: idxs[0] + 1] + [code_sec] + DATA["sections"][idxos := idxs[0] + 1:]

out_path = os.path.join(_article_dir, "article_data.json")
with open(out_path, "w", encoding="utf-8") as f:
    json.dump(DATA, f, ensure_ascii=False, indent=2)
print(f"OK wrote {out_path} ({len(json.dumps(DATA, ensure_ascii=False))} chars, {len(DATA['sections'])} sections)")
