#!/usr/bin/env python3
# -*- coding: utf-8 -*-
import json, os, sys

_article_dir = sys.argv[1] if len(sys.argv) > 1 else os.getcwd()

DATA = {
    "summary": [
        {"key": "定位", "body": "552B 主干参数的多模态 MoE，支持 1M 上下文；prefill 每 token 激活 8B、decode 激活 16B"},
        {"key": "压缩结果", "body": "全局 KV cache 890 字节/token（约 V4-Flash 的 1/4、V1 的 1/437），持久 KV cache 约 1/8"},
        {"key": "怎么做到", "body": "CED 让解码器 KV 从编码器投影、prefill 只算一半层；CSA2 跨层复用 main KV 与 indexer K；主 KV 用 FP4"},
    ],

    "lead": [
        "长程 agent 把模型负载推向输入密集型：prefill 依旧昂贵，庞大的 KV cache 持续压迫 HBM、SSD 容量与数据传输带宽。这篇只覆盖 DeepSeek-V4.1-Flash 技术报告的前两章，引言与架构。",
        "结论先放这里：它是一个多模态 MoE 模型，552B 主干参数、原生 100 万 token 上下文；CED 架构让 prefill 每 token 只激活 8B 参数、decode 激活 16B；全局 KV cache 压到 890 字节/token，约为 DeepSeek-V4-Flash 的 1/4。",
    ],

    "sections": [
        {
            "type": "h2",
            "title": "1. Introduction：为什么盯着 KV cache 打",
            "paras": [
                "DeepSeek-V4 把全局注意力分支与局部滑动窗口注意力（SWA）结合：全局分支维护 main KV 与 indexer K，SWA 维护局部 KV 状态。固定窗口下 SWA 的存储与序列长度无关，于是超长序列下全局 KV 成为运行时 KV 占用的主体、受限于 HBM 容量；另一部分为前缀复用而持久化的 KV 受 SSD 与主机内存约束；I/O 与互连带宽还会限制缓存的迁移和加载。这些约束共同压低服务吞吐、抬高部署成本，最终妨碍 agent 走向更长的任务与更广的场景。",
                "V4.1-Flash 在三个层面同时下手：架构上设计 CSA2 做跨层复用，精度上把全局 KV 换成 FP4，部署上引入 SWA Bounded Replay。论文把 DeepSeek-V4 视作一个以 SWA 为局部处理主干、外挂压缩全局上下文的模型，因此这一代的思路是简化全局分支、基本保留局部注意力设计。",
                "效果可以量化为两个数字：全局 KV cache 约 1/4（890 字节/token，相比最早的 DeepSeek-V1 减少约 437 倍），持久 KV cache 约 1/8。同时模型性能不降反升：在同等序列长度下，它只需要约 1/4 的运行时 KV 存储和 1/8 的持久 KV 存储。",
                "预训练方面，模型在包含 45T token 的多模态语料上训练，稀疏注意力从 64K 序列长度直接从头训练、没有任何稠密注意力预热阶段，训练完成后原生具备多模态能力与 1M 上下文。评测中，DeepSeek-V4.1-Flash-Base 在世界知识、推理与编码能力上和 DeepSeek-V4-Pro-Base 相当，并在留出评测上带来 5% 到 10% 的提升，而只用了约 1/3 的总参数和 1/4 的激活参数。",
                "能力画像有三个看点。推理上，它在数学、竞赛编程等推理密集基准上保持高准确率，与 Kimi-K3、DeepSeek-V4-Pro 等顶级开源模型相当。Agent 上，它在 Terminal-Bench 2.1、DeepSWE v1.1、AutomationBench 等标准 agent 基准上达到闭源前沿模型的水平，足以承担日常编码与白领工作流，但在需要专家级领域知识的科学型 agent 任务（如 Terminal-Bench 4.0）上仍与巨型模型有差距。多模态上，它在视觉推理与专业图表解读上超过 Kimi-K3 这类开源对手，并能在前端开发、办公自动化等真实视觉 agent 工作流里利用渲染截图做视觉检查与自我纠错，不过相比巨型闭源系统仍有整体差距。",
                "后训练没有任何算法创新：配方就是标准的监督微调（SFT）、强化学习（RL）再加 on-policy 蒸馏（OPD），与 DeepSeek-V4 一致，所有实质变化都在数据管线里——大规模自动化的数据合成与环境构建，并逐步放大 RL 阶段的数据、任务与 rollout。论文的结论是，V4.1-Flash 已能在绝大多数基准上对标闭源前沿模型，可完成 95% 以上的真实任务，同时小激活量带来低延迟与低服务成本，是一个能力与效率之间相当均衡的选择。",
            ],
            "fig_after": {
                "2": [{"src": "fig01.png", "caption": "图 1：(a) DeepSeek-V4.1-Flash 与同代模型在 agent 基准上的表现；(b) 历代 DeepSeek 模型每 token 的全局 KV cache 大小（字节），V4.1-Flash 约为 V4-Flash 的 1/4、V1 的 1/437"}],
                "5": [{"src": "fig02.png", "caption": "图 2：历代 DeepSeek 模型单 token 解码 FLOPs 随上下文长度的变化。按 BF16、FP8、FP4 分别以 1、0.5、0.25 加权计算量；V4.1-Flash 的解码 FLOPs 几乎不随上下文增长，上下文从 4K 扩到 1M（256 倍）只增加约 1/4 计算量"}],
            },
        },
        {
            "type": "h2",
            "title": "2.1 架构总览",
            "paras": [
                "模型是一个多模态 MoE Transformer，输入图像与文本、自回归生成文本。语言主干由 40 个因果 Transformer 层组成，组织为 20 层因果编码器加 20 层解码器；除最前两层只用滑动窗口注意力（SWA），其余每层都同时包含全局注意力与 SWA。视觉编码器与一个 MLP 投影器把图像转成视觉嵌入，与文本嵌入一起处理，多模态数据从语言模型预训练一开始就进入。整体上，主干 552B 参数、Engram 196B 参数，每 token 激活参数在 prefill 阶段为 8B、decode 阶段为 16B。",
                "CED 与 CSA2 解决的是长上下文推理里互补的两类开销：CED 用编码器输出构造解码器的全局 KV cache，让大部分提示 token 跳过完整的解码器计算，同时保留逐层的滑动窗口注意力，prefill 计算量近乎减半；CSA2 则跨层共享全局 KV 以减少缓存存储、并复用稀疏选择以减少索引工作量。解码器里还有分层稀疏索引器，把后续索引器限制在前一层索引器选出的候选池内，进一步减少每个 query 需要打分的条目数。",
                "细节上，模型保留 DeepSeekMoE 的共享专家与细粒度路由专家，并为图像与文本 token 引入模态特定的负载均衡；Single-Pass mHC 改写残差流混合以便更高效地做 kernel 融合，Engram 增加稀疏访问的条件记忆；主干预训练阶段不用 MTP 模块，投机解码改用 DSpark（在主干预训练之后单独训练）；主 KV cache 压缩到 FP4 以进一步降低存储。",
            ],
            "fig_after": {
                "1": [{"src": "fig03.png", "caption": "图 3：DeepSeek-V4.1-Flash 整体架构。40 层网络分为各 20 层的因果编码器与解码器，前馈层全部使用 DeepSeekMoE；编码器前两层用滑动窗口注意力，其余用 CSA2（括号内标注压缩比与模式）"}],
            },
        },
        {
            "type": "h3",
            "title": "2.1.1 多模态架构",
            "paras": [
                "多模态输入通路由视觉编码器与 MLP 投影器组成：视觉编码器对每张图产出空间网格特征，先做 3×3 像素反混洗（沿通道维重排局部邻域以降低空间分辨率），再由 MLP 投影到语言主干的隐藏维度，最后把视觉嵌入插入输入序列对应的图像 token 位置，与文本嵌入一起由语言主干处理。",
                "团队从头训练了视觉编码器 DeepSeek-ViT：以 ViT 为基础，为支持任意分辨率把绝对位置嵌入换成 2D-RoPE；为与 LLM 设计对齐、并兼容 Muon 优化器，把 patch embedding 的卷积换成线性投影；归一化用 RMSNorm、激活用 SwiGLU；送入 LLM 前做 3×3 下采样的像素反混洗，把视觉 token 数降到 1/9，可支持到约 1344×1344 像素的输入。",
                "图像与文本的表示分布不同，在 MoE 里可能引发不同的专家路由偏好，若只看总体负载就会掩盖模态内的不均衡。为此团队把无辅助损失的负载均衡扩展到为文本与图像 token 分别维护专家级修正偏置：路由时每个 token 用自己模态的偏置做专家选择，权重仍按原始路由分数计算；每步训练后两组偏置各自按本模态的专家负载独立更新。",
            ],
        },
        {
            "type": "h3",
            "title": "2.2 因果编码器-解码器（CED）",
            "paras": [
                "Agent 工作流里频繁的工具调用会制造大量 prefill 请求，一旦 KV cache 未命中，计算开销就非常可观。受 YoCo 启发（让上半部分层直接共享下半部分层生成的 KV cache），CED 把 Transformer 底部一半层当作因果编码器：上半部分层（解码器）的 KV 项不再来自各自隐状态，而是用逐层投影权重直接从第 L/2 层的隐状态投影得到。",
                "这样一来，prefill 阶段只需计算前一半层，就能以极低的计算量得到上层所需的全局 KV cache，整体 prefill 计算量几乎减半：序列长度远大于窗口时，复杂度从 O(N·L) 降到约 O(N·L/2)。",
                "滑动窗口注意力则保留逐层计算：每一层的局部键值都直接来自当层隐状态，这实际上加深了局部 KV 生成的计算深度。代价是解码器需要一个 SWA 重放过程，prefill 时要多处理约「窗口大小 × L/2」个 token，这在每轮提示都很短的多轮交互中不可忽视。已有工作表明 SWA 的实际有效感受野远小于理论值，于是论文引入 Decoder SWA Bounded Replay：只对提示最后一段（一个窗口长度）的 token 做 prefill 来计算 SWA，把这份额外开销显著压下去。",
            ],
        },
        {
            "type": "h3",
            "title": "2.3 压缩稀疏注意力 2（CSA2）",
            "paras": [
                "服务长上下文要同时控制 KV 存储与注意力计算，而这两项成本可以沿三个乘性维度压缩：条目大小（GQA 降低 KV 头数、MLA 让各头共享小型潜向量）、序列维度（每 m 个 token 压成一条目，如 DeepSeek-V4 的 CSA 与 HCA）、以及层维度（一部分层直接复用其他层的缓存与选择，或被更高效的层替代）。已有工作证明层维度压缩有效——IndexCache 跨层复用 Top-K 索引、YOIO 只算一次稀疏路由供所有层共享、HySparse 让稀疏层复用稠密层 KV——但索引复用并不节省 main KV 存储，全局路由共享限制性能，混合设计仍保留全注意力层，而且没有一种方法同时覆盖三个维度。",
                "CSA2 的做法是把三个维度联合起来：跨层共享 main KV 与 indexer K，同时允许层复用 Top-K 索引，并把「缓存共享」与「索引复用」解耦。相对 CSA 它做了两处简化——压缩器去掉了相邻压缩条目之间重叠的源条目和绝对位置编码，indexer K 改为直接由 main KV 投影得到（而不是另走一条从隐状态出发的压缩路径）。这两处既简化实现也提升训练效率。每个 query 会关注被选中的条目，加上该层本地的 SWA KV。",
                "CSA2 的每一层会被静态指定为三种模式之一。Full 模式：自己计算 main KV 与 indexer Q，从 main KV 投影出 indexer K，跑索引器产出全新的 Top-K 索引，责任与 DeepSeek-V4 里完整的 CSA 层一致。Reindex 模式：复用前一层最近的 main KV 与其 indexer K，但索引器用自己的 query 重新打分、产出新的 Top-K，让稀疏选择可以逐层变化而 KV 保持共享。Reuse 模式：同时复用最近的 main KV 和上一层算出的 Top-K 索引，直接做注意力，不再计算 indexer Q、也不评估索引分数，因此开销最低。三种模式都保留各自的全局 Q 与 SWA KV。",
                "论文特别指出，与 DeepSeek-V4 采用的 CSA 加 HCA 混合架构不同，DeepSeek-V4.1-Flash 用的是纯 CSA2。",
            ],
            "fig_after": {
                "2": [{"src": "fig04.png", "caption": "图 4：CSA2 的三种运行模式。绿色块表示当前层计算、黄色块表示复用最近 Full 层的 main KV 与 indexer K、红色块表示复用最近一次索引产出层（Full 或 Reindex）的 Top-K 索引；三种模式都要计算本层的 main Q 与 SWA KV"}],
            },
        },
        {
            "type": "h3",
            "title": "2.3.2 分层稀疏索引器",
            "paras": [
                "跨层索引复用减少了索引器求值次数，但剩下的索引器仍要在全部因果可见上下文上打分，超长上下文下这依然是主要瓶颈。既然解码器里较浅层索引器的信息可以自然用来限制更深层索引器的候选范围、且不引入额外状态，论文提出了分层稀疏索引器（Hierarchical Sparse Indexer），只用在 CED 的解码器里以降低解码期重复打分。",
                "对每个 query，第一个处于 Full 模式的层会构造一个候选池，后续需要重索引的层就在这个池内搜索。具体做法是：该层先给全部因果可见位置打分、产出自己注意力所需的 Top-K 索引；同时做块级候选选择——每个块取块内位置的最大索引分数，选出分数最高的若干块，把这些块覆盖的位置收进候选池（池比最终 Top-K 集合更大）。论文给的例子是选 2048 个块、每块 8 个位置，得到 16384 个候选位置。",
                "后续 Reindex 模式的层只在候选池内打分、在池内选出自己的 Top-K；Reuse 模式的层不做新索引，直接用最近一次针对所复用 KV 算出的 Top-K。于是候选池在索引层之间共享，最终选择却可以各不相同。",
                "机制的关键收益是：候选池大小固定时，每个后续索引器对每个 query 打分的位数与上下文长度无关（第一个 Full 层仍要扫描整个因果可见范围），代价从随上下文长度线性增长变为常数。这个机制是训练感知的，并且在后训练阶段引入——训练与推理施加完全相同的候选限制，保证深层索引器是在与推理一致的搜索域下被优化的。",
            ],
            "fig_after": {
                "2": [{"src": "fig05.png", "caption": "图 5：分层稀疏索引器。方块代表位置，绿色为被选中的索引，蓝色矩形为按最大索引分数选出的块；解码器第一个 Full 模式 CSA2 层选出自己的 Top-512 索引并据此建立共享候选池，后续 Reindex 层在该池内选出各自的 Top-512"}],
            },
        },
        {
            "type": "h3",
            "title": "2.4 高效架构扩展",
            "paras": [
                "Single-Pass mHC。DeepSeek-V4 引入的 mHC 会在相邻 Transformer 块之间维持多条残差流。理想情况下两块之间的残差变换是一次读写的完整映射，激活访存下界是 (2n+2)d；但 DeepSeek-V4 的实现分三个 kernel 顺序执行（残差更新、系数预测、输入混合），总访存达到 (4n+4)d，是下界的两倍，原因是输入混合用到的系数要等整个隐藏维度的归约完成才可用。Single-Pass 的做法是把输入混合的系数整体后移一个块——每个块改用上一块产出的系数，依赖就消失了，而性能损失可以忽略。部署时把残差更新、输入混合、系数预测融合成一个 Mega-mHC kernel，实现理想的 (n+1)d 读与 (n+1)d 写，把激活访存减半。",
                "Engram。这是一个把「记忆」与「计算」解耦的条件记忆模块，沿用此前的设计（分词压缩、多头哈希、上下文感知门控、多分支集成），但去掉因果短卷积，并把嵌入表优化换成动量更新加 Sinkhorn 平衡。196B 参数平均分给两个模块，每个模块使用 2、3、4 阶 N-gram，8 个哈希头，每个阶的嵌入维度 2048，每个头索引约 1600 万条目的表（表大小取互不相同的质数），嵌入表与键值投影都用 FP8。两个模块放在第 1、14 层以平衡各训练流水线阶段的内存占用；推理时确定性寻址让嵌入可以通过后台 RDMA 从主机内存预取，第一个模块的预取还能与第一个 Transformer 块的计算重叠。",
                "DSpark。这是投机解码模块，把半自回归草稿与置信度调度的验证结合起来：草稿器由三个滑动窗口 128 的 Transformer 块组成，一次前向并行算出 5 个草稿位置的基础 logits，一个轻量 Markov 头建模草稿 token 之间的依赖，一个置信头预测每个位置的条件接受概率并估计前缀存活概率；调度器再结合这些估计与实测的引擎吞吐曲线，为每个请求动态选择验证长度，目标是在当前系统负载下最大化整机 token 吞吐。与 DeepSeek-V3 里在预训练全程联合训练的 MTP 不同，DSpark 放在预训练之后的专门阶段训练（冻结主干），后训练期间继续训练但不把梯度回传主干。",
                "FP4 主 KV cache。DeepSeek-V4 已对 FP4 的索引器 query/key 做量化感知训练，现在把 QAT 扩展到主 KV cache，这里 FP4 的作用是省存储而非加速矩阵乘：注意力前先反量化，就能用更精确的格式而不必要求硬件原生支持该格式的矩阵乘。格式选了 OCP 标准的 MXFP4 以兼容尽可能多的硬件平台，具体采用 E2M1 加每 16 通道一个 E4M3 scale（参照 NVFP4，但省掉第二级全局 scale）。省掉这一级也足够：该格式支持的量级上限达 448×6=2688，远超缓存本身的幅度上界——RMSNorm 权重幅度上限约 1，做 RMS 归一化后 512 通道 KV 潜向量的 L2 范数至多约 √512，RoPE 保持范数不变，因此旋转后各通道绝对值上界约 22.6，训练中观测到的最大幅度也只在 10 左右。量化在 RoPE 之后进行，SWA KV 因对量化敏感而保留 FP8；相比 DeepSeek-V4 的 FP8 主 KV，这一格式在 HBM 和卸载到 SSD 时都把存储占用几乎减半。",
            ],
        },
        {
            "type": "h3",
            "title": "2.5 优化",
            "paras": [
                "在 DeepSeek-V4 的优化配置基础上，这一代做了两处针对性调整。第一处是 head-wise Muon：对 Query 权重按头拆分后再施加 Muon 更新。Muon 可以看作预条件梯度下降，原始 Muon 对所有头共用同一个预条件子，而按头拆分能给不同头不同的预条件子，更好地处理注意力头之间的异质性，实测优于原始 Muon——GLM 5 与 Kimi-K3 也验证了这一点。",
                "第二处针对 Engram 参数：如果对新引入的 Engram 参数用 Adam，优化器状态的内存占用会大幅增加。为降低训练内存，论文改用「动量更新 + Sinkhorn 平衡」来优化 Engram 嵌入表、token 嵌入与预测头。Sinkhorn 平衡此前被用于 SinkGD 里的线性层权重矩阵，这里扩展到这些大型参数矩阵——和 Muon 一样只需要一份动量缓冲，实测却优于 Adam。",
                "整体配置上，归一化层权重以及其他非矩阵参数（偏置、缩放因子）继续用 AdamW；语言模型主干线性变换、Engram 投影层与视觉语言投影器的权重矩阵用 Muon，其中 Query 与 Key 权重走 head-wise Muon；Muon 加了去耦权重衰减与 Nesterov 动量，归一化层权重也参与权重衰减，偏置与缩放因子不参与，Sinkhorn 平衡更新同样用 Nesterov 动量但不做权重衰减。预训练期间视觉编码器保持冻结（其最后一层归一化与视觉语言投影器仍可训练），直到学习率衰减阶段开始才解冻，并以更小的学习率与语言模型联合优化。",
            ],
        },
    ],

    "conclusion": [
        "**这两章讲的是一件事：KV cache 不再是长上下文服务的被动开销，而是可以被架构直接压下去的工程量。** 全局缓存 890 字节/token、持久缓存约 1/8，靠的是三层同时动手：CED 让解码器 KV 从编码器投影、prefill 只算一半层；CSA2 把跨层 KV 复用与索引复用解耦成 Full、Reindex、Reuse 三种模式；主 KV 用 FP4 存储、SWA KV 保留 FP8。",
        "更值得注意的是取舍方式：索引器分层只给解码器用、候选池大小固定就把索引成本从线性压成常数；FP4 只在 RoPE 之后量化、并放弃第二级全局 scale。都是先算清误差上界再动手——这种先量化再决策的思路，比一味追求更激进的压缩更值得借鉴。",
    ],

    "reference_url": "https://huggingface.co/deepseek-ai/DeepSeek-V4.1-Flash/resolve/main/DeepSeek_V41_Tech_Report.pdf",
    "title": "DeepSeek-V4.1-Flash 技术报告（第 1-2 章）：引言与架构",
}

out_path = os.path.join(_article_dir, "article_data.json")
with open(out_path, "w", encoding="utf-8") as f:
    json.dump(DATA, f, ensure_ascii=False, indent=2)
print("OK wrote", out_path, len(DATA.get("sections", [])), "sections")
