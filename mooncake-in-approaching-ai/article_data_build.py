#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""article_data_build.py — mooncake-in-approaching-ai

源文: https://kvcache.ai/blog/mooncake-in-approaching-ai/
逐段编译（blocks.jsonl 文本块 1:1 对应），保留原文加粗与项目符号格式。
"""
import json
import os
import sys

_article_dir = sys.argv[1] if len(sys.argv) > 1 else os.getcwd()

S = []


def h2(t):
    S.append({"type": "h2", "title": t, "paras": [], "fig_after": {}})
    return S[-1]


def h3(t):
    S.append({"type": "h3", "title": t, "paras": [], "fig_after": {}})
    return S[-1]


def fig(sec, name, caption, idx=-1):
    i = idx if idx >= 0 else max(0, len(sec["paras"]) - 1)
    sec["fig_after"].setdefault(str(i), []).append({"src": name, "caption": caption})


BULLET = ('<span style="color:#0F4C81;font-size:7px;line-height:1;'
          'vertical-align:middle;">●</span>&nbsp;')


# ══════════ 序言 ══════════
sec = h2('序言')
sec['paras'] += [
    '谁能用同样的算力产出更多 AI token，并且质量更高、更可靠，谁就拥有更高的 AI 生产力。'
    '随着 agent 从单轮问答走向持续规划、工具调用和多轮执行，token 需求正在从零散的 API 调用'
    '转向持续的大规模生产。在生产环境中部署领先的万亿参数模型后，Approaching.AI 现在每天稳定'
    '产出超过一万亿高质量 AI token。自 2026 年 2 月以来，单节点平均 token 产出效率提升三倍以上，'
    '总产能扩大三十倍以上。支撑这场大规模生产的，是覆盖推理引擎、调度、缓存与基础设施的一整套'
    '系统级优化，其中 Mooncake 扮演了关键角色。',
]

# ══════════ 背景 ══════════
sec = h2('背景')
sec['paras'] += [
    'token 工厂的成本结构相对固定：硬件采购与租赁、电力、数据中心和网络的支出基本固定，'
    '而收入取决于 **token 产出量 × 每 token 价格**。每 token 价格又直接取决于服务质量，'
    'TTFT、TPOT 和可靠性等指标决定了这些 token 能否以高标准交付。',
    '目标因此很明确：在严格满足 SLO 的前提下，**最大化系统吞吐量，以及单位算力的 token 产出**。'
    '任何性能优化都不能以牺牲 SLO 为代价。',
    'agent 负载的快速增长让这种矛盾更加突出。编码 agent、多轮推理和工具调用会反复复用很长的'
    '上下文。KV 缓存命中能大幅减少 prefill 计算，而一次缓存未命中可能意味着重算几十万个 token。'
    '除了浪费算力，这还会显著拉长响应时间，甚至阻塞其他请求的正常响应。',
    '在 Approaching.AI，这已经是一个万亿 token 级别的问题。生产推理系统必须**每天可靠地产出'
    '超过一万亿 token**。在这样规模的系统里，哪怕只浪费百分之几的算力，都会转化为巨大的'
    '基础设施成本。在万亿 token 级工厂里，KV 缓存不再是一个可选的本地优化，而是影响整体'
    '产能与成本的关键基础设施。',
    '**我们面对的核心挑战，是把 KV 缓存从单节点资源变成集群级共享资源**，在提升缓存命中率'
    '和系统吞吐的同时，仍然满足严格的 SLO。',
]

# ══════════ 从本地缓存到集群级 KV 缓存池 ══════════
sec = h2('从本地缓存到集群级 KV 缓存池')
sec['paras'] += [
    'KV 缓存的价值在 agent 与长上下文场景中进一步放大。但如果只依赖 GPU 的 HBM，'
    'KV 缓存的复用就存在一个内在取舍：分配的缓存空间太小会拉低命中率，迫使系统大量重算'
    '历史上下文；分配得太多又会占用处理请求所需的显存，降低新 token 计算的效率。'
    '上下文极长时，这个取舍尤其突出。',
    '因此我们先把 **SGLang HiCache** 引入生产环境，在 KV 缓存的层级中加入容量更大的主机 DRAM。'
    '这在基本不影响 GPU 计算效率的前提下，显著提升了可用 KV 缓存容量与命中率。',
    '但当系统规模扩展到每天超过一万亿 token 之后，单机缓存的局限很快暴露出来。',
    '一方面，单节点的 DRAM 容量终究有限，散落在不同节点上的重复 KV 缓存数据无法共享。'
    '另一方面，对于某些 KV 缓存结构，当时的 HiCache 架构会在同一节点内的多个 TP rank 上'
    '各存一份，导致单节点内最多出现八倍的数据冗余。这些问题让缓存命中率远低于理想值。',
    '更关键的是，**单机本地缓存实际上把 KV 缓存的位置和请求执行的位置绑在了一起**。',
    '当一个会话大部分请求的 KV 缓存只存在于特定的 prefill 节点上时，复用这些缓存就要求把会话'
    '后续的请求路由回同一批节点。这样，本应由实时负载、请求特征和资源可用性驱动的调度决策，'
    '就被缓存所在的位置限制了。',
    '如果 KV 缓存始终是各节点私有的，缓存复用和集群调度就被耦合在一起：**调度器每扩大一次'
    '候选集合，都可能以更低的缓存命中率、更多的重算甚至 SLO 违约为代价**。集群规模越大，'
    '这种耦合不仅限制了资源池化带来的收益，也压缩了调度算法的设计空间。系统难以根据流量波动、'
    '节点故障等实时状况灵活迁移请求，也难以结合上下文长度、缓存命中、计算特征和不同计算节点的'
    '硬件特征，做出更细粒度的调度决策。',
    '以负载均衡为例。为了提高命中率，把相同前缀的请求持续路由到少数几个 prefill 节点，'
    '有时会把热门请求集中到同一节点上。请求随即在该节点上堆积排队，违反 TTFT 的 SLO。'
    '而把请求挪到其他节点来缓解热点，又会因为 KV 缓存无法跨节点复用而触发大量重算。'
    '在高峰时段，这甚至可能把压力扩散到新节点上。',
    '对万亿 token 级工厂来说，这已经不是几个百分点缓存命中率的问题，而是直接影响'
    '**集群调度灵活性、峰值吞吐能力、故障韧性以及 SLO 达成能力**的系统级问题。',
    '因此我们引入 **Mooncake Store**，把原先分散在各个节点上的 KV 缓存汇聚成一个集群级共享资源。',
    '我们希望同时达成三件事：',
    BULLET + '**进一步提升 KV 缓存命中率**；',
    BULLET + '**解除 KV 缓存位置对集群调度的约束**；',
    BULLET + '**不为推理关键路径引入额外性能开销或系统风险**。',
    '从架构上看，**SGLang HiCache 与 Mooncake Store 的组合天然解决了前两个目标。真正的挑战在'
    '第三个：缓存系统必须足够快、足够稳，绝不能拖慢、更不能拖垮推理系统**。这也是过去六个月'
    '我们把 Mooncake 推向万亿 token 级生产环境时的核心工程难题。',
    '下面先介绍整体部署架构，再说明我们如何解决这些核心挑战。',
]

# ══════════ SGLang + Mooncake 的生产实践 ══════════
sec = h2('SGLang + Mooncake 的生产实践：架构与做法')
sec['paras'] += [
    '当 KV 缓存从单机资源变成集群级共享资源，它就不再只是一个缓存系统，'
    '而必须与计算、网络和调度协同设计。',
]

sec = h3('架构总览')
sec['paras'] += [
    'Approaching.AI 的生产推理系统由多个 group 组成，每个 group 包含若干 GPU 节点，'
    '并通过 RDMA 做高速 KV 缓存传输。请求先在网关层分发，再按路由策略转发到具体的 group。'
    '下面的讨论聚焦单个 group 内 SGLang + Mooncake 的部署方式。',
    '每个 group 内部采用 prefill 与 decode 分离的部署方式，并且只在 prefill 节点上开启 HiCache。'
    '这是因为长上下文请求的主要计算开销在 prefill 阶段，缓存命中能直接省掉生成首个 token 之前'
    '的大量重算。decode 阶段追求稳定、低延迟的逐 token 生成，因此我们尽量让 decode 路径'
    '保持简单和稳定。',
]
fig(sec, 'fig00.png', '图 1：单个 PD 组内 HiCache + Mooncake Store 的架构', 0)

sec = h3('Mooncake Store：把节点内存变成共享 KV 缓存池')
sec['paras'] += [
    'Mooncake Store 在所有 prefill 和 decode 节点上以独立的 Store Service 形式运行。',
    '在 decode 节点上，主机内存主要由 Mooncake Store 使用；在 prefill 节点上，'
    '内存由 HiCache 和 Mooncake Store 共享。这样就把原本散落在各节点的 DRAM 组织成一个'
    '更大的分布式 KV 缓存池，为跨节点复用打下基础。',
    '每个 prefill 节点上嵌入 HiCache 的 Mooncake 客户端被配置为不向全局缓存贡献存储，'
    '存储由独立的 Store Service 进程提供。这让推理引擎与缓存系统解耦，两者可以各自独立'
    '升级与扩容。考虑到服务器都是双 NUMA 节点拓扑，我们把一个 Mooncake Store Service 绑定到'
    '一个 NUMA 节点，尽量为本地 KV 缓存访问和网络传输保留 NUMA 亲和性，减少跨 NUMA 访问开销，'
    '并充分利用 Mooncake Transfer Engine 的拓扑感知传输能力。',
    '控制面上，Mooncake Master 配置三个副本，一主两备，分布在三个 GPU 节点上。'
    'Mooncake 依赖的 etcd 服务同样使用三个副本，与其他集群管理组件一起运行在专用的 CPU 节点上。',
]

sec = h3('用 SMG 做集群调度')
sec['paras'] += [
    '只有共享的 KV 缓存还不够。缓存池化扩大了请求可被调度到的节点集合，但调度器仍需在'
    '缓存局部性、实时负载、节点健康和请求特征之间做出合理决策。例如，只追求缓存局部性'
    '以减少跨节点网络传输，可能把相同前缀的请求反复送到少数 prefill 节点，最终形成热点；'
    '而只关注负载均衡，又可能频繁把请求迁到缓存较冷的节点，增加远程访问甚至重算。',
    'group 内的请求调度基于 **SMG（SGLang Model Gateway）**，并针对生产环境做了改进。'
    '在 prefill 侧，调度器会考虑 HiCache 的本地命中率、节点实时负载、节点健康和请求特征等信息，'
    '在满足 SLO 的前提下尽量提升整体系统吞吐、降低请求 TTFT。',
    '在 decode 侧，重点是持续生成过程中的吞吐量与尾延迟稳定性，调度器会尽可能把生成负载'
    '均匀分散到多个 decode 实例上。',
]

sec = h3('用 RBG 做生产编排')
sec['paras'] += [
    '在 Kubernetes 层，我们用 **RBG（RoleBasedGroup）** 统一管理 SGLang 和 Mooncake 的工作负载。'
    'RBG 为不同应用角色提供拓扑定义与协调策略，让 prefill、decode、Mooncake Store 等角色'
    '作为一个整体部署和管理。作为基础元数据组件，etcd 独立运行，不纳入 RBG 管理。',
]

# ══════════ 把 Mooncake 带进万亿 token 级工厂 ══════════
sec = h2('把 Mooncake 带进万亿 token 级工厂')
sec['paras'] += [
    '要把 Mooncake 带进每天产出超过一万亿 token 的生产环境，核心问题不只是 KV 缓存能否共享，'
    '而是 **Mooncake 能否跟上 token 工厂极其苛刻的性能与稳定性要求**。'
    'SGLang HiCache + Mooncake Store 在架构层面解决了缓存命中率和跨节点调度问题，'
    '但前提是这套缓存基础设施要足够快、足够稳，不会给推理路径引入新的性能开销或系统风险。',
    '自 2026 年 2 月以来，Approaching.AI 单节点平均 AI token 产出效率提升三倍以上，'
    '总 token 产能扩大三十倍以上，KV 缓存命中率也显著改善，始终稳定在 90% 以上。'
    '这意味着 Mooncake 必须在每个节点上承受数倍增长的 KV 缓存吞吐，同时支撑单个集群'
    '纳入更多推理节点。**应用层每提升一分 token 产出效率，都要求底层缓存系统同步前进**。'
    '过去六个月，这就是悬在 Mooncake 生产优化工作头上的达摩克利斯之剑。',
]

sec = h3('快速取数：把远程 KV 缓存读取藏在计算背后')
sec['paras'] += [
    'KV 缓存复用主要发生在 prefill 阶段。对一个 prefill 请求，HiCache 会先查本地缓存；'
    '本地未命中时，它通过 RPC 向 Mooncake Master 查询对应 KV 缓存的元数据，'
    '再用 RDMA 从多个 Mooncake Store 节点并行取回可用数据。数据就绪之后，请求才进入 GPU '
    '完成剩余的 prefill 计算。prefill 完成后，新算出、Mooncake 中尚不存在的 KV 缓存会被写回 '
    'Store，供后续请求复用。随着 KV 缓存数据持续写入，Mooncake Master 还必须持续维护元数据'
    '并执行 LRU 淘汰。',
    '对生产推理来说，取数速度至关重要。请求进入调度队列后，HiCache 会尽早异步发起远程 KV 缓存'
    '预取，让网络传输与 GPU 上已在执行的计算重叠。因此 Mooncake 需要确保 **KV 缓存的取回'
    '尽量被藏在前面请求的执行时间之后**。只要远程数据在 GPU 开始处理当前请求之前就绪，'
    'Mooncake Store 给推理引擎带来的额外开销几乎不可感知。反过来，如果取数跟不上调度节奏，'
    'GPU 就必须等待 KV 缓存到位，缓存系统反而制造了新的空闲时间和 TTFT 开销。',
    'Mooncake 的批量读数据路径很直接：一次 Master RPC 查询定位缓存数据，随后从多个 Store 节点'
    '并行直读 RDMA。因此读性能主要取决于两个因素：Master RPC 的查询延迟与吞吐，以及 RDMA '
    '传输效率，后者又取决于网络带宽和 Transfer Engine 的性能。我们的生产部署通常使用 '
    '800 Gbps 高性能网卡。再加上 Transfer Engine 在架构上的优势，例如多网卡聚合和拓扑感知的'
    '路径选择，在当前的网络配置和负载特征下，我们尚未观察到 RDMA 数据传输成为主要性能瓶颈。'
    '优化第一个因素之后，生产环境中批量 KV 缓存读请求的平均延迟低于 50 毫秒，绝大多数在 100 '
    '毫秒内完成。这使远程 KV 缓存复用尽可能远离 GPU 的等待路径。',
]

sec = h3('扩展到更大的集群')
sec['paras'] += [
    '把 KV 缓存跨节点池化之后，我们希望每个 Mooncake 集群能服务尽可能多的 prefill '
    '和 decode 节点。更大的 group 意味着更宽的缓存复用范围、更强的流量波动吸收能力，'
    '以及更大的调度优化空间。',
    '我们采取渐进式扩容，而不是一步到位。先在小规模 group 中验证稳定性，再在测试集群中'
    '扩大规模做长时间稳定性测试，持续监控性能与稳定性瓶颈。问题解决后再进入生产灰度发布，'
    '确认稳定后全量部署，并以这次部署为基础继续扩到更大的 group。通过这种分批验证和'
    '逐步扩大的过程，我们把集群扩容本身变成了一套可控、可重复的工程流程。',
    '这个过程里我们遇到了不少问题与挑战。',
    '**第一个是 Master 服务的性能瓶颈**。Mooncake Master 中的数据被哈希到 1024 个分片，'
    '每个分片有自己的读写锁来控制并发访问。随着 Store 缓存容量和并发请求增加，Master 必须'
    '处理更频繁的元数据查询，周期性触发的 LRU 淘汰也更耗时。淘汰过程中，淘汰线程会依次获取'
    '每个分片的写锁。元数据过多时，它会长时间持有分片写锁，阻塞读写请求，'
    '让淘汰期间的请求延迟显著上升。',
    '我们从两方面解决：提升淘汰效率，减少淘汰与读写请求之间的锁竞争。前者，我们大幅削减了'
    '字符串拷贝、对象构造和内存分配的开销；同时增强写入机制，允许推理引擎的多个 rank 写入'
    '同一对象的不同偏移，从而显著减少缓存对象数量。后者，我们先把副本销毁、内存释放等'
    '耗时操作移出淘汰的写锁临界区，缩短持锁时间；随后把锁粒度从分片进一步细化到对象，'
    '大幅降低竞争。',
    '**第二个是扩容时的性能问题**。一个 Store 节点上线时，需要分配数 TB 内存并把这块内存'
    '注册到多张 RDMA 网卡；下线时，又要把内存从这些网卡上注销并释放。这个过程非常耗时。'
    '我们优化了内存初始化和 RDMA 注册，取得数倍性能提升，随后加入大页支持，'
    '进一步缩短内存分配、注册、注销和释放的时间。',
    '在 Master 侧，过去让一个 Store 节点下线需要遍历所有分片以删除该节点相关的元数据，'
    '否则后续请求会尝试从已下线节点读取数据而失败。元数据量大时，这个过程可能耗时很久，'
    '显著拖延节点摘除。我们把节点摘除拆成两个阶段：「同步失效、异步清理」。先把该 segment '
    '从分配池中移除并标记为下线中，阻止后续请求读写这个节点的数据，再由后台线程清理元数据。'
    '这使 Master 完成一次节点摘除请求的时间降到毫秒级。',
    '**第三个是 KV 缓存传输过程中的网络问题**。我们大多数集群使用 RoCE 网络并开启基于 RTT 的'
    '拥塞控制。基于 Mooncake 的 prefill-decode 分离与 KV 缓存复用会产生大量 KV 缓存传输流量。'
    '在生产环境中，我们观察到频繁的 all-to-all 微突发与 incast、ECN/CNP 报文明显尖峰，'
    '以及服务器内部拥塞导致的 PFC 计数上升。但经过长期监控与测试，我们判断这些现象'
    '并不会显著影响 KV 缓存传输速度或 TTFT 等生产指标，也不影响推理集群吞吐。',
    '对 KV 缓存传输来说，生产环境的网络稳定性与抖动比网络性能本身更值得关注。我们发现'
    '很多故障与 Mooncake Transfer Engine 的实现无关，而是源于集群配置。例如 OVS 配置、'
    '路由配置、IOMMU 配置、网卡固件与驱动版本不一致，以及 Kubernetes 网络故障，'
    '都可能导致 Mooncake Transfer Engine 报错。排查 RDMA 网络错误因此需要检查整条链路上的'
    '潜在故障点，而不是只看 nccl-tests 这类连通性测试。',
]

sec = h3('集群稳定性与安全：控制故障的影响半径')
sec['paras'] += [
    '对大规模推理集群来说，性能决定系统的上限，而稳定性决定这个上限能否在生产中被放心使用。',
    '集群变大之后，硬件故障、网络抖动、进程错误和配置失误会逐渐从偶发事件变成日常事件。'
    '而 Mooncake Store 又处在共享 KV 缓存数据路径上：如果缓存系统的故障传导到推理引擎，'
    '一个局限在单个 Store 节点、单张网卡或单条传输链路上的问题，就可能升级为整个推理集群的'
    '请求阻塞、TTFT 波动甚至可用性下降。',
    '因此在部署 Mooncake 的早期，我们在稳定性上采取了保守策略：**分布式 KV 缓存服务可以'
    '短暂不可用，但绝不能影响推理服务本身；局部故障可以发生，但绝不能升级为集群级故障**。',
    '按照这个原则，我们在集群隔离、网络隔离、超时与熔断、数据分配策略上加入了一系列'
    '保护措施。其中一些在初期上线时刻意做得偏保守，随着 Mooncake 与集群网络环境趋于稳定，'
    '可以依据运维经验适当简化或移除。但在系统快速扩张的阶段，这些机制帮助我们控制了'
    '新基础设施带来的风险与故障半径。',
    '第一，我们没有让所有推理节点共用一个巨大的 Mooncake Store 集群，而是保留推理系统的 '
    'group 边界，在每个 group 内部署独立的 Mooncake Store 集群。缓存服务在 group 之间相互隔离，'
    '即使某个 group 的 Mooncake Store 整体不可用，影响也局限在该 group 内，不会波及其它 group '
    '的 KV 缓存服务。这牺牲了一部分跨 group 缓存共享的潜在收益，但换来了更清晰的故障域，'
    '也让灰度升级、扩容和故障处理更可控。',
    '第二，我们进一步隔离了 Mooncake 使用的网络资源。生产中，prefill-decode 分离本身就需要 '
    'prefill 与 decode 节点之间的 RDMA 传输，而 Mooncake Store 又引入了另一股可观的 KV 缓存'
    '读写流量。如果两类流量完全共用同一批网卡和 Transfer Engine，任何一侧出现异常流量、'
    '资源竞争或 Transfer Engine 故障，都可能影响另一侧。因此在初期上线时，我们为 '
    'Mooncake Store 分配了专用网卡，并为 Store 与 prefill-decode 分离使用独立的 '
    'Transfer Engine 实例，尽可能在数据面和软件实例两个层面切断故障传播路径。',
    '更重要的保护来自超时与熔断。HiCache 本身提供了读超时机制：当一次从 Mooncake 的 KV 缓存'
    '读取超过可配置阈值时，HiCache 停止等待未完成的传输，只使用已经成功取回的缓存数据，'
    '其余部分重新计算。这保证了即使某次远程读取出现长尾延迟，也不会阻塞 prefill 计算。',
    '在此之上，我们增加了 **Mooncake Store 集群级熔断器**。当系统检测到某个 Store 集群'
    '持续报错或不可用时，可以自动把 HiCache 与该集群断开，临时退化为仅本地 KV 缓存，'
    '让推理继续运行。',
    '我们还专门优化了 KV 缓存的数据分配策略，以缩小单节点故障的影响范围。在 Mooncake 默认的'
    '分配策略下，KV 缓存数据被随机分配到 Store 节点上，一个长请求的 KV 缓存会散布在几乎所有'
    '节点上。一旦某个节点故障、KV 缓存中间某个块丢失，即使后续缓存块仍然可用，也无法再用于'
    '前缀复用，从而放大了故障影响。随机的远程写入还会增加跨节点网络开销与延迟。因此我们实现了'
    '一套新的分配策略：写入 KV 缓存数据时，Mooncake 优先使用本地 Store；本地空间不足时，'
    '按确定的顺序尝试其他节点。对同一个写入节点而言这个顺序保持一致，使一个请求的 KV 缓存'
    '尽量集中在前几个候选节点上；不同的写入节点使用不同顺序以维持负载均衡。',
]

# ══════════ 未来展望 ══════════
sec = h2('未来展望')
sec['paras'] += [
    'SGLang HiCache + Mooncake 目前已经可靠支撑 Approaching.AI 每天超过一万亿 token 的生产，'
    '但在缓存容量、缓存复用范围和硬件配置上仍有优化空间。我们将关注以下几个方向。',
    '**引入 SSD 作为第三级缓存**。在 agent 负载中，KV 缓存的生命周期呈现长尾分布：'
    '大部分缓存数据很快失效，但有一小部分会在很久之后被再次使用。把这类数据留在昂贵的 DRAM 中'
    '并不划算。我们计划引入 SSD 作为下一级缓存，把冷 KV 缓存数据从 DRAM 异步迁移到 SSD，'
    '在控制成本的同时扩大有效缓存容量，进一步提升 KV 缓存命中率。',
    '**联邦 Mooncake：实现跨 group 的 KV 缓存复用**。目前我们的 Mooncake Store 集群在'
    '各 group 内独立部署。这种架构清晰地隔离了故障域，但也给 KV 缓存复用人为划出了边界：'
    '即使相同前缀已经存在于另一个 group，进入新 group 的请求也无法直接复用。接下来我们会在'
    '继续扩大单个 group 的同时打破 group 边界，计划保留独立集群，同时允许 Mooncake 客户端'
    '在必要时跨集群查询和读取 KV 缓存数据。这将进一步提升全局缓存命中率，也让路由拥有更大的'
    '调度灵活性，请求不必再在命中缓存和跨 group 调度之间做非此即彼的选择。跨 group 共享'
    '自然会带来更复杂的元数据管理和网络流量控制，以及更大的故障传播风险。随着 KV 缓存复用'
    '范围扩大，我们也必须同步提升跨集群的故障隔离与容错能力，确保更广的共享不会以更大的'
    '故障半径为代价。',
    '**支持异构推理集群**。随着推理基础设施演进，token 工厂将不再由完全同构的 GPU 节点组成。'
    '我们计划支持更灵活的异构部署，例如为 prefill 和 decode 节点使用不同的加速器。'
    '作为连接不同计算节点的共享 KV 缓存基础设施，Mooncake 可以进一步松绑推理服务与特定'
    '计算设备之间的耦合。通过进一步解耦计算、传输与缓存，不同阶段的任务可以被动态分配到'
    '最契合其负载特征的计算资源上。',
]

# ══════════ 致谢 ══════════
sec = h2('致谢')
sec['paras'] += [
    '感谢 Mooncake 与 SGLang 社区在 Approaching.AI 生产部署与持续优化过程中给予的大力帮助与支持。',
    '文中讨论的 Mooncake 优化与改进，正在逐步回馈到开源社区。',
]

# ══════════ 相关链接 ══════════
sec = h2('相关链接')
sec['paras'] += [
    'Approaching.AI：https://approaching-ai.com/en/',
    'Mooncake 项目：https://github.com/kvcache-ai/Mooncake',
    'SGLang 项目：https://github.com/sgl-project/sglang',
]


DATA = {
    'title': '每天超一万亿 token：Mooncake 如何支撑 Approaching.AI 的 token 工厂',
    'summary': [
        {'key': '核心规模', 'body': '每天稳定产出超过一万亿 token，单节点效率提升三倍以上，'
                                    '总产能扩大三十倍以上。'},
        {'key': '关键改造', 'body': 'HiCache 加 Mooncake Store 把 KV 缓存变成集群级共享池，'
                                    '命中率稳定高于 90%。'},
        {'key': '难点所在', 'body': '共享只是起点，真正的挑战是缓存必须快到不拖慢推理、'
                                    '稳到不拖垮推理。'},
    ],
    'lead': [
        '把同样的算力变成更多的 token，就是更高的 AI 生产力。Approaching.AI 在生产环境中'
        '把 KV 缓存从单机资源改造成集群级共享资源池，支撑起每天超过一万亿 token 的产出。',
    ],
    'sections': S,
    'conclusion': [
        '把缓存做成基础设施，而不是优化项，是万亿 token 级工厂的入场券。',
        '① **规模变了，问题就变了**：单机缓存把最多八倍的数据冗余和调度耦合一起带进了'
        '万亿 token 的产能里，Mooncake Store 要解决的不是缓存快不快，而是缓存的位置'
        '不再决定请求该去哪台机器。',
        '② **难的是第三个目标**：命中率和调度的收益由架构直接带来，真正吃力的是不把延迟'
        '和风险带进推理关键路径。批量读延迟压到 50 毫秒以下、读超时、Store 集群级熔断、'
        '专用网卡与独立 Transfer Engine，做的都是同一件事，让缓存的问题止步于缓存。',
        '③ **稳定性来自切分故障域**：分组隔离、同步失效加异步清理、本地优先的确定性分配，'
        '把一次节点故障限制成一次节点故障，而不是整集群的请求阻塞。',
        '对做 LLM serving 的人来说，一条值得记住的判据是：当缓存命中率长期高于九成、'
        '缓存成为跨节点共享资源之后，它就不再是性能加分项，而是决定可用性的组件。'
        '它的隔离、超时和降级路径，应该和推理引擎本身一起设计，而不是等故障发生后再补。',
    ],
    'reference_url': 'https://kvcache.ai/blog/mooncake-in-approaching-ai/',
}

out_path = os.path.join(_article_dir, 'article_data.json')
with open(out_path, 'w', encoding='utf-8') as f:
    json.dump(DATA, f, ensure_ascii=False, indent=2)

n_paras = 0
n_figs = 0
for s in S:
    n_paras += len(s['paras'])
    for k, v in (s.get('fig_after') or {}).items():
        assert int(k) < len(s['paras']), f'fig_after 越界: {s["title"]} key={k} paras={len(s["paras"])}'
        n_figs += len(v)

body_chars = sum(len(p) for s in S for p in s['paras'])
print(f'✅ 写入 {out_path}')
print(f'   sections={len(S)} paras={n_paras} figs={n_figs} body_chars={body_chars}')
print(f'   summary={len(DATA["summary"])} lead={len(DATA["lead"])} conclusion={len(DATA["conclusion"])}')
