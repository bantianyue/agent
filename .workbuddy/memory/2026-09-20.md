# 2026-09-20

## codex → WorkBuddy skill 迁移（11 个业务 skill）

用户从 Codex 迁移到 WorkBuddy，要求把 `.codex\skills` 下的 skill 复制一份到 WorkBuddy 用户级目录，源目录保持不变。

**落点**：`C:\Users\twfehh7\.workbuddy\skills\`（11 个 skill，358 文件 / 3.02 MB）
**源**：`C:\Users\twfehh7\.codex\skills\`（未改动，419 文件 / 3561361 字节，复制前后 sha256 一致）

### 依赖闭包结论（grep 实证，非推测）
- `wechat-article-sop` 只真实引用两个 skill：**baoyu-post-to-wechat**（硬依赖，`wechat-api.ts` 是推送引擎，被 15+ 篇 ref 以绝对路径调用）、**figure-extraction**（软依赖，iframe 动画转 GIF 配方）。
- 其余 8 个（paper-analysis-wechat / url-content-extraction / web-content-fetch / video-to-wechat-article / video-to-article / article-collect / article-tracker-maintenance / articles-content-edit）在 wcsop 内引用数为 0，是并列入口。
- 用户最终选择全部 11 个都复制。

### 关键决策
1. **排除 `node_modules`** — `baoyu-post-to-wechat\scripts\node_modules` 独占 6335 文件 / 54.9MB，占全部 skill 体积的 91%；源仓库 `.gitignore` 第 2 行已写「可 npm install 重建，不入库」。需要时 `cd scripts && bun install` 重建。
2. **改写硬编码绝对路径** — `.codex/skills` → `.workbuddy/skills`，25 个文件 39 处（`pipeline.json` 9 处、`SKILL.md` 4 处、`references` 20 个文件各 1-3 处）。副本已自包含、无残留。
3. 排除 `.git`（源是 git 仓库）、`__pycache__`/`*.pyc`；保留 `SKILL.md.bak.*` 4 个历史备份（忠实复制）。

### 运行时可运行性（已冒烟测试通过）
- 统一运行时 `C:/Users/twfehh7/AppData/Local/hermes/hermes-agent/venv/Scripts/python.exe`（3.11.15）**路径未变，继续有效**。
- jinja2 / pygments / yaml / bs4 / PIL / httpx / websockets 全部可导入；PyMuPDF 1.28.0 可用。
- `render-article.py --help`、`preflight-check.py` 均正常执行。

### ⚠️ 遗留摩擦点（未修，需时再处理）
18+ 个 md 文件引用 Codex/Hermes 专属工具，WorkBuddy 无对应能力：
`delegate_task`、`execute_code`、`process poll`、`read_file`、`terminal()`、`hermes send`、`hermes skills install`、ClawBot 通知。
`references/failure-modes.md`（10 处）、`humanizer-subagent-completion.md`（7 处）最密集。
**影响**：脚本链（提取→render→preflight→push）是独立 Python，可原样跑；受影响的是 SOP 里要求 agent 用 subagent / 后台进程 / 通知的那几条编排指令。

### 环境坑
- Bash 工具在本机不可用（shim 报 `dirname: command not found`）。
- PowerShell 的 stdout 不回传 → 一律 `Out-File` 落盘 + Read 读取。
- Windows 沙箱会拦 `node_modules` 内个别路径（本次是 `cheerio\dist\commonjs\types.d.ts`），导致 `shutil.copytree` 中途抛错留下半成品；排障时改用 `dirs_exist_ok=True` 幂等重跑。

### 产出物
- `_tmp_skillmig/audit_report.md` — 安装前安全审计（结论：无真实凭据硬编码、无 base64 混淆、无 eval 混淆；仅 AppID `wxc1e78e05c88ccbcb` 出现在排障文档，属公开标识）
- `_tmp_skillmig/migration_report.md` — 迁移与校验报告
