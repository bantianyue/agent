# 2026-09-21 工作日志

## skill 迁移收尾：验证 + 补齐 baoyu node_modules

- 复核 `C:\Users\twfehh7\.workbuddy\skills\` 下 12 个 skill 全部在册、SKILL.md 齐全、frontmatter name 与目录名一致。
- 残留 `.codex/skills` 路径仅剩 1 处，在自建的 `agent-skill-migration/SKILL.md` 里（记录源目录，属正常），11 个业务副本为 0。
- 源目录 `C:\Users\twfehh7\.codex\skills` 完好未动。
- 实测通过：`wcsop-step.py --help` 正常输出；Python 3.11.15 运行时可用。
- **补齐了 baoyu 依赖**：在 `baoyu-post-to-wechat/scripts` 执行 `bun install`（5 个直接依赖：@jsquash/webp、baoyu-chrome-cdp、baoyu-md、jimp、socks），164 包 / 8.7s / 54.97MB 装完。
- 推送引擎实测可加载：`bun wechat-api.ts --help` 正常打印完整用法（含 --theme/--cover/--account/--draft-media-id 等）。
- 结论：wcsop 全链路（提取 → render → preflight → 推送）现已可用。剩余唯一差异是 SOP 编排指令里的 Codex/Hermes 专属工具名（delegate_task / execute_code / process poll / terminal() 等），不影响纯脚本链。

## 环境事实补充

- `bun` 已装：`C:\Users\twfehh7\AppData\Roaming\npm\bun.ps1`（v1.3.14），可直接用。
- PowerShell 捕获子进程中文输出时会乱码（`Out-File -Encoding UTF8` 也救不回），判断脚本是否正常只看法文/英文结构行即可，不必因乱码怀疑文件损坏。
- PowerShell 批量 `Remove-Item -LiteralPath <数组>` 连续 exit 1 不生效；改用 venv python `os.remove` 一次成功。

## wcsop 实战：sglang-omni-code2wav-batchsize（X 长文 2101785062157193316）

- 全流程跑通并推送草稿：目录 `D:\06_Hermes\articles\sglang-omni-code2wav-batchsize`，draft.id 存于该目录。
- 原文是中文 X Article（89 blocks），走「原文直用」模式未翻译。4 正文图 + 封面、4 代码块、1 表格，preflight ALL PASSED，草稿回读核验通过。
- 临时文件已清理（removed=15），保留产物：article.html / article_data.json / article_data_build.py / fig01-04.png / cover.png / cover-square.png / draft.id / progress.md / source.url。

## wcsop 实战：pufferlib-5-degrees-parallelism（X 长文 2099512342539391130）

- 全流程跑通并推送草稿：5 节 16 段中文编译 + 2 图（fig00.gif 动图 + fig01.png 带图注）+ 封面，preflight ALL PASSED，verify 2/2 图，draft.id 在文章目录。
- **pw-extract 兜底路径验证**：图注绑定失败（渲染 DOM 图序 0 vs 原文有 1 条图注 → exit 1）时，直接用 fxtwitter JSON 手动保序提取完全可行——手写 blocks.jsonl（type=figure + after_section/after_para）+ article_data_build.py，write-article-data.py 正常走 derive_fig_after 派生图位。
- **video.twimg mp4 三通道全挂**（直连 10054 / 代理 502 / weserv 不支持视频）→ CDP 页面内 fetch 成功：Chrome 9222 新建 tab 直接导航到 mp4 URL（同源），Runtime.evaluate fetch→FileReader→dataURL，531KB 一次拿到。静态图仍走 weserv（urllib + ProxyHandler({}) 去环境代理）。
- **mp4→GIF 压缩参数阶梯**：palettegen/paletteuse，900w/64色=3.97MB、820/64=2.81、760/64=2.45、700/48+bayer=1.94MB（181 帧全保留）。源 15fps 只有 181 帧时不要为凑 200-300 帧去抽帧。
- **新坑：resolve_local_src 静默解析错扩展名**——blocks.jsonl 写 `fig01.png` 但当时磁盘只有 `fig01.gif`（动图），它会按扩展名轮询解析成 gif，渲染出 src="fig01.gif" 而 preflight 报 Missing。正确顺序：先把图按 0-indexed `figNN.ext` 落盘对齐 blocks img 字段，再跑 write。
- 短文段落阈值：write-article-data 硬校验 sections 合计 ≥15 段（compact 模式也是 max(15, 40%)），短文靠合理拆段满足，不注水。

## skill 修正：wechat-article-sop/references/x-article-draftjs-extraction.md

- **entityMap 取 entity 方式不唯一**（旧文档写死「key 即列表下标」，本篇实测必须按每项自己的 `key` 匹配，否则 6 个 atomic 全误判 LINK、图全丢）。已改为 `make_ent_lookup()` 三形态自适应（dict / 下标型 list / 散乱 key 型 list）+ MEDIA 计数断言；同步修正 LATEX 节与 mediaId 注释里的 `emap[int(key)]` 旧写法。
- **新增节**：fxtwitter 响应体被 latin-1 二次编码（中文变 `æå³`），还原 `s.encode('latin-1').decode('utf-8')`，须 `rb` 读字节判断；处理顺序 = 编码还原 → `\_` 清洗 → json.loads。
