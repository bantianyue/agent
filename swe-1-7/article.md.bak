<div style="background:#e8f4fd;padding:14px 16px 10px 16px;border-radius:6px;margin-bottom:18px;">
<div style="text-align:center;margin-bottom:10px;">
<strong style="font-size:16px;color:#1a6ba0;">要点速览</strong>
</div>
<div style="font-size:14px;color:#3f3f3f;line-height:1.75;">
- <strong>性价比破局</strong>：SWE-1.7在FrontierCode 1.1 Main拿到43.0%，逼近GPT-5.5(43.0%)和Claude Opus 4.8(46.5%)，但每个rollout成本仅约4美元，远低于对手的8-10美元<br><br>
- <strong>后训练天花板被打破</strong>：基座是已经过大量RL后训练的Kimi K2.7，再次RL仍带来巨大增益，说明RL能把能力推得比此前认为的更远<br><br>
- <strong>top-p回放保熵</strong>：用采样分布回放（sampling distribution replay）解决长周期RL的熵崩塌与训练-推理不匹配，让熵在整个训练过程保持恒定<br><br>
- <strong>跨三洲多集群</strong>：仅靠对象存储传压缩权重delta（体积缩减99%+），1T模型跨大陆权重同步1-2分钟完成，硬件故障不中断训练<br><br>
- <strong>自压缩+交替长度惩罚</strong>：Agent学会总结自身状态续跑，rollout最长可达6小时；推理更彻底、改动范围更大，行为明显区别于基座
</div>
</div>

---

## 发布与基准表现

今天发布的SWE-1.7，是Cognition迄今训练出的最强模型。它把前沿级别的软件工程智能，压到了远低于同行的成本线上，直接把性价比的帕累托前沿向前推了一大步。

最关键的信号在成本：SWE-1.7在FrontierCode 1.1 Main拿到43.0%，与GPT-5.5（43.0%）几乎持平，略低于Claude Opus 4.8（46.5%），但已经超过了Claude Opus 4.7（42.3%）和其基座Kimi K2.7（38.5%）。而它每个rollout的平均花费只有约4美元，Opus 4.8和GPT-5.5则要到8-10美元。在「分数-成本」图上，SWE-1.7稳稳站在低成本高分数的前沿位置。

![](img1.png)
<span style="font-size:12px;color:rgb(153,153,153);">FrontierCode 1.1 Main：分数（%）vs 平均每次rollout成本（美元），SWE-1.7位于低成本高分区</span>

在多基准上，SWE-1.7同样处于第一梯队：Terminal-Bench 2.1通过率81.5%，SWE-Bench Multilingual 77.8%，均紧追Opus 4.8（86.9%/84.4%）和GPT-5.5（84.2%/76.8%）。

模型今天已上线Devin（Web、Desktop、CLI），通过Cerebras提供，速度达1000 TPS。

值得一提的是**基座的选择**。SWE-1.7是在Kimi K2.7基座上训练的，而该基座本身已经过大量RL后训练。在这样一个「已经RL过」的模型上再做大量RL还能拿到巨大增益，本身就挑战了「后训练存在天花板」的流行说法，也说明RL的可挖掘空间远比此前认为的更大。

文章的余下部分讲清了这背后的四块工作：保熵与稳定训练、多集群与容错、数据质量、以及面向长周期任务的自压缩。

## 保留熵并稳定训练

长周期异步RL最头疼的问题之一，是**训练策略与采样策略不一致**带来的KL散度不匹配。朴素做法是重要性采样加量化感知训练，但到了更大规模，这还不够。

更隐蔽的杀手是**熵崩塌**：强模型在几百步内就停止探索，奖励曲线早早走平。Cognition发现，根因之一是softmax会把极低概率的token越推越尖。数学上，如果一个低概率token导向低奖励，策略梯度更新会放大已主导token的领先优势，让分布持续锐化、熵持续下降。top-p采样从机制上挡住了这些低概率token被采作优化目标，从而延缓熵崩塌。

但问题来了：rollout用top-p子集采样，而训练器对所有token算概率，两者的分布散度会迅速拉大，导致训练几百步后就崩。Cognition的解法是**采样分布回放（sampling distribution replay）**：记录rollout时实际可采样的token集合（kept-set），训练器只对这套mask重新归一化概率。这样一来，训练过程中的熵大致保持恒定，推理-训练散度被限制在有界范围内。

![](img2.png)
<span style="font-size:12px;color:rgb(153,153,153);">训练-推理不匹配随训练步数的变化，引入top-p回放后被控制在有界范围</span>

还有个附带收益：概率高于top-p阈值的token，其kept-set大小为1，重归一化后概率恒为1，梯度被直接清零。模型采样的大量token其实都高于标准阈值，于是这些token被排除出梯度计算，整体梯度噪声下降，优化得以聚焦于轨迹中真正有学习信号的部分。再叠加Muon优化器和消除训练器的非确定性操作，训练稳定性进一步提升。

## 多集群训练与容错

Cognition想训万亿参数模型，但单一网络fabric上10-100k芯片的大集群是稀缺资源。RL的结构却天然适合「分而治之」：只有训练器必须待在单一高带宽集群，生成rollout的推理引擎是自包含的，只要有当前权重就能跑在任何地方。

于是他们的RL训练横跨**三大洲四个数据中心**，把自有GPU和Fireworks等推理提供商的算力拼到一起。核心难题是权重同步：每K个梯度步，训练器算出当前权重相对前一版的压缩delta（体积缩减超过99%），写入对象存储作为唯一真相源；各集群的权重控制器轮询到新manifest后，下载分片并经树状广播在本地复制。推理引擎先在服务途中把delta预取到CPU内存，等完全就位才短暂停几秒原地应用，在途轨迹凭借KV cache完好地直接续跑。结果是1T参数模型的跨大陆权重同步端到端1-2分钟完成。

容错按故障位置分开处理。推理引擎死了只丢在途会话，因为Dynamo记录着每个沙盒的进出token，能重路由到健康worker并从对象存储的最新checkpoint重放delta。训练器是唯一的单点，所以每个节点每步异步checkpoint到本地并复制分片给peers，死节点秒级从副本重建；容量不足时按整个数据并行副本收缩、节点回来再增长，rollout流水线全程不冷。

## 面向长周期任务的自压缩

Devin从一开始就是为异步长周期任务设计的，SWE-1.7直接在Devin的harness里训练，自然要处理两个矛盾：rollout会冲出原始上下文窗口，而RL又容易让回复越写越长。

解法是**自压缩（self-compaction）**：Agent逼近上下文上限时，被要求总结自己的工作状态，再从这份自撰摘要续跑。训练中模型同时学会写更紧凑的摘要、以及更好地基于摘要工作。这项技术最早在Kevin（kernel优化）里验证过，到SWE-1.7训练时，单个rollout最长可以跑到六小时。

配合它的是**交替长度惩罚**：无约束阶段只优化任务成功率；预算阶段则惩罚超出加权成本（token、轮次、工具调用耗时）的解。结果是能力范围内的任务回复被压缩，困难任务上的长周期探索得以保留。

![](img3.png)
<span style="font-size:12px;color:rgb(153,153,153);">交替长度惩罚下模型回复长度的变化趋势</span>

## 数据质量

数据是模型学到什么能力的根本决定因素。Cognition在三条线上发力：

验证器质量。验证器会双向出错，要么接受错解（假阳性），要么拒掉正解（假阴性），他们用大量QA流水线把两类都压到最低。

难度校准。模型总是做对或总是做错的任务，学不到东西。他们只保留模型以很低比例解出的任务，既产生真实学习信号，又推高智能上限。

反作弊。沙盒做网络隔离、剥掉git历史和参考制品，评分路径与Agent隔离，再用程序化检查抓已知exploit签名；任何出现作弊尝试的轨迹，无论成败一律记0分。

## 结果：模型行为

大量RL让SWE-1.7的行为明显区别于基座Kimi K2.7 Code。首先是对齐和可信度更高（配套博客专文讨论）。其次是**浓缩的思维链**：第一段CoT的功能词比例更低，平均每句词数几乎减半，这被归因于交替长度惩罚里的预算阶段。

更显眼的是**行动前的彻底探索**。SWE-1.7在动手前会大量读文件、跑搜索、调工具，bug修复时不仅看表面症状，更愿意追根因、考虑边界情况和对抗输入。遇到歧义语义，它倾向于写个小脚本实测，而不是直接猜。

![](img4.png)
<span style="font-size:12px;color:rgb(153,153,153);">FrontierCode 1.1 Main上的行为倾向对比（SWE-1.7 vs Kimi-K2.7-Code）</span>

![](img5.png)
<span style="font-size:12px;color:rgb(153,153,153);">思维链探究边界情况与隐藏需求的频率对比</span>

Cognition认为，这些行为直接来自剔除假阳性和假阴性带来的端到端完整求解压力，并最终转化为基准上的更高分数。代价也有：想得越多、改得越多，SWE-1.7会比任务表面所需触碰更多文件、补更多测试，这与业界「推理越强、改动范围越大」的普遍趋势一致，也是他们计划继续优化的方向。

<div style="background:#f5f0eb;padding:14px 16px 10px 16px;border-radius:6px;margin-bottom:16px;">
<div style="text-align:center;margin-bottom:8px;">
<strong style="font-size:15px;color:#8b6f4c;">结语</strong>
</div>
<div style="font-size:14px;color:#3f3f3f;line-height:1.75;">
SWE-1.7最值得记的不是某个分数，而是它证明「在已充分RL的基座上继续RL仍有巨大空间」这件事本身，这直接松动了后训练天花板的预设。<br><br>
多集群+对象存储同步权重的思路，把RL从「必须抢到单一大集群」的约束里解放出来，对算力受限的小团队是更现实的可扩展路径。<br><br>
自压缩让rollout跑到6小时，意味着长周期Agent训练的工程边界又被推远了一截，而不只是调参技巧的堆砌。<br><br>
推理越强的模型改动范围越大的副作用，说明「省token」和「做对题」之间仍有张力，这会是下一代训练要正面解决的课题。
</div>
</div>

---

<span style="font-size:12px;color:#888888;font-family:'Courier New',monospace;">参考：https://cognition.com/blog/swe-1-7</span>
