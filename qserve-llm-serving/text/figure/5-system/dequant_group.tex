\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figure/5-system/dequant_group_backup.pdf}
    \caption{Our progressive quantization algorithm ensures that all intermediate results in the \textbf{subtraction after multiplication} computation order will not overflow, thereby enabling \textbf{register-level parallelism} and reducing main loop overhead.}
    \label{fig:sys:dequant_group}
    \vspace{-30pt}
\end{figure}
