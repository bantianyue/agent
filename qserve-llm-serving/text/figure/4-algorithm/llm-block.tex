
\begin{figure}[t]
    \centering
    \includegraphics[width=0.85\linewidth]{figure/4-algorithm/llm-block.pdf}
    \caption{\textbf{\system's precision mapping} for an \texttt{FP16} in, \texttt{FP16} out LLM block. All GEMM operators take in \textbf{\texttt{W4A8}} inputs and produce \texttt{FP16} outputs. Activation quantization happens in normalization and activation layers.}
    \label{fig:system:overview}
    \vspace{-10pt}
\end{figure}
