
\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figure/2-background/gemm-flow.pdf}
    \caption{\textbf{Illustration of $m\times n\times k$ GPU GEMM}: $m, n$ are parallel dimensions and the reduction dimension $k$ has a sequential main loop. In LLM serving, $m$ is small and $n, k$ are large. Thus, the main loop is long.}
    \label{fig:background:gemm}
    \vspace{-20pt}
\end{figure}
