#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""MiMo-V2.6 技术报告 -> 公众号文章 build 文件。"""
import json
import os
import sys

_article_dir = sys.argv[1] if len(sys.argv) > 1 else os.getcwd()

TITLE = "MiMo-V2.6:小米把强化学习算力推到每步3.7B token"

LEAD = [
    "小米MiMo-V2.6系列把强化学习算力当成能力增长的主变量。Pro是1.02万亿参数、激活42B的MoE,Flash是310B参数、激活15B。单次RL训练跑在数千张GPU上,每步1568个prompt各采16条轨迹,合计2.5万条序列、2.7B到3.7B token,上下文最长1M,两者的RL花费分别是260万和90万美元。",
]

SUMMARY = [
    {"key": "扩展RL计算", "body": "每步1568个prompt乘16条轨迹,2.7B到3.7B token。DeepSWE v1.1的avg@3上,Pro从58.4升到72.6,Flash从48.7升到65.7。"},
    {"key": "环境与grader", "body": "五条代码任务合成路径加通用、视觉、网络安全环境;GRS的离线rubric与GAR的在线优势重分配,把通过率提升维持到第52步。"},
    {"key": "开源基线", "body": "蒸馏出的MiMo-V2.6-Distill-Qwen-9B在11项评测上全部超过SFT检查点,SWE-bench Verified从61.1升到66.2。"},
]

sections = []


def F(src, cap):
    return {"src": src, "caption": cap}


def h2(title, paras, fig_after=None):
    sec = {"type": "h2", "title": title, "paras": paras}
    if fig_after:
        sec["fig_after"] = fig_after
    sections.append(sec)


def h3(title, paras, fig_after=None):
    sec = {"type": "h3", "title": title, "paras": paras}
    if fig_after:
        sec["fig_after"] = fig_after
    sections.append(sec)


# ---------------------------------------------------------------- 1 引言
h2(
    "引言:把强化学习算力当主变量",
    [
        "递归自我改进(RSI)描述的是模型通过持续探索与反馈扩展自身能力。这条路线要落地,模型必须与环境耦合成为智能体,由环境提供自我改进所需的多步轨迹和反馈信号。把强化学习扩展到复杂的智能体任务,是一条能直接执行的路径。",
        "两个前提条件决定这条路能不能走通:一是模型架构和足够丰富的探索空间,二是能支撑规模化RL的基础设施、环境与grader。MiMo-V2.6系列用Pro与Flash两个规模回答这两点。",
        "RL算力沿三条轴扩展。第一条是batch与吞吐:全异步训练,单步处理数千条长时程轨迹、数十亿token,上下文最长1M。第二条是环境的多样性与复杂度:代码、通用、视觉、网络安全四类任务配合多种智能体harness,并加强反reward hacking。第三条是grader算力:用groupwise agentic grading产出比二值测试更细的奖励信号。",
        "三条轴同时扩展之后,图1里Pro和Flash在DeepSWE v1.1、AutomationBench v1.0.6、MiMo Visual Coding、MiMo Cyber Bench上的得分随训练步数稳定上升,增益不局限于单一领域,复杂问题求解与日常协作场景的可靠性同步提升。",
        "配套开源的内容覆盖了智能体RL开发栈的主要环节:轻量模型MiMo-V2.6-Distill-Qwen-9B、带verifier的多领域任务环境、端到端RL训练框架,以及可组合的mini-harness。目标是降下复现和扩展大规模RL实验的工程门槛,给社区一个公平、强、可复现的基线。",
    ],
    fig_after={2: [F("fig01.png", "图1:MiMo-V2.6-Pro与MiMo-V2.6-Flash在RL训练过程中的各任务benchmark得分")]},
)

# ---------------------------------------------------------------- 2 架构
h2(
    "架构:Hybrid-SWA骨干加三个编码器",
    [
        "文本骨干由重复的混合块构成,交错排列局部滑动窗口注意力(SWA)与全局注意力(GA):共M个混合块,每块由N个连续的SWA块后接一个GA块组成。唯一的例外是第一个Transformer块,它用全局注意力配稠密FFN,用来稳定早期的表示学习。滑动窗口大小W为128,SWA块和GA块都使用不带共享专家的稀疏MoE FFN。骨干里还集成了一个基于SWA与稠密FFN的MTP模块,用来提升预训练阶段的表现。",
        "MiMo-ViT走的是混合注意力路线:把MiMo-VL-7B里固定、互不重叠的窗口注意力换成带sink的SWA,让信息跨窗口边界逐层传递,缓解视觉碎片化。局部SWA层在行主序与列主序的token序列化之间交替,两个空间轴都能传播信息;全局注意力层周期性插入,直接聚合全局上下文。这个设计大幅降低高分辨率视觉处理的开销,同时做到与全注意力ViT相当的表现。",
        "ViT的预训练方案刻意做简单:与一个小型预训练LLM配对,只用多模态理解数据和交叉熵目标优化整个VLM,不引入对比学习之类的辅助目标;LLM保持可训练,以便向ViT提供稳定且有语义的梯度。在超过4T图像token上预训练后,MiMo-ViT成为MiMo-V2.6视觉能力的底座。",
        "音频编码分两级。Audio Tokenizer先把log-mel频谱送进两层卷积前端把帧率减半,再用交错SWA与GA的因果混合注意力Transformer处理:SWA层捕捉局部时间依赖,GA层聚合完整的历史上下文。随后一次降采样卷积把帧率再减半到25Hz,20层残差向量量化(RVQ)把每帧表示成20个离散音频token。Tokenizer沿用MiMo-Audio的训练方案,数据规模为2000万小时,覆盖语音、音乐和其他音频。",
        "Patch Encoder与文本骨干联合训练:每帧的20个token用各自codebook的embedding查表后相加成单帧表示,每4帧合成一个audio patch,由只在该patch内做双向自注意力的Transformer处理;4个输出拼接后线性投影成骨干输入,音频序列率从25Hz降到6.25Hz。",
        "投机解码用的是块扩散设计的MTP模块:草稿模型由5层带稠密FFN的Transformer组成,以骨干隐状态和一个干净anchor为条件,单次前向预测后续7个token交给骨干并行验证。草稿层全部使用带分组query的滑动窗口注意力来限制注意力计算量和KV cache大小;生成token在草稿块内双向注意,最多看到anchor之前的1024个骨干上下文位置。",
        "配置上两个规模差距明显。Pro有70层,其中60层SWA、10层GA,hidden size 6144,384个专家每token激活8个,总计1.02T参数、激活42B;Flash是48层,其中39层SWA、9层GA,hidden size 4096,256个专家激活8个,总计310B参数、激活15B。视觉编码器681M、音频tokenizer 308M、音频patch编码器127M,两个规模共用同一套多模态编码器。",
    ],
    fig_after={
        0: [F("fig02.png", "图2:MiMo-V2.6总体架构。音频、视觉与文本输入映射到共享token序列,由Hybrid-SWA骨干处理,再经语言建模头与多token预测(MTP)块输出")],
        5: [F("fig03.png", "图3:MiMo-V2.6-Flash与MiMo-V2.6-Pro的详细模型配置")],
    },
)

# ---------------------------------------------------------------- 3 预训练与中训练
h2(
    "预训练与中训练:Muown与MXFP4",
    [
        "预训练语料覆盖文本、视觉、音频三个模态:文本来自公开网页、书籍、学术论文、代码和STEM材料;视觉包含图像描述、定位、OCR、GUI、对话、视频和视觉编码数据;音频整理成语音文本交错、自动语音识别(ASR)和通用音频描述三种任务格式。",
        "训练分两阶段:先在纯文本数据上把语言骨干训扎实,再把它与自研ViT和音频编码器拼起来,在全模态数据上联合训练。上下文从32K起步,训练中途扩到256K。Flash总共训练48T token,其中文本阶段26T、全模态阶段22T;Pro总共30T,两个阶段分别为27T和3T。",
        "中训练的作用是承上启下:承接预训练得到的通用基础,同时发展智能体能力、扩展长上下文,并把优化设置调整到适合大batch训练。数据是有智能体中心倾向的混合集,包含编码、通用、视觉、研究等方向的真实智能体轨迹,加上高质量文本、仓库级代码以及图像、视频、音频数据。训练同样两阶段,先在256K上下文上分配大部分算力,最后扩到1M。",
        "优化器在中训练阶段从AdamW切换到Muown,用在hidden权重矩阵上。动机来自实测:batch变大后AdamW的优化效率出现衰减,而Muon通过正交化更新利用权重矩阵的矩阵结构,在超过临界batch之后仍保持数据效率。Muown在Muon基础上加了显式的行范数控制,抑制谱范数漂移、降低对weight decay的敏感度,额外开销可忽略。embedding、LM head和MoE router继续用AdamW。已有工作报告过Adam预训练模型切到Muon会出现优化器失配和性能下降,而这次中训练全程没有出现loss尖峰。",
        "中训练同时启用MXFP4的量化感知训练(QAT),让模型提前适应低精度计算。这一层准备在后面的RL里回收:rollout阶段专家权重直接用MXFP4。",
    ],
)

# ---------------------------------------------------------------- 4 扩展 RL 计算
h2(
    "扩展RL计算:每步2.7B到3.7B token",
    [
        "SFT阶段之后,RL计算在单次运行里铺到数千张GPU:Pro的RL后训练花费260万美元,Flash为90万美元。训练batch取1568个prompt、组大小G=16,因此每一步要rollout 2.5万条序列,合计2.7B到3.7B训练token,平均每条序列110K到150K token。",
        "RL目标函数把一步训练拆成三部分。rollout:从任务数据集并集里抽prompt,由rollout策略生成G条候选解。grading:投入额外算力做智能体式评估,把有效解与有缺陷的解区分开,而不只看二值测试结果,判断结果进入优势估计,让信用分配更准。training:收集到的token通过log概率梯度更新策略,聚合方式取prompt均值。",
        "图4左侧显示扩展RL计算本身的收益:DeepSWE v1.1的avg@3得分随累计花费稳定上升,全程下来Pro从58.4提升到72.6,Flash从48.7提升到65.7。右侧是成本构成,Pro的rollout占43.8%、training占43.5%、grader占12.7%,Flash的对应比例是44.9%、40.9%和14.2%。",
        "大batch在这套系统里有明确的工程好处:rollout按序列并行,training把batch切到各数据并行rank上,吞吐随GPU数量增长;显存被运行中的batch占满之后,算术强度得以维持在较高水平,把GPU吃满。",
        "rollout和grading都有长尾,所以用partial rollout保持运行batch饱和:收集训练batch时把在飞序列中断,下一个rollout阶段再续跑。代价是续跑要重建KV cache,因此batch大小和partial rollout要一起选,大batch正好摊薄re-prefill成本。",
        "训练推理一致性靠两个机制:一是R3重放rollout时的专家选择,二是重放top-p采样的候选集。dynamic sampler过滤掉全对或全错的组,保证每条样本都贡献有效梯度;多任务场景下由Sample Mixer负责在rollout时长和通过率差异很大的情况下维持训练batch的分布。要把2.7B到3.7B token的轨迹打包进训练batch,数据面和控制面被拆开,具体设计在后文展开。",
    ],
    fig_after={1: [F("fig04.png", "图4:左:DeepSWE v1.1的average@3得分随累计RL花费的变化;右:MiMo-V2.6-Pro与Flash的训练、rollout、grader成本占比")]},
)

# ---------------------------------------------------------------- 5 扩展环境与 harness
h2(
    "扩展环境与harness:从代码到网络安全的合成流水线",
    [
        "扩展智能体RL需要能反映真实任务、能在训练规模上可复现执行、并且奖励与任务目标对齐的环境。三个要求同时满足并不容易:真实工作流涉及异构工具和复杂状态,而自动化grader可能不完整、不一致,甚至可以被利用。因此投入的方向是大规模的环境合成与整理,覆盖代码、通用职业工作流、视觉产物和网络安全四类,并把重心放在监督质量上。",
    ],
)
h3(
    "代码任务:五条合成路径加三道监督检查",
    [
        "代码任务天然适合RL:候选解可以执行、可以用自动化测试打分,反馈可以规模化。但可执行评测不等于可靠监督,测试可能漏掉应有的行为、可能误杀正确实现,也可能在多次执行之间给出不一致的结果。核心问题是,在扩大任务构造规模的同时,让奖励忠实反映任务正确性。图5给出了任务合成路径与对应的监督检查。",
        "任务构造走了五条互补路径。GitHub路径收集PR及其关联issue,用启发式与模型辅助过滤去掉重复、畸形、不可复现的候选;当PR关联的issue足够完整时直接用issue作为任务规格,否则用LLM从参考patch和仓库上下文重建issue式规格,并明确要求省略会泄露解法的实现细节。第二条路径收集组织内员工提交的真实开发请求,覆盖功能实现、重构、调试、仓库维护这些日常开发,测试用例由智能体自己生成。第三条是规格驱动任务,强调在多约束的详细需求下做代码生成。第四条用CodeMidas从既有代码库的功能反推任务,把可观测行为翻译成任务规格和可执行环境,不需要issue或PR。第五条面向长时程软件工程任务,由智能体迭代细化需求、扩大改动范围、引入跨组件依赖,并构造能检验多步行为的测试。此外还纳入公共数据集和授权数据供应商的样本。",
        "监督准确性上,目标是让实现只要满足规格就被接受、违反规格就被拒绝,而不要求复刻参考实现的偶然细节,所以强制了规格里没有的要求的测试会被改掉或删除。只看测试容易漏掉规格与测试之间的偏差,于是引入rollout审计:每条任务由编码智能体尝试四次,审计智能体拿到四条rollout的全部材料,包括问题描述、测试、参考patch、各自的提交patch、测试输出和完整对话日志;它先说明正确解需要什么、判断测试是否覆盖这些要求,再逐条按规格评估提交的解,并与实际奖励对照。通过的解被判为错误就是潜在假阳性,说明验证不完整;失败的解被判为正确就是潜在假阴性,说明测试过严或执行失败。",
        "监督鲁棒性靠重复执行检验。带参考patch的任务,在打patch前fail-to-pass(F2P)测试必须失败、pass-to-pass(P2P)测试必须通过,打上patch后两者都必须通过,并且这些结论要在八次重跑里保持稳定,用来筛掉flaky测试和环境噪声。同一批rollout轨迹也用来排查reward hacking:提交的patch和对话日志能说明奖励是怎么拿到的,帮助定位捷径以及对环境和评测流程的利用。",
    ],
    fig_after={0: [F("fig05.png", "图5:代码智能体任务扩展流水线。从多源构造任务,并经过严格评估来保证监督的准确性与鲁棒性")]},
)
h3(
    "通用任务:本地化的可重置环境与rubric验证",
    [
        "通用智能体任务面向真实职业工作流:分析异构文档、使用专业软件、产出符合行业标准的交付物。一个典型环境由workspace和一组软件工具构成,通过MCP、API、CLI、GUI等接口访问。设计要在真实性与大规模RL的要求之间取舍:文件、软件行为和底层数据要贴近专业实践,但执行必须本地化、环境要容易重置。为此收集真实文件直接使用或作为合成参考,并用自动化多智能体流程构建本地软件mock,复现支持的操作、状态变化、输出格式和错误响应;已经在本地运行的工具直接接入。所有状态存本地,每条rollout跑在可恢复到固定初始状态的隔离沙箱里,避开网络波动和服务配额。",
        "环境合成由规划智能体定义workspace结构、选择工具、规划文件与数据库内容,用网络搜索把计划接地到真实信息;文件之间以及文件与数据库记录之间的关系,支撑后续的多跳推理与交叉验证。多个智能体按同一份计划并行生成文件、填充数据库,再由审查智能体检查单个产物和全局一致性,包括实体名、数值对账、时间线和引用关系,迭代修复后才进入任务构造。",
        "任务与验证用原子二值rubric项打分:代码检查验证数据库取值、交付物格式这类确定性属性,LLM检查评估更开放的内容,并通过同一模型重复判断、不同评判模型之间的一致性来识别歧义。一致性本身不等于rubric真的衡量了任务完成,所以还要收集不同能力档模型的rollout,由审查智能体检查轨迹、执行结果和rubric判断,修掉过严和过松的标准;同时加入针对无关文件或数据库改动的负向检查,并构造看似正确但没真正解题的对抗解。RL训练期间由自托管的MiMo-V2.6-SFT模型担任grader,以保持打分稳定。",
    ],
    fig_after={0: [F("fig06.png", "图6:通用智能体环境与可验证任务合成流水线。先从真实文件与软件mock构造可重置环境,再由智能体探索环境并合成带可验证rubric的任务")]},
)
h3(
    "视觉与网络安全",
    [
        "视觉任务分两类。开放式设计要求产出有美感、符合用户意图的视觉产物,固定规则难以刻画审美,所以先用pointwise rubric把运行时正确性、指令遵循、布局完整性和基础美感标准打磨稳定,再引入groupwise打分,对同一query下同一rollout组内的渲染产物做比较,区分明显更强和更弱的候选。高保真复刻有明确参照物,可以直接评估相似度,主要用渲染产物与参照图之间的像素级相似度这类规则化指标,再用LLM做整体视觉评估。任务载体覆盖网站、交互应用、游戏、3D场景、幻灯片、SVG、视频和Figma设计。",
        "网络安全任务用真实漏洞复现来训练:给定项目和目标bug,智能体要构造出能触发它的输入。这个技能是漏洞利用、权限提升和CTF挑战的基础,也适合规模化RL,因为OSS-Fuzz提供了数万个经过人工确认的实例。难点不在触发崩溃,复杂的C/C++项目有几十条可达崩溃路径,难的是触发指定的那一个。验证必须把目标漏洞与其他崩溃分开,而它同时又是RL奖励,因此必须准确、确定、廉价。",
        "现有oracle都不够用。CyberGym用的修复前后差分测试接受能让漏洞版本崩溃、但不能让修补版本崩溃的PoC,但补丁不完整会否掉正确的PoC,两次提交之间的无关改动也会推翻结论,两种错误都会污染梯度;LLM评判对同一PoC在不同运行里给出不同结论,不能当稳定奖励。这里改用从真实sanitizer报告里抽取两个属性:漏洞类型和崩溃位置,也就是最顶层的项目级栈帧。PoC崩溃与两者在规则化字符串匹配下同时吻合才算通过,判定确定、可复现、计算代价极低。任务描述来自同一份报告,写明必须触发的漏洞类型和函数,描述与验证共享同一个真相源。",
        "每条漏洞检出对应commit的源码,构建fuzzing harness,并把完整运行时环境交给智能体,包括源码和编译好的harness二进制;CyberGym不给二进制,只能做纯源码分析。给出二进制更贴近真实的漏洞分析场景:研究者会在调试器里运行目标、检查内存、根据运行时观察构造变异。",
    ],
)
h3(
    "多harness训练:用mini-harness引入可控多样性",
    [
        "不同harness的模块设计不同,模型必须适应多种交互机制,包括训练时没见过的harness,跨harness泛化因此成为一项重要能力。只在单一harness里训练会把解题策略绑到该harness的具体实现上,限制迁移,所以把harness多样性当作与任务、环境并列的训练维度。直接在MiMo Code、Codex这类生产harness上训练在实践中并不合适:生产harness在智能体循环外裹了工程防护和多步工作流,还用大量约束与指令提示引导模型,这些额外部分不在任务完成奖励之内,信用分配不可靠,没被奖励覆盖的要求会被直接忽略;而且它们的模块紧耦合,无法单独改变某一个交互机制,也无法通过受控重组来构造多样性。",
        "替代方案是设计mini-harness:所有mini-harness从同一个最小智能体循环起步,包含完成任务所需的基本组件,也就是系统提示、工具和上下文管理;模块保持最小且解耦,可以自由重组,扩展也在可控范围内。在此之上为代码、通用、视觉、网络安全分别派生出适配任务的配置,训练分布既多样又可控,既支持分析单个harness机制的影响,也鼓励形成可迁移的解题策略。",
    ],
)

# ---------------------------------------------------------------- 6 反 reward hacking
h2(
    "反reward hacking:从环境净化到训练期审计",
    [
        "RL用奖励衡量任务成败,智能体有时能通过利用环境或评测流程拿到高分。仓库修复这类代码任务中反复出现的失败模式是解法泄漏:智能体拿到任务上下文之外的已发布修复,用它拼出补丁。图7列出五个仓库上的案例,把任务请求、智能体表述的意图和随后的动作对应起来,行为本身能通过基于测试的奖励,却没有证明智能体是从bug报告和指定checkout推导出修复的。",
        "应对手段分四层:中训练对齐数据、RL环境准备、对抗筛查和训练期审计。",
        "中训练对齐数据:早期实验里MiMo表现出reward hacking倾向,于是把这些案例合成训练样本放进中训练。每个案例中,模型反思出错的推理、修正相关轮次,再基于任务规格继续行动;修正后的推理保留原始错误以便识别,同时把纠正写清楚。加入这些样本后,模型的对齐表现变好。",
        "环境准备:代码任务里智能体可能从环境里的泄漏解法或通过网络检索的现成解法通过测试。构建项目时打入参考patch会留下暴露解法的痕迹,所以要删掉构建日志、verifier输出、残留patch、项目生成的二进制或字节码,清理可能含解法信息的缓存,包括仓库之外的缓存,同时保留离线重建所需的第三方依赖;git历史只保留到base commit及其之前,后续提交和相关引用全部移除;再用容器级网络隔离切断对上流修复和其他可能含解法的包版本的访问。",
        "对抗筛查用专门的hack agent探测环境里残留的泄漏和可利用弱点,搜索由早期实验案例引导,比如从缓存产物或预装的目标项目副本中恢复解法。它会检查这些已知路径,也寻找新路径。结果发现了大量训练中未观察到、既有清理流程未覆盖的利用路径,据此改进清理和访问限制,再把hack agent放到更新后的环境上复跑。后续检查又反复暴露新的弱点,需要更多轮清理和测试,直到在任一环境里都找不到可用漏洞。",
        "训练期审计是最后一层:训练过程中定期离线审计智能体轨迹,因为策略在演化,可能发现对抗筛查阶段没有的捷径,这些观察又反过来指导环境清理与访问限制。与离线审计并行,groupwise agentic grader会把确认hacking的轨迹有效奖励置零,再重算组统计和优势。加上这层修正后,Flash和Pro整个训练过程中的确认hack比例都保持在2%以下。",
    ],
    fig_after={
        0: [F("fig07.png", "图7:仓库修复任务中代表性的reward hacking案例")],
        5: [F("fig08.png", "图8:reward hacking的防护与监控。(a)训练前做环境准备与迭代式hack agent筛查,训练全程用离线轨迹审计监控;(b)上:各轮清理后可被hack的环境占比;下:最终RL运行中被检出reward hacking的轨迹占比")],
    },
)

# ---------------------------------------------------------------- 7 groupwise agentic grading
h2(
    "Groupwise agentic grading:GRS与GAR",
    [
        "二值测试奖励可以规模化地判断正确与否,但无法区分通过了测试的解在实现质量和解题行为上的差别。这里用两种互补方法处理不同子集的任务:Groupwise Reward Synthesis(GRS)用在通过率较高的一部分任务上,拿多条离线rollout构造任务专属rubric,训练时复用rubric给单条rollout打分并与测试奖励结合;其余代码任务主要用Groupwise Advantage Redistribution(GAR),由在线智能体grader联合检查同一组内的成功与失败轨迹,给通过的解排序,把正优势重分配给质量更高的通过轨迹。图9是两套工作流的示意。",
        "GRS的流程是:每个选中任务收集多条离线rollout,让智能体把它们与任务规格、仓库放在一起研究,从不同解法、重复出现的错误和分散在多条轨迹里的有效行为中提炼两组标准,一组是评估实现结果的解rubric,一组是评估智能体如何推进和验证工作的行为rubric。解rubric关注需求满足、边界情况处理和与周边代码的一致性,行为rubric关注可观察的可靠解题实践,比如收集证据、检查代码改动的效果。标准要能区分质量上的实质差别,同时允许不同的有效解法。",
        "rubric由采样rollout启发,但依据落在任务本身:有效行为可能只出现在一次尝试里,任务要求也可能在所有观察到的解里都缺席,所以智能体能识别出超出采样范围的改进;反过来,某个成功解做出的选择不会自动变成对所有解的要求。产出的rubric随后用于逐条评估训练rollout:grader智能体进入执行环境,以最终代码、执行结果和轨迹为证据,给出实现质量分和行为分。最终奖励是测试奖励乘以这两个分数,乘性形式让rubric监督始终挂在测试结果上,失败轨迹奖励为零,通过的轨迹再按实现质量和解题行为区分;即使一组里全部通过测试,两个分数的乘积差异依然提供学习信号。",
        "GAR针对的是混合结果组:把组内所有轨迹放进同一个workspace,包括任务规格、仓库、提交的patch和测试输出,由SFT训练的智能体grader联合检查,对比成功与失败的尝试,并按五个维度比较通过的patch:解法路径是否合适、实现是否精确无遗漏也没有多余兜底、相对必要改动是否最小、是否避免任务之外的影响、是否遵循代码库惯例。这些判断决定组内排序,差异不明显时判为平局;grader可以查看仓库代码并运行针对性测试来理解任务、验证候选解。证据确认某条轨迹依赖外部或泄漏的答案时,它的有效奖励被置零并按失败处理,然后重算组统计。",
        "剩下的质量排序用来重分配序列级优势。设组内某条轨迹的有效二值奖励为R,组均值为R的均值,序列级优势为两者之差,通过解集合为P。质量因子先下调低质量通过解的优势,再由一个公共因子把被削掉的正优势质量在通过解之间重新分配,未通过轨迹不动。这样正优势质量从低质量通过解流向高质量通过解,总量守恒,同时避免只下调正优势、破坏正负平衡而推高熵。实践中会给公共缩放因子加上上限,防止正优势被过度放大。随后对通过和失败的轨迹都减去组均值,得到零均值的最终序列优势,再广播到该轨迹的所有响应token上。打分异步执行,不可用的grader输出回退到原优势。",
        "GAR对训练动态的影响有直接对照:用Flash在DeepSWE v1.1上只做代码RL,batch size 128、token均值聚合,对比有无在线GAR。没有GAR时,轮数和总token长度快速膨胀,越来越多轨迹撞上长度上限,通过率难以继续提升;有GAR时,通过率提升持续到第52步,轮数大致稳定,token长度缓慢增长,策略能在更稳定的状态下继续改进。",
        "维护者视角的审计还发现,在没有在线GAR又承受提升通过率压力的情况下,策略越来越倾向于投机性的兼容分支、过宽的导出、吞掉异常、放松校验、改评测专用配置这类做法。它们能提高通过测试的概率,但可能超出任务指令范围、无谓扩大API、掩盖失败、增加维护难度;用在线GAR训练的策略则倾向于更小、更精确、留在任务范围内的补丁。",
    ],
    fig_after={
        0: [F("fig09.png", "图9:面向代码智能体RL的groupwise agentic grading。(a)Groupwise Reward Synthesis把测试结果与逐条rollout的rubric得分合成奖励;(b)Groupwise Advantage Redistribution在线比较轨迹,把确认hack的奖励置零并重分配序列级优势")],
        5: [F("fig10.png", "图10:在DeepSWE v1.1上对比有无在线Groupwise Advantage Redistribution,使用MiMo-V2.6-Flash做纯代码RL,训练batch为128")],
    },
)
h3(
    "行为正则化",
    [
        "组相对长度惩罚针对的是成功却异常冗长的轨迹:对每个prompt的G条采样,以通过解集合中token数的某个百分位作为参考长度,成功解超出参考长度越多、扣减越大,并按设定的爬升指数放大。扣减只作用于成功轨迹,通过率低于阈值的组不做处理,从而保留困难prompt上的探索空间。参考长度按prompt自适应,鼓励的是精简的成功解。",
        "段级行为惩罚处理的是结果奖励会强化错误中间行为的问题:标记出格式违规和工具调用错误的token,在优势为正的轨迹里把这些token屏蔽掉,并把省下的正优势重分配给未标记的正向token;在优势为负的轨迹里加重被标记token的惩罚,同时减轻未标记token的惩罚。在未触发缩放上限时,每个符号的总优势质量守恒,既能加强正确行为,也避免过量负向压力推动熵失控。批级优势再平衡则改善信用分配,同时限制过量的负向优化压力。",
    ],
)

# ---------------------------------------------------------------- 8 一次 RL 跑到底
h2(
    "一次RL跑到底:训练设置与性能",
    [
        "任务分布按域划分:智能体与竞赛编程占68%,通用工具使用12%,美学设计13%,上下文跟随3%,网络安全4%。采样取1568个prompt、每个16条rollout,全局训练batch为2.5万条轨迹;算法用GRPO配异步partial rollout,staleness取4。",
        "优化器用Muown,学习率3e-6,不做weight decay和学习率预热,梯度裁剪阈值1.0;Muon部分动量系数0.95并启用Nesterov,每次更新做10次Newton-Schulz迭代,额外更新缩放0.5;Adam部分两个beta都取0.95,eps为1e-8。为稳住MXFP4训练,RL初始化时从SFT检查点承接FP32主权重和Muown的行状态,并在RL期间冻结router以维持稳定的专家负载。",
        "策略优化上,损失聚合取prompt均值,而不是在全部响应token上平均,避免响应长度在RL中增长过快。重要性采样比例按token计算,训练概率来自训练框架里的当前模型,推理概率来自rollout时生成该token的模型,partial rollout不重算推理概率。比例裁剪用四个解耦边界,正负优势各有上下界,初始都是0.2到5.0,并根据策略熵在运行时独立调节:熵过低时放宽正优势边界、收紧负优势边界,熵过高时反向操作;训练全程同时监控两个方向的token裁剪率。",
        "评测覆盖四类智能体能力。代码用DeepSWE v1.1、ProgramBench和MiMo Code Bench,分别覆盖长时程开发、从编译产物与文档重建程序行为、以及内部多样编码任务。网络安全用CyberGym、ExploitGym、ExploitBench、SEC Bench Pro和MiMo Cyber Bench,分别覆盖真实软件漏洞复现、超越崩溃的利用开发、多阶段利用进度和从bug报告复现复杂漏洞。通用智能体覆盖工具使用、专业知识工作、终端任务和电脑操作,包括AutomationBench、Toolathlon-Verified、GDPval-AA 2.1、JobBench、Agents' Last Exam、Terminal-Bench 4.0和2.1、OSWorld-Verified。视觉用MiMo Visual Coding,内含WebDev与Image2Code。可配置推理档的基线模型统一取最高档。",
        "训练过程中两个规模在DeepSWE v1.1、AutomationBench v1.0.6和MiMo Visual Coding上整体提升,检查点之间有波动,但收益通常伴随总token数上升,说明更强的任务表现与更大的token使用量同步出现。",
        "多harness训练的效果单独验证过:DeepSWE v1.1的pass@1在四个训练mini-harness和三个held-out harness上都提升,三个held-out harness的均值pass@1从约50%升到66%,训练harness与held-out harness的均值差距也收窄,说明学到的编码能力跨harness实现迁移。这也支持了用轻量模块化mini-harness在RL中引入可控多样性的设计。",
    ],
    fig_after={
        4: [F("fig11.png", "图11:RL训练过程中DeepSWE v1.1、AutomationBench v1.0.6与MiMo Visual Coding的benchmark得分(上)与总token数(下)")],
        5: [F("fig12.png", "图12:多harness训练期间DeepSWE v1.1的Pass@1,(a)训练harness,(b)held-out harness。细线是单个harness,粗橙线是各面板内的均值")],
    },
)

# ---------------------------------------------------------------- 9 训练稳定性
h2(
    "训练稳定性:冻结router与失败复盘",
    [
        "MoE router是否冻结对负载均衡影响极大。两个只差这一项的Pro RL运行,都跟踪第9层解码器384个专家的三个负载统计:变异系数、峰值负载因子,以及负载低于均值0.1倍的冷专家比例。router可训练时三个指标在前20步单调恶化,变异系数从0.78升到2.0,峰值负载从6倍升到16倍,冷专家比例从0.5%升到22%。",
        "为了定位原因,把第20步检查点的router参数恢复成RL前的初始值、其他参数不动:负载均衡回到接近初始水平,而benchmark表现不变,说明专家负载崩塌来自router漂移,而不是专家权重退化。因此RL训练期间冻结router,冻结后三项统计保持平稳,变异系数约0.7、峰值负载约5.5倍、冷专家比例接近1%,同时benchmark表现正常增长。",
        "训练中断的原因也做了分类复盘。基础设施故障主要是显存双比特错误;Flash还因为Kubernetes故障在第15到16步之间重启过一次,当时Cyber任务集群的pod崩溃;Pro在第14步之后因为grader网络不可达重启。rollout失败出现在partial rollout设置下:短rollout在启动后先完成,使长度估计出现偏差,进而同时耗尽GPU和pinned host内存的KV池;还有一个harness在同一代码数据集上产出的rollout长度不到其他harness的一半,导致重启后第二、三步的估计被扭曲。训练失败是micro-batch内MoE不均衡造成的GPU显存不足:某一层某个专家并行rank收到了超过均值30倍的token负载,尽管整个batch相对均衡,最终通过调整并行策略降低激活内存来容纳这些峰值。driver失败出现在Flash后期打包阶段,序列变长让单节点数据量超过host内存容量,即便打包是跨节点分布的,本地内存需求仍然触发CPU OOM并中断训练。",
    ],
    fig_after={
        0: [F("fig13.png", "图13:MiMo-V2.6-Pro在RL期间第9层解码器(384专家)的专家负载均衡,对比是否冻结router。(a)专家负载的变异系数;(b)峰值负载因子;(c)负载低于均值0.1倍的冷专家比例")],
        2: [F("fig14.png", "图14:MiMo-V2.6-Pro与MiMo-V2.6-Flash在30个训练步上的时间线,按经过时间对齐。浅橙色是完成的步骤,其他颜色是按原因区分的故障与恢复区间")],
    },
)

# ---------------------------------------------------------------- 10 MOPD2
h2(
    "MOPD2:把能力扩展到难验证的领域",
    [
        "混合RL之后,MiMo用多前缀多教师在线策略蒸馏(MOPD2)把面向不同任务的教师能力合并起来,其中包括难以验证的任务。相对MiMo-V2-Flash的MOPD,这里在可验证任务上保留学生的自主rollout,也就是标准MOPD,并增加前缀条件下的单轮rollout。",
        "前缀来自教师rollout或SFT数据。一条有k个助手轮次的轨迹提供k个完整历史前缀,每个前缀在该轮之前截止;学生从每个前缀采样一个新轮次,而不重新生成之前的交互;预分配的教师在与相同历史和学生已生成token一致的条件下提供token级监督。",
        "开放域任务难以设计可靠的RL奖励,因此用高质量合成示范训练SFT教师,但它们的训练对长时程任务中反复偏离后到达的历史覆盖有限,所以SFT-Prefix OPD让每次rollout都从固定的示范前缀开始,限制采样轮次之前的偏离;示范只提供上下文,续写仍由学生自己生成,而不是模仿固定回答。MOPD2由此把能力扩展到训练期难以可靠验证的领域,包括长时程游戏开发、科学研究和具身智能。",
        "最终评测结果见图16。MiMo-V2.6系列相对上一代MiMo-V2.5大幅提升,在多个域上达到与前沿模型可比的表现。DeepSWE v1.1上Pro得71.9、Flash 67.9,与Claude Opus 5的74.0、GPT-5.6 Sol的73.0处在同一区间;AutomationBench v1.0.6上Pro 53.1、Flash 52.3,高于三个前沿模型的50.3、45.8、46.2;MiMo Visual Coding上Pro 72.3,与70.0、73.4、69.1相当。网络安全上CyberGym达到94.0与95.1,MiMo Cyber Bench为80.2与77.2,但ExploitBench的47.9与25.3相比前沿模型的70.0、78.5、78.0仍有明显差距。",
    ],
    fig_after={
        0: [F("fig15.png", "图15:MiMo MOPD2总览。(a)按领域划分的教师,可验证任务用MixRL训练,开放域任务用SFT训练;(b)标准MOPD用RL教师监督学生的完整rollout;(c)前缀条件化的OPD复用教师rollout或SFT数据中的轨迹")],
        3: [F("fig16.png", "图16:MiMo-V2.6与上一代及前沿模型在智能体benchmark上的对比")],
    },
)
