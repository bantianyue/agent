<div style="background:#e8f4fd;padding:14px 16px 10px 16px;border-radius:6px;margin-bottom:18px;">
<div style="text-align:center;margin-bottom:10px;">
<strong style="font-size:16px;color:#1a6ba0;">要点速览</strong>
</div>
<div style="font-size:14px;color:#3f3f3f;line-height:1.75;">
- <strong>一个INDEX.md文件就能让Agent找文件速度快10倍</strong>：在主要文件夹的根目录放一份索引文件，Agent先读它再工作，从打开7个文件降到1个<br><br>
- <strong>按关注域而非内容类型组织文件夹</strong>：人类按「文章」「研究」「策略」分类，但Agent需要按「品牌」「编辑策略」「推广」等关注域来组织<br><br>
- <strong>数字编号让阅读顺序明确</strong>：文件夹前加数字编号（01. 02. 03.），Agent不猜顺序，不依赖字母排序<br><br>
- <strong>先测量再动手</strong>：跑3个实际任务计时，找到最耗时的那个文件夹，只修它一个，确认有效再修下一个
</div>
</div>

---

**Hermes Agent作者wandermist花了一天时间计时和分析自己的Agent为什么会「变慢」**。他发现即使使用Opus、GPT 5.5、Qwen和GLM等顶级模型，Agent仍然在一项简单的任务上花了将近两分钟：从文件夹中找到最新的文章简报。问题不在模型，在文件夹结构。

**人类按内容类型组织文件，Agent却需要一个地图。**

大多数人的文件结构是按内容类型组织的：文章放Articles文件夹、研究笔记放Research、策略文档放Strategy。这种结构对人类来说是自然的，因为我们的大脑会自动做交叉引用。但对一个每次都要从头搜索的Agent来说，它不知道哪个草案是当前的、哪个研究笔记是这个月的还是六个月前的。它只能打开一个又一个文件，像在黑暗中摸索。

> 围绕Agent的结构比Agent自身的能力做更多的工作。

作者追踪了优化前的数据：找当前文章简报要打开7个文件、耗时2分钟；找品牌颜色定义打开5个文件、耗时1分12秒；找文章队列打开4个文件、耗时48秒。最夸张的是，同一个启动计划需要3个文件，而这3个文件分别躺在3个不同的文件夹里，Agent做了3次错误尝试才找到正确的。

**一个INDEX.md就是全部修复方案。**

作者的方案极其简单：每个主要文件夹的根目录放一个INDEX.md，列出所有子文件夹和规范文件，并告诉Agent从哪里开始。Agent先读INDEX.md，知道里面有什么再动手。用作者的话说，这就像一个软准入闸门：Agent不能开始工作，直到它知道自己在处理什么。

品牌文件夹优化后的结构是这样的：
- `01. Brand System/`（品牌系统）
- `02. Editorial Strategy/`（编辑策略）
- `03. Promotion/`（推广）
- `04. Content Calendar/`（内容日历）
- `05. Analytics/`（分析）
- `06. Archived/`（归档）

以前按内容类型组织时，品牌工作、推广策略和内容日历分散在不同的顶层文件夹里。现在每个关注域独占一个文件夹，Agent不需要跨边界找东西。

数字编号让阅读顺序明确，不依赖字母排序。作者说文件夹名前面加数字**对他自己来说也更容易记住**。

INDEX.md经过三次迭代。第一版把文件夹里的每个文件都列出来，写了四十行：技术上是完整的，但Agent每次都要解析这么长的索引，反而成了新的开销。第二版又太短，大约十五行，Agent仍然会问没回答到的问题。第三版只列子文件夹和规范文件，加上一小段「Where To Go」告诉Agent它的起始点。

**优化后的数据：从2分钟到10秒。**

当作者问Agent去拉取启动推广策略时，它读 `05.Brand/INDEX.md`，打开 `03.Promotion/01. Promotion Strategy.md`，然后直接开始工作。优化后的计时是：

- 找当前文章简报：1个文件，10秒
- 找品牌颜色定义：3个文件，22秒
- 找文章队列：1个文件，10秒
- 找同类历史文章：2个文件，18秒
- 拉取启动推广策略：1个文件，12秒

最慢的任务从2分钟降到26秒，最快的跑在10秒左右。没有换模型，没有换工具链：只是文件夹结构不再浪费Agent的能力。

**深度是快速查找的敌人。**

作者也踩了几个坑。第一个是在每个子文件夹里都放INDEX.md：地图太多了，Agent花在读索引上的时间比做正事还多。只在有足够子文件夹需要地图的顶层文件夹放INDEX.md，小文件夹（4-5个文件）直接导航，不需要地图。

第二个坑是嵌套结构。在文件夹里套文件夹，把两级变成五级，Agent需要读多个INDEX.md才能到达一个文件。作者把这些额外层级折叠回扁平子文件夹，导航速度立刻恢复。

第三个误解：在Agent表现出困惑之前就搭建结构。大多数人在Agent还没出问题时就过度设计基础设施。作者的原则是：只用解决问题的最小接口。Agent迷路时才加结构，只加够解决那个具体问题的量。

**跑一个五分钟的诊断测试，再动手。**

作者建议在动手重组前先选三个实际任务，计时，记录打开的文件数和错误次数。任何超过30秒或打开3个以上错误文件的任务，背后的文件夹就是问题所在。

只修最常出问题的那一个文件夹。写一份INDEX.md，列出子文件夹和规范文件。保存。再跑同一个任务。如果Agent在30秒内找到正确文件，修复方案有效，把相同模式应用到下一个问题文件夹。如果还是失败，问题可能是缺少编号或一个关注域一个文件夹的规则没遵守：这两个是接下来要修的。

> 从最浪费时间的一个文件夹开始，修好它，准备好了再移动到下一个。

<div style="background:#f5f0eb;padding:14px 16px 10px 16px;border-radius:6px;margin-bottom:16px;">
<div style="text-align:center;margin-bottom:8px;">
<strong style="font-size:15px;color:#8b6f4c;">结语</strong>
</div>
<div style="font-size:14px;color:#3f3f3f;line-height:1.75;">
这篇分享的核心判断很扎人：Agent的瓶颈往往不在Agent本身，而在它周围的人为组织方式。能力很便宜：当围绕它的脚手架坏了的时候。最讽刺的是，这个痛点恰恰说明Agent的推理能力已经足够强了：它不是找不到文件，而是文件太多了，它在认真、系统地阅读每一个候选对象，然后因为路径太混乱而犯错。如果Agent不够聪明，它根本不会浪费时间找东西，它会直接告诉你找不到。
<br><br>
但INDEX.md这个方案也暴露了Agent自身的一个深层缺陷：它不会自动维护自己的知识边界。一个足够好的Agent应该在第一次经历某个文件夹后，自动生成一份索引并记住哪些文件在哪里。把路径管理完全交给用户手工维护，在实践层面意味着每次新增文件夹、每次重命名、每次归档操作，都要同步更新INDEX.md。这不是终点方案，这是从纯搜索模式到启发式导航的第一步。
</div>
</div>

---

<span style="font-size:12px;color:#888888;font-family:'Courier New',monospace;">参考：

https://x.com/wandermist/status/2071930382581195105</span>
