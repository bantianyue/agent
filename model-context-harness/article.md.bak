<div style="background:#e8f4fd;padding:14px 16px 10px 16px;border-radius:6px;margin-bottom:18px;">
<div style="text-align:center;margin-bottom:10px;">
<strong style="font-size:16px;color:#1a6ba0;">要点速览</strong>
</div>
<div style="font-size:14px;color:#3f3f3f;line-height:1.75;">
- <strong>外化是Agent进化的核心逻辑</strong>：从模型权重→上下文窗口→Harness基础设施，能力的载体逐步从模型内部迁移到外部<br><br>
- <strong>三个外化维度</strong>：记忆外化状态（recall→recognition）、技能外化专业知识（generation→composition）、协议外化交互结构（ad hoc→structured）<br><br>
- <strong>Harness是运行时环境</strong>：不是第四种外化，而是编排三个维度的统一层，提供沙箱、审批、可观测性、预算管理<br><br>
- <strong>系统级框架</strong>：解释了为什么Agent进步不只取决于更强的模型，更取决于更好的外部认知基础设施
</div>
</div>

**一篇来自上海交大、中山大学、卡内基梅隆大学等多家机构的综述论文，用"外化（Externalization）"这个统一视角，串联了Agent记忆、技能、协议和Harness工程的全部演进逻辑。**

这篇论文的核心悬念很简单：为什么很多Agent最大的可靠性提升不来自换模型，而来自**给模型搭环境**？

答案藏在"外化"这个词里。

---

### 一、从权重到上下文再到基础设施

论文用一条清晰的历史弧线打开了整个画卷。今天的AGI叙事往往被简化为"模型变大=能力变强"，但作者们指出了一种更有意思的模式：能力的载体在逐步外移。

**第一站：能力在权重中。** 2022-2023年，能力几乎完全等价于模型参数。预训练把知识、推理习惯和统计规律都压进了权重里。优点是快、紧凑、泛化强；缺点是动一个事实就要重训练，审计黑盒，个性化更是无从谈起。

**第二站：能力在上下文中。** 提示工程的时代证明了不碰权重也能大幅改变行为。Few-shot、CoT、ReAct、RAG：这些技术的共同点是：**把"模型知道什么"的考题变成了"模型能识别并运用什么"的考题**。引用Donald Norman的理论：购物清单没有扩大你的记忆容量，它把困难的是回忆问题变成了容易的识别问题。这是外化的雏形。

但上下文也有硬伤：窗口再大也有限、"lost in the middle"效应无处不在、每个新会话就是一次失忆。

**第三站：能力通过基础设施。** 这才是Harness纪元。工程注意力从"我们该告诉模型什么？"彻底转向了"模型应该在一个什么样的环境中运行？"外部记忆存储、工具注册表、MCP协议、沙箱隔离、审批循环：所有这一切构成了一个比模型本身更大、更复杂的系统。

![Figure 2: Community theme evolution](timeline.png)
<span style="font-size:12px;color:rgb(153,153,153);">论文绘制的时间线：从Weights（橙色）经Context（蓝色）到Harness（绿色），社区研究主题的重心在过去几年发生了根本性迁移。</span>

---

### 二、记忆：外化状态，把回忆变成识别

论文对记忆的定义值得留意：记忆不是简单的"存东西"，而是**将状态跨时间外化**。

一个未经外化的Agent，其"记忆"就只是一个上下文窗口里的即时文本。新会话一来，过往的用户偏好、已解决的歧义、领域事实就全部消失。这不是因为模型"记不住"：而是人类把记忆这个认知负担完全压在了模型内部。

论文把外化后的记忆架构分为四个阶段：
- **单体上下文**：所有历史焊在一个提示里
- **带检索存储的上下文**：RAG风格，从外部索引拿相关片段
- **分层记忆与编排**：区分工作上下文、情节经验、语义知识、个性化记忆，各有各的生命周期和存取策略
- **自适应记忆系统**：用MOE、强化学习等手段让记忆策略根据反馈自动调整

有意思的是，论文在Section 3.3专门讨论了"不同Harness时代对记忆的需求"：不是所有Agent都需要同级别的记忆系统。一个做单轮问答的Agent不需要工作记忆；但一个做长期任务执行的工程Agent，没有分层记忆系统就等于没有"持续存在"的能力。

![Figure 4: Memory as externalized state](memory_f.png)
<span style="font-size:12px;color:rgb(153,153,153);">记忆作为外化状态的完整架构：从临时上下文窗口出发，经过提取、整合、遗忘、冷热交换等机制，最终沉淀为四种持久记忆维度。</span>

---

### 三、技能：外化专业知识，把即兴变成组装

如果说记忆解决的是"状态断片"，那技能解决的则是**行为随机**的问题。

论文的观察很精准：未经外化的LLM在每次执行同一个任务时，几乎是从头"即兴发挥"一次。让Agent打开一个仓库、跑测试、提PR：这个过程它可能每次都不一样。人类不会每次写代码都重新发明if-else，但模型每次调用都在重新推导整个工作流。

技能系统就是把这种往复的即兴创作**打包成可重用的过程工件**。

**技能外化的五个环节**：
1. **规格（Specification）**：定义能力、前置条件、使用范围
2. **发现（Discovery）**：通过注册表找到匹配的技能
3. **渐进披露（Progressive Disclosure）**：先看摘要，再深入全文
4. **执行绑定（Execution Binding）**：将技能关联到具体的工具、API、文件
5. **组合（Composition）**：多个技能串联成更大的工作流

一个特别值得说的细节是"渐进披露"。论文指出，技能虽然好，但如果把所有技能的全文都塞进上下文，上下文预算很快就爆了。所以技能系统必须支持**层级式暴露**：Agent先看到一个摘要，了解这个技能是干吗的，再决定是否要看全文。这与Hermes Agent的 `.skill_view(name)` 模式高度吻合。

技能还有一条重要的边界条件：**异味。** 技能会过时、会与当前上下文脱节、组合可能不安全。好Harness要能检测这些状况。

![Figure 5: Skills as externalized expertise](skill_f.png)
<span style="font-size:12px;color:rgb(153,153,153);">技能的完整生命周期：从获取（作者编写/经验蒸馏/环境探索/组合）→量化为三类内容→注册发现→渐进披露→绑定执行。</span>

---

### 四、协议：外化交互，把随意变成契约

记忆和技能主要管的是Agent内部的能力组织。但一个**在真实世界中运行**的Agent必须与外部世界打交道：调用工具、委托子Agent、响应人类指令。

这就引出了第三个外化维度：协议。

论文的核心判断是：**未经协议化的交互是脆弱且不可治理的。** Agent与工具之间的Free-form prompt式调用，在只有一个工具时还能用；一旦有几十个工具、多个子Agent、需要分级权限，就必然崩溃。

论文将Agent协议分成了几类：
- **Agent-工具协议**：Function Calling / MCP：定义工具如何暴露能力，Agent如何调用
- **Agent-Agent协议**：ACP / A2A：子Agent之间的委托与通信
- **Agent-用户协议**：AUP：用户意图捕获、审批、权限声明
- **其他协议**：生命周期管理、会话管理

协议作为认知工具的独特价值在于：它将**隐式的交互期望变成了显式的可检查契约**。写死的API调用在改了endpoint时悄无声息地断了，但一个协议化的接口会明确告诉调用方它有什么能力、需要什么参数、返回什么格式。

![Figure 6: Protocols as externalized interaction](protocol_f.png)
<span style="font-size:12px;color:rgb(153,153,153);">协议的进化轨迹：从孤立的模型调用→硬编码API→标准化协议→去中心化Agent网络。Harness通过三个功能面管理外化交互：交互、感知、协作。</span>

---

### 五、Harness：统一外化层

前三个维度各自解决了一个问题：状态断片、行为随机、交互脆弱。但把它们丢在一起并不会自动和谐：**记忆扩展与技能加载争抢上下文预算，协议标准化约束了能力打包方式，技能执行产生的痕迹又变回了记忆。**

这就是Harness登场的原因。

论文很明确地定位了Harness：**它不是第四种外化，而是承载三个维度的运行时环境。** 它提供编排逻辑、约束条件、可观测性和反馈循环。

论文从六个维度分析Harness设计：

**1. Agent循环与控制流**：循环是同步的还是异步的？有中断、恢复、和超时机制吗？

**2. 沙箱与执行隔离**：Agent的权力边界在哪？文件系统、网络、API的访问控制怎么划分？

**3. 人工监督与审批关口**：哪些操作必须经过人类确认？审批的粒度可以是操作级的，也可以是整条工作流级的。

**4. 可观测性与结构化反馈**：每一次函数调用、每个子Agent的结果、每一步决策的上下文：这些都需要被记录并反馈给系统。

**5. 配置、权限与策略编码**：策略不是写在提示词里的"请小心"，而是声明式的、可检查的、不会随模型版本变化而失效的。

**6. 上下文预算管理**：既然上下文窗口有限，那记忆、技能内容、工具描述之间怎么分这个预算？压缩、选择性加载、遗忘机制：这些都是Harness的责任。

论文还画了一张Harness全景图：Foundation Model在中心，三个外化模块（Memory, Skills, Protocols）和三个运维面（Permission, Control, Observability）围成一圈，箭头在它们之间不断流动。

![Figure 3: Externalization architecture of a harnessed LLM agent](circle.png)
<span style="font-size:12px;color:rgb(153,153,153);">Harness架构全景：Foundation Model居中，三个外化模块（Memor, Skills, Protocols）和三个运维面（Permission, Control, Observability）构成完整的运行时环境。</span>

---

### 六、参数化能力vs. 外化能力

论文的Section 7做了一个关键的权衡分析：模型越强，对外化的依赖越少：这是常识。但真正有价值的发现是：**即使是最强的模型，也从结构化外部基础设施中受益。**

一个反直觉的观察是：模型越聪明，它越能从结构化的外部系统中受益：不是因为离了系统它不行，而是因为它可以更好地利用系统提供的分工。

比如，一个高度智能的模型拿到一份技能文档时，可以更快地理解适用范围并决定是否调用，但有了渐进披露机制，它甚至不需要在上下文中保留全文就能做出判断。有了协议化的工具接口，它不需要猜测API参数：schema就在那。

这指向了一个循环：**更强的模型 + 更好的外化基础设施 = 组合出的可靠性 > 两者之和。**

---

<div style="background:#f5f0eb;padding:14px 16px 10px 16px;border-radius:6px;margin-bottom:16px;">
<div style="text-align:center;margin-bottom:8px;">
<strong style="font-size:15px;color:#8b6f4c;">结语</strong>
</div>
<div style="font-size:14px;color:#3f3f3f;line-height:1.75;">
这篇论文的最大贡献不是提出了什么新系统，而是**给出了一个可以统一解释过去三年Agent进化的框架**。把记忆、技能、协议和Harness工程串联在"外化"这条主线下，你会发现很多零散的努力突然就有了位置：为什么大家从2024年开始疯狂做MCP？为什么每个平台都在推自己的Agent技能系统？为什么"给模型搭环境"的投入越来越大？<br><br>
框架本身也暗示了一种焦虑：如果Agent的可靠性更多来自基础设施而非模型本身，那基础设施的标准化和共享化就必然成为下一阶段的主题。从私人脚手架走向公用基础设施：这可能是这篇论文最有前瞻性的判断。
</div>
</div>

---

<span style="font-size:12px;color:#888888;font-family:'Courier New',monospace;">参考：

https://arxiv.org/html/2604.08224v1</span>
