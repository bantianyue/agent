[LINKREL] /maharshii
[LINKREL] /maharshii
[LINKREL] /maharshii
[LINKREL] /maharshii/article/2100193318277890174/media/2099932595538808832
[p] maharshi@maharshii
[IMG] https://pbs.twimg.com/media/HSR1kdla0AA1L4M?format=jpg&name=900x900
[LINKREL] /maharshii/status/2100193318277890174/analytics
[p] gpu kernel porting with agents4523,567
[P] Few weeks ago, my AI agent ported and then optimized a CUDA C++ attention kernel to CuTeDSL in an afternoon. The resulting kernel was 1.2x faster than the original and 2.8x faster than the cuDNN version. I think a killer usecase for agentic kernel development is porting.
[P] When I say "porting" I mean two things:
[P] Different GPU versions: When you need to convert a kernel that runs on one GPU type to another GPU type, with architecture specific features. let's say converting from H100s (hopper) to B200s (blackwell).
[P] Different abstraction levels: When you need to convert a kernel from one abstraction level to other. Let's say converting from low-level NVIDIA CuTe C++ to a high level DSL (domain specific language) like Triton.
[P] Agentic kernel development seems to be great for both. Let me talk about my experience with porting and then optimizing Sage Attention 3 kernel from CuTe C++ to CuteDSL.
[LINKREL] /maharshii/article/2100193318277890174/media/2099947199681740800
[IMG] https://pbs.twimg.com/media/HSSC2iQbQAA7Lsz?format=jpg&name=900x900
[P] ported vs. other versions
[P] Sage Attention 3
[LINK] https://arxiv.org/pdf/2505.11594
[P] To make the attention operation more efficient on NVIDIA's consumer blackwell GPUs, Sage attention 3 leverages quantization and low-bit tensor cores to accelerate the inference of the GEMMs (matrix multiplications) within the operation. FP4 tensor cores on GPUs like RTX 5090 and 6000 PROs offer much higher performance compared to FP16 tensor cores. So, we must utilize them smartly for the GEMMs within the attention kernel. Here's the paper: https://arxiv.org/pdf/2505.11594
[P] Sage attention 3 also proposes a method to mitigate the severe accuracy loss when values are quantized to FP4 e2m1, since it has only 15 representable values:
[LINKREL] /maharshii/article/2100193318277890174/media/2100160713381863424
[IMG] https://pbs.twimg.com/media/HSVFCqxbYAA_hwu?format=jpg&name=900x900
[P] sage3 method
[P] FP4 attention has two major challenges:
[P] Both per-tensor and per-token quantization approaches are inadequate for preserving model accuracy. So, restricting the quantization group size to 1x16 is needed, which is the NVFP4 format.
[P] The attention map P consists primarily of small values in the range [0, 1]. When directly quantized to FP4, these values force the scaling factors into an extremely narrow dynamic range. Hardware requires the quantization factors to be in FP8 e4m3 data type. This leads to significant accuracy loss when presenting these scale factors in FP8.
[P] For the second challenge, the method proposed is using a two-level quantization approach. Basically, this approach first normalizes each token’s range to [0, 448 x 6] through per-token (i.e. each row) quantization to utilize the range of FP8 e4m3 scaling factors, then applies FP4 microscaling quantization.
[LINKREL] /maharshii/article/2100193318277890174/media/2100163350684717056
[IMG] https://pbs.twimg.com/media/HSVHcLfbUAAm2DV?format=jpg&name=900x900
[P] Analysis of the benefit of two-level quantization
[P] The process
[LINK] https://github.com/thu-ml/SageAttention
[P] The original implementation of Sage attention 3 is written in CUDA C++ which spans around 20 files of templating and NVIDIA's CuTe abstractions. Merely understanding the entire thing would take me days. The code is here: https://github.com/thu-ml/SageAttention
[p] bash
[PRE]
    sageattention3_blackwell ├── README.md ├── sageattn3 │ ├── __init__.py │ ├── api.py │ ├── blackwell │ │ ├── __init__.py │ │ ├── api.cu │ │ ├── block_info.h │ │ ├── blockscaled_layout.h │ │ ├── cute_extension.h │ │ ├── epilogue_tma_ws.h │ │ ├── kernel_traits.h │ │ ├── kernel_ws.h │ │ ├── launch.h │ │ ├── mainloop_tma_ws.h │ │ ├── named_barrier.h │ │ ├── params.h │ │ ├── softmax_fused.h │ │ ├── static_switch.h │ │ ├── tile_scheduler.h │ │ └── utils.h │ └── quantization │ ├── __init__.py │ ├── cuda_utils.h │ └── fp4_quantization_4d.cu └── setup.py
[/PRE]
[P] I wanted to port this to CuTeDSL to get the benefits of fast JIT compilation, and easier maintenance without having to go through the C++ template hell. Not just that, I wanted to iteratively optimize the kernel to squeeze out the last bit of performance which the C++ implementation may have overlooked.
[P] The optimized CuTeDSL port has only 4 main files. Rest of the files consist of the actual harness (testing correctness and benchmarking), and some probe files which Claude generated to check the behaviour of APIs specific to CuTeDSL not present in the original C++ implementation
[p] bash
[PRE]
    sage3_sm120 ├── api.py ├── attn.py ├── ptx.py └── quant.py
[/PRE]
[LINK] https://x.com/maharshii/status/2086442755748970889
[P] The process for porting was largely similar to what I discuss in a previous article of mine: https://x.com/maharshii/status/2086442755748970889
[P] For the context directory, I only gave Claude access to NVIDIA's cutlass and SageAttention repositories, with a locally installed version of CuTeDSL as well. I also asked it to maintain an INDEX.md file which specifies what each directory in context can be used for which looks something like:
[TH] folder
[TH] what it is
[TH] authoritative for
[TH] read for
[TH] size
[TD] cutlass/ (v4.8.0, include/cutlass/version.h)
[TD] NVIDIA CUTLASS, whole checkout. Only examples/python/CuTeDSL/ and python/CuTeDSL/ matter: the first is the example corpus a port starts from, the second is the DSL's own source, which is worth more than the examples — signatures, LaunchConfig fields and env-var handling are all readable there, and guessing at them costs a GPU run each.
[TD] the CuTe DSL API surface at 4.8, the Blackwell example kernels (dense/blockscaled/grouped GEMM, attention), the cute_ext pipeline + epilogue helpers, blockscaled_layout.BlockScaledBasicChunk (the ue8m0 / nvfp4 scale atom), and utils/blackwell_helpers.py
[TD] sage_sm120 and any future CuTeDSL kernel
[TD] 265 MB. examples/python/CuTeDSL/ is ~4 MB and python/CuTeDSL/cutlass/ ~10 MB; include/, test/ and tools/ are the rest and are C++ that informs nothing here.
[TD] SageAttention/ (main, shallow)
[TD] thu-ml's official SageAttention / SageAttention2++ / SageAttention3 — quantized attention as hand-written CUDA (mma.sync + cp.async + swizzled smem), plus Triton fallbacks and the fused quantizers.
[TD] the SageAttention3 arithmetic and CuTe layout abstractions
[TD] sage3_sm120 and any quantized fp4/fp8/int8 attention work
[TD] 106 MB checkout; 388 KB of csrc/ is the whole kernel. assets/ is 42 MB and example/ 12 MB — both are noise.
[P] Throughout the porting process, I asked my agent to note down any CuTeDSL gotchas and pitfalls it encountered in a README.md file. This helps it retain the knowledge related to DSL specific things and not forget it.
[P] Testing
[P] The harness file follows from my previous article as well, which was also iteratively improved based on the phase of the porting process. It runs a ladder of tests ordered in such a way that failures, if any, localize. It starts with the host-side code and then goes on to the kernel-side tests, ending with comparison vs. references.
[LINKREL] /maharshii/article/2100193318277890174/media/2100177195213987840
[IMG] https://pbs.twimg.com/media/HSVUCCYbIAAo_Vh?format=jpg&name=900x900
[P] reference test ladder
[P] PTX/SASS dumps
[P] The harness file also makes the agent dump PTX, SASS, and CUBIN files which lets it check for any kind of register spilling, instruction counts, vectorization, and other optimization opportunities. It creates asm_info.json where it writes the information related to the kernel from the dump files and it looks something like below:
[p] json
[PRE]
    { "backend": "cutedsl", "arch": "sm_120", "ptx_lines": 4500, "launch_directives": [ ".reqntid 384, 1, 1", ".minnctapersm 1", "setmaxnreg.dec.sync.aligned.u32 \t24;", "setmaxnreg.inc.sync.aligned.u32 \t232;" ], "ptx_target": "sm_120a", "ptxas": { "registers": 168, "spill_stores": 0, "spill_loads": 0, "smem": 0, "cmem": 0 }, "cuobjdump": { "registers": 168, "spill_bytes": 0, "stack": 0, "smem": 1024 }, "sass_counts": { "shared load": 116, "shared store": 16, "global load": 7, "global store": 2, "ldmatrix": 66, "stmatrix": 16, "blockscaled MMA": 128, "transcendental": 170, "barrier": 6, "narrowing convert": 104 } }
[/PRE]
[P] Optimization log
[P] The agent also kept a running log of the optimizations it tried in the same README.md file. The highest gains were observed from increasing/decreasing registers from the TMA/MMA warps and some deliberate departures the agent took compared to the C++ implementation:
[PRE]
    - masking compares token indices, not tile column indices, - causal masking also applies the unpadded-length limit, which the C++ drops - ``AbsMaxP`` is seeded from -inf rather than from uninitialised registers - the O accumulator is zeroed before the first PV MMA accumulates onto it - O is rescaled in place and accumulated onto directly, dropping the C++'s second accumulator -- 64 registers against a 456-byte spill - a causal tile with no work runs one fully masked block instead of taking the C++ early-exit, which would desynchronise the epilogue handshake
[/PRE]
[P] The optimization log table in the README file is:
[TH] change
[TH] spill
[TH] ld.shared
[TD] baseline (12 warps, setmaxnreg)
[TD] 456
[TD] 194 scalar
[TD] drop tOrO_tmp, rescale O in place
[TD] 304
[TD] 194
[TD] autovec_copy for the SF copies
[TD] 296
[TD] 98
[TD] 12 → 10 warps, setmaxnreg removed
[TD] 296
[TD] 98
[TD] min_blocks_per_mp=1
[TD] 272
[TD] 98
[TD] .align(16) on the delta_s loads
[TD] —
[TD] 50 (48 v4)
[TD] rcp.approx + mask only the peeled block
[TD] 104
[TD] 50
[TD] quantize interleaved under the PV MMA
[TD] 280
[TD] 50
[TD] 12 warps + setmaxnreg 24/232
[TD] 0
[TD] 50
[P] These implementation differences resulted in the CuTeDSL kernel being 1.2x faster than the original C++ implementation.
[P] Conclusion
[P] All in all, I feel that porting kernels quickly is a very good usecase of agentic kernel development. I wrote zero lines of CuTeDSL code during this porting process and still was able to get a kernel faster than the original implementation, condensing days of work to only hours. Some back and forth was still required to steer the agent to a better path, and it resulted in faster development compared to letting the agent figure everything out on its own. However, I'd bet that gap, where a human still has to step in, will keep closing as the models get better.
[LINKREL] /i/premium_sign_up
[LINKREL] /maharshii/status/2100193318277890174
[LINKREL] /maharshii/status/2100193318277890174/analytics
[p] 想发布自己的文章？升级为 Premium下午8:01 · 2026年9月16日·3,567 查看45246