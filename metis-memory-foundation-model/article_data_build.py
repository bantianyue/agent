#!/usr/bin/env python3
# -*- coding: utf-8 -*-
import json, os, sys

_article_dir = os.path.abspath(sys.argv[1] if len(sys.argv) > 1 else os.getcwd())

DATA = {
    "title": "Metis：首个记忆基础模型——把记忆内化进 Transformer 前向计算，128K 长文本快 1.5 倍",
    "summary": [
        {"key": "核心洞察", "body": "Agent 记忆一直靠外部模块（RAG等），Metis 首次提出记忆基础模型，把记忆内化进 backbone——原生记忆状态 + 原生记忆过程"},
        {"key": "架构设计", "body": "受 Fast Weight Programming 启发，Metis blocks 由 hyper memory block + local memory block 组成，通过 memory attention 集成；在线维护无梯度"},
        {"key": "关键结果", "body": "无上下文设置下 MemOps/LoCoMo 最佳；128K 下比 Full Context 快 1.5×、比 δ-Mem 快 2.4-2.7×、比 Temp-LoRA 快 6.5-8.7×"},
    ],
    "lead": [
        "近年来大模型快速发展，支撑起 AI agents 处理复杂任务。在推理能力之外，**记忆是 AI agent 的另一项关键能力**——负责保留过去信息并用于支持未来推理。此前绝大多数工作把记忆实现为模型外部模块（典型如 RAG 检索文本进 prompt），真正的原生记忆能力几乎未被探索。**MemTensor（上海）与多所高校提出的 Metis，迈出第一原型：用「记忆基础模型」把记忆从外部模块变成 backbone 的内部机制，直接参与前向计算。** 在线记忆维护全程无梯度、每次更新只需一次前向；推理时所有权重冻结，原生记忆状态随标准前向计算自主演化。",
    ],
    "sections": [
        {
            "type": "h2",
            "title": "为什么外部记忆有瓶颈",
            "paras": [
                "大多数之前的工作把记忆实现为模型外部的模块。代表性做法是用检索增强生成（RAG）检索相关文本信息并拼进 prompt 来辅助推理。**但外部记忆有三个结构性局限：** ① 与 backbone 解耦、目标与处理阶段分离——外部记忆旨在构造有信息量的上下文输入，backbone 只对构造好的上下文做条件语言建模，因此记忆未必提供对 backbone 推理最有用信息，backbone 也未必最优利用记忆；② 端到端优化困难——梯度无法有效穿过离散记忆操作传播，领域特定后训练极具挑战；③ 需要在 backbone 外对存储做额外显式操作，不可避免增加在线推理时延。",
            ],
            "fig_after": {
                "0": [{
                    "src": "fig00.png",
                    "caption": "Figure 1: 从外部记忆到原生记忆。"
                }]
            }
        },
        {
            "type": "h2",
            "title": "记忆基础模型：原生记忆的形式化",
            "paras": [
                "记忆基础模型把记忆从外部模块转化为 backbone 的内部机制，直接参与前向计算：基于输入指令和原生记忆生成回复，并自主做记忆变换。**原生态记忆从两个关键方面定义：**",
                "**原生记忆状态（Native Memory State）**：记忆基础模型跨多次推理保持状态（stateful）——在 backbone 内部形成、维护、利用来自先前推理的记忆状态。记忆状态动态地表示成 backbone 参数的一部分，其语义空间在预训练或中间训练阶段对齐。与之相对，**静态参数**中包含的是交互前的离线知识（严格说不算记忆，因为不含个人化轨迹信息、不需实时适应）。",
                "**原生记忆过程（Native Memory Procedure）**：不同于记忆工程，记忆基础模型把记忆过程原生集成进推理。记住、遗忘、更新等具体记忆操作在前向计算中自主完成，随输入指令影响原生记忆状态。**因记忆状态以动态参数表示、记忆过程通过计算执行，记忆基础模型可以像普通基础模型那样，用数据驱动方式做领域适配的后训练——这与从基础模型到推理模型（native CoT）的演化如出一辙。**",
                "从记忆的角度看，在线信息的「时间流」属性带来根本问题：存储阶段无法预知信息将如何被用作；推理阶段原始信息已不可得，只能用已存信息。**因此记忆本质上是预测问题——模型预测收到的信息将来如何被利用。既然记忆过程应在连续函数空间建模、经数值计算与 backbone 前向紧密耦合，规则型外部操作在离散函数空间里难以达到最优预测性能。**",
            ],
            "fig_after": {
                "3": [{
                    "src": "fig01.png",
                    "caption": "Figure 2: Metis 架构总览——融合原生记忆状态与记忆过程的记忆基础模型原型。"
                }]
            }
        },
        {
            "type": "h2",
            "title": "与 TTT / MANN / 记忆增强方法的区别",
            "paras": [
                "**vs 测试时训练（TTT）**：三点不同。① TTT 通常在单个序列内适应模型，动态参数更新以更好拟合当前输入；记忆基础模型定义在多步交互下，动态参数充当跨先前步的持久原生记忆状态并支持未来推理。② TTT 不显式提供原生记忆过程，其更新常由自监督语言建模驱动；记忆基础模型用记忆重建与记忆操作目标训练，使模型自主在潜参数空间执行语义记忆操作。③ TTT 多动机于高效长上下文建模、常引入循环层替代全注意力；记忆基础模型不替代当前步的标准全注意力，而是通过原生记忆状态以残差形式引入跨步信息。**TTT 主要是推理时适应机制，而记忆基础模型把记忆建立为持久的、指令驱动、过程感知的能力。**",
                "**vs 记忆增强神经网络（MANNs）**：MANN 引入额外记忆模块（可微记忆槽、学习读写操作），但记忆模块通常是受神经控制器控制的独立存储组件，仍外在于主模型参数。记忆基础模型把记忆内化进 backbone 计算——记忆状态以参数形式表示直接参与前向，记忆过程也用与 backbone 相同的连续函数空间建模，而非独立控制器。",
                "**vs 其他**：与 In-place TTT、MemGen、δ-Mem 等仍依赖上下文文本记忆并加潜摘要不同，记忆基础模型把文本记忆**彻底移出上下文**。与主要处理离线文档的 MEMO、MemFT 不同，记忆基础模型聚焦测试时信息。相比 Memory3 从离线语料编码可检索显式记忆，记忆基础模型把在线交互获取的信息内化为 backbone 内持久动态状态，并经原生记忆过程跨多步自主维护变换。",
            ],
        },
        {
            "type": "h2",
            "title": "Metis 架构：Fast Weight 启发的记忆块",
            "paras": [
                "Metis 采用因果语言模型作为记忆基础模型的原型实现。**受 Fast Weight Programming (FWP) 启发，Metis 设计了带原生记忆状态的模型架构，通过记忆注意力（memory attention）集成进 backbone 计算。** 具体地，Metis blocks 作为原生记忆的基本单元，每个主要由 hyper memory block（超记忆块）和 local memory block（局部记忆块）组成。",
                "原生记忆状态采用**参数化记忆**而非文本记忆：文本记忆虽有可解释性和跨模型兼容性的优势，但其离散表示信息密度低、需要重复预填充；参数化记忆以密集形式表示先前信息，提升存储与利用效率。动态参数与静态参数的语义空间需在预训练/中间训练阶段对齐，使不同步的动态参数能与静态参数协同计算。",
                "**在线记忆维护是免梯度的**——记忆更新只需前向 pass；推理时所有学习到的模型权重保持冻结，原生记忆状态经标准前向计算自主变换。这是效率的核心来源。",
                "**训练是这样完成的**：为支持 Metis 学习原生记忆能力，研究者从公开数据集构造大规模记忆特定训练数据（提取种子实体的多轮骨架：显式/隐式/干扰三种风格），设计记忆重建目标与记忆操作目标（对应记忆状态的压缩上限与操作目标），并加正则化目标增强复杂场景鲁棒性，通过**中间训练（mid-training）**让模型习得原生记忆过程。",
            ],
            "fig_after": {
                "0": [{
                    "src": "fig02.png",
                    "caption": "Figure 3: 记忆容量结果——步级（左）与轨迹级（右）。"
                }]
            }
        },
        {
            "type": "h2",
            "title": "实验结果：记忆能力与效率双验证",
            "paras": [
                "**记忆操作任务（MemOps）**：模型接收三段完整证据（Full 设置 24 个话语）或仅 oracle 轮（Gold 设置），评测记住/遗忘/更新/反思操作。**全文模型整体最强，但去掉上下文时标准 backbone 大幅下降；部分上下文在 Metis 测试集保留部分信息却在 MemOps (Gold) 表现差，说明不完整历史无法可靠支撑记忆操作。** Temp-LoRA 与 δ-Mem 恢复部分损失但增益有限。在相同无上下文设置下，Metis 在 MemOps (Gold) 与 Metis 测试集都取得最佳平均结果——证明 Metis 能把信息保存在原生记忆状态并用于后续步骤，无需重放原始上下文。",
                "**规模效应**：Metis-27B 在无上下文设置下两基准平均最佳，相对 4B/9B 在记住、更新、反思及整体表现明显提升，遗忘（尤其 Metis 测试集）提升最大——足够大的 backbone 能更好构建和利用原生记忆状态；遗忘仍是最难的操作。",
                "**效率：长上下文下的优势**：在 4B 规模、单张 A800、受控历史 512 到 128K tokens 下评测。上下文到 32K 前 Metis 端到端时延略高于 Full Context；在首个 64K 点反超。**到 128K，E2E 相对 Full Context 加速到 1.497×（32 token）与 1.595×（128 token）。** 相比状态型基线，128K 下 Metis 比 δ-Mem 快 2.40–2.67×、比 Temp-LoRA 快 6.46–8.71×。记忆写入（commit）仅占 Metis 时延的 2.1–4.3%，主要摄取耗时来自历史编码 backbone 前向。",
                "**记忆容量与低秩分解**：步级与轨迹级记忆容量实验表明 Metis 对多步信息的保持能力；低秩分解实验（不同记忆状态秩）揭示记忆状态的信息压缩特性与可分离性，为记忆容量的高效利用提供分析。",
            ],
            "fig_after": {
                "0": [{
                    "src": "fig04.png",
                    "caption": "Figure 5: Metis 在不同对话场景的案例研究。"
                }],
                "2": [{
                    "src": "fig06.png",
                    "caption": "Figure 7: 端到端延迟随受控上下文长度增长，32 生成 token（左）与 128（右）——Metis E2E 含流式历史摄取与查询生成。"
                }]
            }
        },
        {
            "type": "h2",
            "title": "局限与方向",
            "paras": [
                "尽管 Metis 在记忆相关任务表现优秀，仍有若干局限：**① 长程任务性能退化**——历史压缩进固定大小参数造成信息丢失；**② 某些情况出现信息混淆**——可能源于潜空间中语义的混合。除此之外，实现记忆基础模型的终极目标是彻底消除上下文对外部记忆的依赖。",
                "**路线图与方向**：原生记忆旨在逐步把基础模型从「无状态预测器」转变成「有状态学习者」，最终成为有能力持续学习与跨会话积累知识的模型。记忆基础模型为研究社区和工业界提供了一条通往记忆基础模型的潜在路径。项目与模型权重已开源（MemTensor / Metis）。",
            ],
            "fig_after": {
                "0": [{
                    "src": "fig05.png",
                    "caption": "Figure 6: 记忆基础模型路线图——原生记忆旨在逐步把基础模型从无状态预测器转化为有状态学习者，最终成为能持续学习的模型。"
                }]
            }
        },
    ],
    "conclusion": [
        "Metis 的贡献在于把「记忆」从工程组件重新定义为模型架构与训练目标的一等公民：**原生记忆状态成为 backbone 动态参数、原生记忆过程融入前向计算**。这使得记忆像 CoT 之于推理那样，从外部 prompt 技巧变成模型内化的能力——在线维护免梯度、更新只需一次前向，正是它在长上下文下既省 KV 缓存又端到端可优化的根源。",
        "效率数据最说明问题：128K 上下文下相对 Full Context 提速约 1.5 倍，相对 δ-Mem 快 2.4-2.7 倍、相对 Temp-LoRA 快 6.5-8.7 倍，而记忆写入只占时延 2-4%。外部记忆在长程/多会话场景的「重放膨胀」被参数化原生记忆彻底绕开。当然它仍有硬边界——固定参数容量对长程信息的丢失、潜空间的信息混淆——这些正是记忆基础模型下一步要攻克的核心。",
    ],
    "reference_url": "https://arxiv.org/html/2607.26760v1",
    "title": "Metis：首个记忆基础模型——把记忆内化进 Transformer 前向计算，128K 长文本快 1.5 倍",
}

out_path = os.path.join(_article_dir, "article_data.json")
with open(out_path, "w", encoding="utf-8") as f:
    json.dump(DATA, f, ensure_ascii=False, indent=2)
print(f"✅ 写入 {out_path} ({len(json.dumps(DATA, ensure_ascii=False))} chars, {len(DATA.get('sections', []))} sections)")
