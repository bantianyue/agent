Claude Mythos 系列：下一代前沿 MoE 模型的训练算力与成本分析

估算在 NVIDIA Blackwell GPU 上，参数量分别为 2T、4T、10T 总量级、且 MoE 稀疏度比为 5% 的假设性前沿 MoE 模型所需的基础设施。

2026年4月9日

说 Claude Mythos 令人惊艳都算是轻描淡写了。它代表了能力上的下一次阶跃式飞跃，正如 Dario 所说，它没有上限。

我们想搞清楚，训练下一代处于 2T、4T 和 10T 参数量级的模型，究竟需要多少算力和数据（传闻！）。话说回来，Elon 今天宣布他们正在训练这一量级的模型，所以这个估计非常应景。

方法论：我们采用估算 DeepSeek V3、ARCEE、KIMI 2.5 等模型训练成本时用过的公式和方法。GPU 性能数字取自 Nvidia 的 B200/B300 以及 NVL72 机柜的数据手册，数据可得性统计取自 Epoch AI。当我们提到 B200/B300 时，实际指的是 GB200/GB300 超级芯片，作为 NVL72 机柜的一部分。NVL72 的世界规模为 72 块 GPU，这让训练这一量级的模型变得更容易，因为那 72 块 GPU 可以通过 NVLink/NVSwitch 互联，以高吞吐互相访问彼此的 HBM。尽管如此，仅靠数据并行还不够，你仍然需要其他并行方式（流水线并行、张量并行、序列并行）。为了把所有瓶颈都考虑进去，我们假设 MFU（模型浮点利用率）只有 20%。

1. 假设条件
2. 训练数据需求

公式：

D = 每个参数对应的 token 数 × N_total

D = 40 × N_total

token 总数基于总参数量——所有专家权重在整个训练过程中都必须被训练，即使每个 token 只有 5% 被激活。

公开可爬取的互联网提供约 100T token（Epoch AI 分析，我在下方标出了 100T token 的刻度线）。需要更多 token 的模型必须依赖合成数据生成、改写，或多轮（multi-epoch）训练。正如我们稍后将会看到的，当我们逼近 10T 参数量级时，合成数据生成会变得非常昂贵。

3. 前沿 MoE 模型的稀疏度比

为了证明 5% 激活比的合理性，我们调研了最新的前沿模型：

为什么 5% 是一个可靠的假设

· DeepSeek V3/V3.2（5.4%）、GLM-5（5.4%）和 MiniMax M2.5（4.3%）都聚集在 4–6% 区间。5% 正好是这个区间的中心。
· Kimi K2.5 已经做到 3.1%——趋势是朝着更稀疏的方向发展。对下一代模型而言，5% 是偏保守的。
· 较老的模型（Mixtral、DBRX）使用约 27% 的激活率。18 个月内，整个领域从 3–6% 迁移，这是一个决定性的转变。
· DeepSeek V3 在 14.8T token 上以 5.5% 激活率训练成功，相比稠密基线没有任何质量损失。
· 5% 激活率带来实用的尺寸：2T→100B、4T→200B、10T→500B 每 token 激活参数——都是已被验证可行的架构规模。

4. 训练算力（FLOPs）

对于 MoE，每个 token 只有激活参数参与计算：

N_active = N_total × 0.05

C = 6 × N_active × D

C = 6 × (N_total × 0.05) × (40 × N_total)

C = 12 × N_total²

5. GPU 规格

B300 只有在 FP4 下才优于 B200。在 BF16 下，二者完全相同（2.25 PFLOPS）。

6. 计算链

步骤 1：Effective_FLOPS = Peak_FLOPS × 0.20
步骤 2：GPU_seconds = C ÷ Effective_FLOPS
步骤 3：GPU_hours = GPU_seconds ÷ 3,600
步骤 4：GPUs_needed = GPU_hours ÷ 2,160（= 90 天 × 24 小时）
步骤 5：NVL72_Racks = GPUs_needed ÷ 72
步骤 6：Total_Cost = GPU_hours × 每 GPU 每小时价格

7. FP4 情景

理论上的最佳情况——所有 FLOPs 都以 FP4 吞吐运行。

据我所知，这在今天还不可能，或者在这一规模上尚未被验证。Nvidia 在更小的模型（如 12B）上用 4 位精度训练做过。但即便在那里，他们也将许多其他部分保持在更高精度。引用原话："注意力、嵌入、非线性层以及其他张量：为了保证训练过程中的数值稳定性，我们对嵌入、输出投影头、归一化层、非线性激活，以及注意力组件（包括 softmax 和 query-key、注意力分数-值 的批处理 GEMM）保留原始精度（如 BF16 或 FP32）。主权重（由优化器存储）、权重梯度（用于跨微批次和跨数据并行副本的梯度累积）和优化器状态也保持在 FP32。张量并行归约在 BF16 精度下执行。"

B200（FP4）—— 每 GPU 每小时 $5
B300（FP4）—— 每 GPU 每小时 $6

8. BF16 情景

这是一个更现实的情景，即他们全程用 BF16 训练。

B200（BF16）—— 每 GPU 每小时 $5
B300（BF16）—— 每 GPU 每小时 $6

9. 成本对比总结

总训练成本
所需 NVL72 机柜数

10. 训练成本可视化

图1：按模型规模和 GPU 配置划分的训练成本
图2：所需 NVL72 机柜数

11. 合成数据生成成本

10T 模型需要 400T token，但互联网上只有约 100T 可用。剩下的 300T 必须合成生成。我们用 Claude Sonnet 4.6 作为代表性前沿 API 模型来分析成本。

Sonnet 4.6 定价
生成 300T 合成 token 的成本

假设输入与输出比例为 1:10（30T 提示 token → 300T 生成 token）：

标准版：(30T ÷ 1M × $3) + (300T ÷ 1M × $15) = $90M + $4,500M = $4.6B
批量版：(30T ÷ 1M × $1.50) + (300T ÷ 1M × $7.50) = $45M + $2,250M = $2.3B

（当然，对 Anthropic 来说，成本会比外部人低得多，因为他们不用付自己的利润率 :)

12. 总成本：训练 + 合成数据（10T 量级）

图3：10T 量级成本拆分——训练 vs 合成数据

节省合成数据成本的务实替代方案

· 自托管推理：在自有 GPU 上运行一个 Llama 量级的模型（MiniMax 等会不错——因为它们的激活参数更少且性能稳健），生成 300T token 成本约 $50-100M——比 Sonnet 4.6 API 便宜 20-40 倍。
· 多轮训练：用课程学习在同一批 100T token 上训练 4 次。零数据生成成本，尽管在 2-3 轮之后收益递减。
· 改写/增强：取 100T 互联网 token，用廉价模型改写 3-4 次。比从零生成便宜得多。
· 混合方法：100T 互联网 + 50T 高质量合成 + 3 轮，约 400T 有效 token，生成成本极低。

13. 那后训练呢？

我们主要关注预训练和合成数据生成成本。后训练（SFT + RL）可能额外花费 20%（Epoch AI 对 DeepSeek R1 的分析）。xAI 报告说，他们在 Grok 4 的后训练上花费的算力几乎与模型预训练一样多。

本次思维推演的关键发现

2T 量级非常可实现：191 个 NVL72 机柜，BF16 下 $148M。NeoLabs 仍能应付。数据需求（80T）在互联网可得性范围内。

4T 量级可行：762 个机柜，BF16 下 $593M。需要约 60T 合成 token 才能达到总共 160T token——可管理，但开始变得非常昂贵。

10T 量级在超大规模云厂商伙伴和成吨现金的支持下触手可及：4,763 个机柜，BF16 下 $3.7B。但 300T 合成 token 还要额外增加 $2.3B（API）或约 $75M（自托管更便宜的模型）。

合成数据的成本可能和训练一样高：按 API 定价，通过 Sonnet 4.6 生成 300T token 花费 $2.3B——这几乎相当于 10T 量级模型在 B200 上以 BF16 精度训练成本 $3.7B 的 60%。在这一量级上，自托管生成可能变得不可或缺。

B300 只有在 FP4 下才胜出：在 BF16 下，吞吐量与 B200 完全相同，但贵 20%。据我理解，Nvidia 将 B300 定位为推理而非训练。

数据是主要瓶颈：不只是算力或 GPU 本身——10T 模型所需的 400T token 要求可能是最难满足的约束！！！！

免责声明

这只是一个思维推演，用来讨论下一代模型所需算力的大致数量级。我使用了所有公开的公式和性能数字。谢谢！
