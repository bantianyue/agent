{"0": "深入解析 AMD Instinct MI450 GPU 上的 LDS 优化", "2": "在 GEMM kernel 中，操作数从 global memory 经由 Local Data Share (LDS) 进入 register，再由矩阵核心消费。数据通过 LDS 的搬运效率往往是整体性能的首要决定因素。", "3": "LDS 的两个特性会悄悄消耗掉可达吞吐的很大一部分。第一个出现在内存中的 layout 与矩阵核心对 WMMA（Wave Matrix-Multiply-Accumulate）所期望的 layout 不一致时，例如矩阵核心需要 K-contiguous layout，而 B 操作数是 N-contiguous 的。此时普通的 LDS load 无法向量化，而转置加载（transposed load）会即时重排数据，恢复向量化访问。第二个是物理 LDS 分区上的争用：通过不同端口读取 LDS 的 warp[1] 反复命中同一分区，导致访问串行化。本文考察 AMD Instinct™ MI450 GPU（gfx1250）上的这两种效应以及规避方法。", "4": "本文将介绍：", "5": "«B»第一部分：转置 LDS 加载«/B»：？0？ 协作转置（cooperative-transpose）指令的工作原理及其用法。", "6": "«B»第二部分：分区冲突«/B»：什么是分区冲突（partition conflict），如何组织 WMMA ？0？ 与 ？1？ 来避免它，以及编译器的分区感知分配器如何把数据在物理上分散到各个 LDS 分区。", "7": "本文面向已掌握 GEMM kernel 编写基础的 Gluon 与 Triton kernel 开发者。Gluon kernel 新手请先阅读 From Naive to Near-Peak: Building High-Performance GEMM Kernels with Gluon。", "8": "linear layout 回顾", "9": "本文两部分都大量依赖 Triton 的«B»linear layout«/B»，因此先在此汇总要点。linear layout 见 «I»Linear Layouts: Robust Code Generation of Efficient Tensor Computation Using ？0？«/I»；更平缓的讲解可参考 Lei Zhang 的 “Triton Linear Layout: Concept” 与 “Triton Linear Layout: Examples”。", "10": "基本思路", "11": "«I»tensor layout«/I» 是逻辑 tensor 坐标与持有它们的硬件资源（register、lane（一个 warp 的线程）、warp 或 shared-memory 偏移）之间的映射。过去每种这样的映射都是定制的、手写的属性，任意两者之间的转换都需要逐例编写代码，其复杂度随 layout 数量呈二次增长。", "13": "本文中 layout 写作一列输出偏移，每个输入基对应一个。例如 ？0？ 表示输入基 ？1？ 映射到 ？2？，？3？ 映射到 ？4？，因此 ？5？（= 1 XOR 2）映射到 ？6？。", "14": "layout 的除法：一条指令如何在 tile 上重复", "15": "单条硬件指令，无论是 ？0？、？1？ 还是向量化 load，都有一个很小且固定的 layout，描述«I»一条«/I»指令的哪些 lane 与 register 触及小 tile 的哪些元素。完整的操作数 tile 更大，因此该指令必须在其上«I»重复«/I»。", "17": "\\begin{split}L = [ T amp; 0 ; 0 amp; R ].\\end{split}", "18": "若 ？0？ 是整个 tile 的 layout，？1？ 是一个 layout 因子，则商 ？2？ 是约去 ？3？ 后剩下的 layout。当 ？4？ 表示一条完整指令时，商枚举该指令的各个副本。但在下文的 ？5？ lowering 中，？6？ 只是 contiguous 基 ？7？，因此商会保留额外的 lane 与 register 结构。", "19": "在 Triton 中，这些重复体现为 distributed layout 的 ？0？（register）输入基。这正是我们后面操作的 ？1？ 维度：当一个 warp 发出多条指令时，其 ？2？ 基在 tile 上跨步，依次放置副本 0、副本 1 等。", "20": "第一部分：转置 LDS 加载", "21": "当源 LDS layout 与目标 register layout 的 contiguity 不同时，一个 ？0？ 可能被 lower 成许多窄的 ？1？ 指令。本节说明 ？2？ 如何用每 lane 的宽读取和固定的跨 lane 数据交换来处理这种不匹配。", "22": "为什么重要？", "23": "不使用 ？0？ 时，在 HBM 中 contiguity 不同的 WMMA 操作数（例如 N-contiguous 的 B 操作数）会在两方面代价高昂：", "24": "若写入 LDS 时仍保持不同的 contiguity，则要求其为 K-contiguous 的 ？0？ 无法使用宽 ？1？ 向量 load，因为元素在内存中是 strided 的。编译器只能退回到更小的 load，每个 lane 会发出多条 ？2？ 指令。", "25": "反之，若把 shared layout 的 contiguity 设为 K-contiguous 以便 load 向量化，则 ？0？ 和 ？1？ 都无法在从 HBM 拷贝到 LDS 时完成这种重排。操作数必须先 load 到 register 再 store 到 LDS，把改变 contiguity 的开销转移到 store 一侧。编译器只能退回到更小的 store，每个 lane 会以 strided 的 K-contiguous 模式发出多条 ？2？ 指令。", "26": "使用 ？0？ 可同时避开这两个取舍。每个 lane 都能发出向量化的 LDS 读取（依数据类型为 64 或 128 位），随后硬件在 lane 组之间重新分配数据，使读取的数据匹配目标 layout。这在指令数量与 register 压力两方面都有收益。大量聚集在一起的小 ？1？ 指令还可能耗尽 ？2？ 计数器，迫使硬件等待 ？3？ 完成。", "27": "举一个具体例子：Triton ？0？ 中的 fp16 forward attention kernel，使用 ？1？、？2？、？3？ 和八个 warp，在无 causal masking 的情况下为 gfx1250 编译。启用转置加载时，生成的汇编包含 128 条 ？4？ 指令。禁用转置加载后，这些指令被 1024 条 ？5？ 指令取代，VGPR 用量从 308 增至 512，并导致 233 个 VGPR spill。", "28": "？0？ 做什么？", "29": "MI450 提供多种 ？0？ 变体。我们以 ？1？ 和 ？2？ 作为代表性例子，因为它们在实践中出现最多；两者都以 4 个独立的 8 lane 组，在整个 32 lane 的 warp 上完成协作转置加载。？3？/？4？ 后缀给出每 lane 读取的比特数，？5？/？6？ 给出元素宽度。图 1a 与 1b 给出这两个变体在重新分配前后 32x8 tensor 的每 lane 归属。", "30": "图1a：？0？ 的 32x8 tensor 归属示意图。左侧显示每个源 lane S 读取的八个元素；右侧显示四次独立 8x8 转置后每个目标 lane D 持有的八个值。", "31": "图1b：？0？ 的 32x8 tensor 归属示意图。与 ？1？ 使用的连续分组不同，每个源组由两段各四个 lane 组合而成。在每张图中，颜色将其 ？2？ 或 ？3？ lane 标签分成每块八个。", "32": "该指令加载的数据以 ？0？ 为 tile 大小进行转置，其中 ？1？ 是每个 lane 读取的连续元素数，？2？ 是一组中的 lane 数。对 ？3？ 和 ？4？ 而言，这都构成一个由 8/16 位元素组成的 8x8 tile，硬件将其转置，使目标 lane ？5？ 收到第 ？6？ 列。", "33": "例如，在一个 8 lane 组内（？0？…？1？ 指源 lane 0…7）：", "34": "目标 lane ？0？ 收到第 ？1？ 列：？2？，即每个源 lane 加载的第一个 b16 元素。", "35": "目标 lane ？0？ 收到第 ？1？ 列：？2？，即每个源 lane 加载的第二个 b16 元素。", "36": "目标 lane ？0？ 收到下一个独立组的第 ？1？ 列：？2？。由于 N 等于组大小，组内每个 lane 都从该组的«I»全部«/I» 8 个 lane 取值。", "37": "？0？ 变体使用与 b16 版本不同的 lane 分布：每个 source group 由两段各 4 个 lane 组成。例如，destination lane ？1？ 到 ？2？ 取自 source lane ？3？ 到 ？4？ 以及 ？5？ 到 ？6？。图 2a 和 2b 把这些按组划分的 8x8 tile 组装成完整的 16-bit 与 8-bit WMMA B-operand tile。", "38": "图2a：完整的 16-bit WMMA B-operand tile 的 gfx1250 lane 归属。每个 16x16 panel 对应一条 ？0？ 指令。", "39": "图2b：完整的 8-bit WMMA B-operand tile 的 gfx1250 lane 归属。每个 16x16 panel 对应一条 ？0？ 指令，每个 source group 拆分为两段各四个 lane。", "40": "上面的示例展示了当每个 lane 的 LDS 地址构成 WMMA operand 的一个 8x8 slice 时该指令的行为。这并非硬件限制：只要每个 lane 自身的 64/128-bit 读取是连续的，它就可以从独立的 LDS 地址读取；无论地址是什么、这些 bit 代表什么，硬件都应用相同的固定重分布。", "41": "由于重分布不依赖地址之间的相互关系，“transpose” 只是某一种特定地址模式的心智模型；该指令可以支持更通用的数据移动。是否可用仅取决于所请求的 ？0？ mapping 是否符合该指令固定的 data-movement pattern。把两者都表示为 linear layout，编译器就能直接检测这种匹配，而无需识别高层的 transpose 操作。图 3 展示了这种灵活性：每个 lane 提供独立的 LDS 地址，？1？ 对所选向量应用相同的固定跨 lane 重分布。", "42": "图3：？0？ 不要求 source 地址描述某个几何 tile 的行。每个 lane 提供独立的 LDS 地址，指令则对所选向量应用相同的固定跨 lane 重分布。", "43": "从 LDS bank conflict 的角度看，？0？ 的行为与普通的 ？1？ 相同。硬件依据各 lane 发出的 LDS 地址选择 bank，跨 lane 重分布在此之后发生。因此分析 bank conflict 应使用 source LDS layout，而非 destination register layout。", "44": "在 Triton 中把 ？0？ lower 为 ？1？", "45": "Triton 在 ？0？ 中实现该 lowering，作为 TTGIR 到 LLVM IR 转换的一部分。该 lowering 把 shared 和 destination encoding 转换为 linear layout，然后计算 ？1？，它把每个 destination 硬件位置 ？2？ 映射到其 source LDS offset。", "46": "考虑一个玩具级 ？0？ 示例：单个 warp，？1？，？2？，每个 lane 8 个元素：", "47": "？0？ 的 ？1？ 使 dim 0（？2？）成为 LDS 连续维度，而 ？3？ 已经表示为 linear layout（即下文几段后的 “Identity” 情形）。将二者 compose：", "48": "？0？ 构建 ？1？ 的方式是：取每个 ？2？ 的目标 ？3？，将其与 ？4？ 的每个目标匹配，然后读取该 basis 的 offset。例如 ？5？ 有 ？6？，它与 ？7？ 的 ？8？ 匹配，因此 ？9？ 将得到 ？10？。要使该查找始终成功，？11？ 必须满射到 ？12？。", "49": "该指令按其操作的最小 layout factor 建模，从而避免编写专门代码去模式匹配特定形状，例如“这个 destination layout 看起来像 shared memory 的 transpose”。这是因为指令的定义取决于它在硬件上如何搬运数据，而不是从 ？0？ 到 tensor 坐标的映射。？1？ 是该指令要求的连续 factor，destination layout ？2？ 只有在 ？3？ 有结果时才能使用该指令。quotient 描述 ？4？ 被分解出去后剩余的结构；？5？ 提供一个完整指令剩余的 lane 和 register 映射。", "50": "该 lowering 用两个相关的 layout 对 ？0？ 建模：", "51": "？0？ 描述指令选择所需的连续部分。对于固定的 destination register，destination lane 0 到 7 请求 8 个连续的 LDS offset。", "52": "？0？ 把 ？1？ 扩展到一个指令涉及的所有 lane 和 register。其 ？2？ 维度标识连续 base ？3？ 内的一个元素，其 ？4？ 维度标识该元素由哪个 lane 的 LDS 读取提供。剩余 lane 和 register base 分配给 ？5？ 的顺序，编码了该指令的数据交换模式。两条指令使用相同的 base ？6？，但在整个指令中分布不同。图 4a 和 4b 分别对两个变体的一个 ownership panel 采样，展示这些 base 如何从 ？7？ 映射到 ？8？。", "54": "图4b：对图 2b 中一个 ？0？ ownership panel 做同样的采样。lane 和 register base 以不同顺序分配给 ？1？，因此八个 image 中只有两个发生变化：？2？ 现在选择 source lane S4 而非 S8，？3？ 选择 source lane S8 而非 S4。正是这一点把每个 group 拆分为两段各四个 source lane，在 source panel 中表现为每个八 register 区域的颜色在中途改变。", "55": "后续两个主要 lowering 步骤是：", "56": "«B»合格性检查：«/B»构建从 destination 硬件位置到 LDS offset 的 access mapping，然后检查它是否包含该指令要求的 data movement pattern。", "57": "«B»指令发射：«/B»从 access mapping 中分解出该 pattern，推导每个运行时 lane 和 warp 读取的 LDS 地址，并为每个剩余 repetition 发射一条指令。", "58": "这些是作用于 layout mapping 的操作，而非特定于 linear layout。Triton 用 linear layout 表示这些 mapping，并使用 compose、inverse 和 division 执行这些操作。图 5 可视化对 ？0？ basis mapping 的合格性检查，图 6 展示用于推导运行时 LDS 地址的 mapping 序列。", "60": "图5：？0？ 的合格性约束 ？1？ 的前 3 个 lane base，即由 destination layout 与 shared layout 的 inverse compose 得到的 ？2？ map。这些 base 必须匹配 base ？3？（？4？），而其余每个 basis 都必须映射到 8 的倍数。repetition base 可以重新排列（中）；交换两个必需的 lane base（右）会使 ？5？ 无结果，load 回退为 scalar。", "61": "如果 ？0？ 成功，其 quotient 描述 ？1？ 的 repetition 如何在 ？2？ 中排布，但位于一个已把 tile 本身分解出去的降维坐标空间中。lowering 计算 ？3？ 将其提升回 ？4？ 的完整 offset 空间。这会把 tile 的 lane base 恢复为零，因此 ？5？ 把每个 destination register/lane/warp 位置映射到其 tile 的基准 LDS offset，而不是该 tile 内的某个位置。", "64": "关键要点", "65": "«B»转置加载（transposed load）消除了连续性不匹配的开销。«/B»当 WMMA operand 位于 LDS 中且其连续性与 register layout 所需不一致时，普通的 ？0？ 会退化为许多窄 ？1？ 指令。？2？ 则让每个 lane 发出一次宽读取，由硬件把数据重分布到 destination layout，从而减少指令数和 register 压力。", "67": "分区冲突（Partition Conflicts）", "68": "转置加载（transposed load）优化的是 «I»单个 warp«/I» 从 LDS 读出的过程。分区冲突（partition conflict）关注的是 «I»多个 warp«/I» 同时读取 LDS 时发生的情况。MI450 上每个 Workgroup Processor（WGP）[2] 包含四个 SIMD（向量）单元，组织为两个物理 pair。Pair A 包含 SIMD 0 和 SIMD 2；Pair B 包含 SIMD 1 和 SIMD 3。warp 以轮转（round-robin）方式派发，warp ？0？ 运行在 SIMD ？1？ 上，因此偶数编号的 warp 总是落在 Pair A，奇数编号的 warp 落在 Pair B。", "69": "WGP 还包含 «B»六个 64 KiB 的硬件内存分区（memory partition）«/B»。该划分为硬件固定：«B»其中五个属于 LDS«/B»，构成其 320 KiB 容量，第六个支撑 L0 向量缓存。这些分区就是后续争用的单位。", "70": "每个 SIMD pair 将其 LDS 请求发送到共享的 «B»LDS/L0 仲裁器（arbiter）«/B»，该仲裁器拥有 «B»两个端口 L 和 C«/B»，每个端口速率 256 B/cycle，且都能到达每一个分区。当仲裁器能为两个到达的请求分配不同端口时，它们在同一 cycle 访问 LDS，合计 512 B/cycle；否则它们被串行化，有效带宽减半至 256 B/cycle。图 7 展示了该数据通路（datapath）：两个 SIMD pair 都送入共享仲裁器，其两个端口可到达全部六个硬件分区。", "71": "图7：单个 MI450 Workgroup Processor 的 LDS 数据通路：两个 SIMD pair 都送入共享的 LDS/L0 仲裁器，其两个端口可到达全部六个硬件分区。", "72": "分区冲突的判定规则", "73": "当来自 «B»不同 SIMD pair«/B» 的 warp 在 «B»同一 cycle«/B» 向 «B»同一物理 LDS 分区«/B»发起 LDS 访问时，就发生 «B»分区冲突（partition conflict）«/B»。", "74": "两个条件必须同时成立：", "75": "«B»位置（Location）«/B»：两次访问指向同一分区。", "76": "«B»时间（Time）«/B»：两者在同一 cycle 发起。", "77": "只有跨 pair 的访问才可能同时满足两个条件，因为一个 pair 每 cycle 最多向仲裁器提交一个 LDS 请求。当两个此类访问在分区和 cycle 上确实重合时，仲裁器将它们串行化，LDS 带宽减半。因此打破«I»任一«/I»条件即可避免冲突。Triton 和 Gluon 无法控制 warp 发起访问的 cycle，因此位置是唯一可施加影响的条件。", "78": "分离位置正是本部分后续逐步构建的内容：", "79": "标准 WMMA layout 为何在 operand A 上产生冲突。", "80": "如何将 CTA layout 写成 linear layout，从而把两个 pair swizzle 开。", "81": "为什么该 swizzle 单独还不够，以及 ？0？ 补上了什么。", "82": "？0？ 的选择如何同时决定将 operand 存入 LDS 的 TDM 开销。", "83": "partition 感知的 allocator 如何让分离落到物理层面。", "84": "由此产生的 local load 如何以几乎零运行时代价完成 lowering。", "85": "分离实际带来多少收益，用一个只读 LDS 的 kernel 测量。", "86": "说明", "87": "由于时间是条件之一，分区冲突不像 bank conflict 那样是一个确定量。同一 warp 内的线程以锁步（lockstep）方式执行，因此 bank conflict 可以计数。但不同 warp 之间存在相对漂移，因此我们«I»假设«/I»四个 warp 在同一 cycle 发出同一条指令来建模。在该假设下，分离位置可直接消除冲突；实际中它只是降低冲突概率，而非严格降到零。", "88": "标准 WMMA layout 为何产生冲突", "89": "考虑一个采用标准 WMMA CTA layout 的 4-warp GEMM，最初在 Gluon 中用 ？0？ 表示。？1？ 是一种专用简写，描述 warp 在输出 tile 上如何排布，同一映射如今 Gluon 也以通用的 WMMA ？2？ 形式表达，稍后会讲到。它将四个 warp 在 tile 上排成 2x2 网格，如图 8 所示：", "90": "图8：在默认 ？0？ layout 下，每个 M-row 由一个 Pair A warp 和一个 Pair B warp 共享。", "91": "位于 tile ？0？ 的每个 warp 读取其 M 位置对应的 A 行和其 N 位置对应的 B 列。这里的「M-row」不是单条矩阵行，而是一个 warp 用单条 WMMA 指令覆盖的一段 M 行。同一 M-row 上的 warp 读取 «B»相同的 A 数据«/B»。在该 layout 中，？1？（Pair A）与 ？2？（Pair B）共享 M-row 0，？3？（Pair A）与 ？4？（Pair B）共享 M-row 1。", "92": "两个 pair 读取相同的字节，这些字节就位于同一分区，而相同的数据无法跨分区散布。因此该 layout 在 A 上锁定了位置条件：两个 pair 必然共处同一位置，只有它们的相对时序决定某次访问对是否真的碰撞。这正是应当避免的情形，因为它没有留下任何可控空间。operand B 则没有问题：共享同一 N 列的 warp（？0？+？1？、？2？+？3？）属于同一 pair。", "93": "将 CTA Layout 表示为 Linear Layout", "94": "回顾 linear layout 基础：layout 通过为每个输入基（input basis）分配一个 ？0？ 偏移并用 XOR 组合，把硬件输入映射到张量坐标。？1？ 是该描述中负责把 warp 置于输出 tile 之上的部分：其 ？2？ 个基为每个 warp 分配 tile，其 ？3？ 个基在单个 warp 覆盖多个 tile 时枚举重复。", "95": "专用的 ？0？ 变为如下 ？1？：", "96": "我们只指定 ？0？ 个基：它们已覆盖输出 tile（该 layout 是满射的），因此 Triton 会在流水线后续自动补齐 ？1？ 重复。解读该 layout：？2？ 映射到 ？3？，？4？ 映射到 ？5？，因此 ？6？（分解为 ？7？ XOR ？8？）落在 ？9？。", "97": "swizzle 后的 layout 保留同样的四个 warp，但改变 ？0？ 使其沿 M 方向移动：", "98": "？0？ 现在映射到 ？1？，而非 ？2？。此改动后，仅靠 ？3？ 个基不再覆盖该 tile，因此我们显式加入一个 ？4？ 基以恢复满射性。该 ？5？ 基即重复：？6？ 在第二次迭代时组合 ？7？ 与 ？8？，落在 ？9？。", "99": "上述两个 ？0？ 仅有一处不同：输入基 ？1？，它决定来自不同 pair 的 warp 是否读取相同的 M-row。在标准 layout 中它映射到 ？2？，即纯 N 方向的移动，这正是跨 pair warp 落在同一 M-row 并读取相同 A 字节的原因。swizzle 将其改为 ？3？，其非零的 M 分量把这些跨 pair warp 推到不同的 M-row，从而读取不同的 A 数据。图 9 展示了结果 layout，两个 pair 位于互不相交的 M-row 上。", "100": "图9：采用 swizzle 后的 layout，所有 Pair A warp 占据一组 M-row，所有 Pair B warp 占据另一组互不相交的 M-row。A 上的跨 pair 共享已消失。", "101": "当 ？0？ 不够时：？1？", "102": "对 ？0？ 做 swizzle 只能保证跨 pair warp 访问不同的 «I»tile«/I»，但无法决定这些 tile 落在哪个 «I»物理分区«/I»。如果整个 operand 都能装进单个 64 KiB 分区，例如 fp16 下的 A ？1？ 恰好为 32 KiB，那么无论 warp 对应哪个 tile，都从这同一个分区读取，Pair A 与 Pair B 再次碰撞。", "103": "？0？ 是 Gluon 中负责这种切分的机制。它接收四个参数：？1？、？2？、？3？ 和 ？4？，它们共同承担两个作用。其一，告知 allocator «I»哪些逻辑片段必须位于不同的物理分区«/I»，以便将其放入不同的 64 KiB 区域。其二，为编译器提供足够信息以构建 «I»shared memory 地址与逻辑张量坐标之间的唯一映射«/I»，从而把每次 load 和 store lower 到正确地址。", "104": "从一个具体 kernel 推导这些参数最容易理解。考虑如下 GEMM：？0？，四个 warp，fp16，使用前文 swizzle 后的 ？1？。该 layout 的 tile 为 64x32，即 4x2 个 16x16 warp tile，并在 128x128 输出上重复。", "105": "图 10 给出完整图示，每个参数都可直接读出。中心是 ？0？ 输出，呈 8x8 个 16x16 warp tile 的网格。swizzle 后 ？1？ 的 tile 是由分隔线勾勒出的 64x32 块（4x2 组 warp tile），它重复 ？2？ 次以覆盖 C。左侧边缘是沿 M 切分的 operand A，顶部是沿 N 切分的 operand B。每个这样的切片是一个 «B»piece«/B»，即 operand 沿切片维度的一段连续区间，图中标注为 ？3？、？4？ 等。", "106": "图10：swizzle 后的 ？0？ tile 在 C 上重复，每种颜色标记一条指令在全部四个 warp 上计算的 tile。A 沿 M 切成 ？1？ 个 piece（group G0-G1）；B 沿 N 切成 ？2？ 个 piece（group G0-G3）。下方子图展示轮转（round-robin）分布：偶数 piece 给 Partition 0，奇数 piece 给 Partition 1。", "107": "«B»？0？«/B» 是切分所沿的轴。swizzle 后的 ？1？ 已把两个 SIMD pair 沿 «B»非 K 维度«/B» 铺开，因此就从这里切：A 切 ？2？（M），B 切 ？3？（N）。", "108": "«B»？0？«/B» 由冲突规则推出。swizzle 按 pair 划分 warp：？1？、？2？ 是 Pair A，？3？、？4？ 是 Pair B，且每条指令中每个 pair 读取 ？5？ 的不同 band。给每个 pair 分配各自的 buffer（Pair A -> partition 0，Pair B -> partition 1），冲突即消除：两个 pair 对应 «B»？6？«/B»。注意这个逻辑数量不是 WGP 的五个物理 partition：冲突严格发生在«I»两个«/I» SIMD pair 之间，因此只需要两个位于不同物理 partition 的 buffer；五个物理 partition 只是给分配器留出空间，把这两个 buffer 分开放置。", "110": "«B»？0？«/B» 是最后一个参数：描述单个 piece «I»在«/I»其 partition 内如何布局。这是一个普通的 ？1？，其 ？2？ 和 ？3？ 的选择使 piece 内部的访问没有 bank conflict。综合起来，两个 operand 为：", "111": "选择 ？0？ 以减少 TDM 流量", "112": "piece 数不只是记账，它决定了 operand 如何从 global memory 加载。在 MI450 上，Tensor Data Movement engine（TDM）把 tensor 拷贝进 LDS，每条 TDM 指令«B»每个 warp 最多搬运一个 piece«/B»，这是与 warp 可写入的 striding 模式相关的硬件约束。因此指令数直接由 piece 数决定：", "113": "TDM 指令数 = ⌈ 片数 / warp 数 ⌉", "114": "四个 warp 时：", "115": "«B»Operand A«/B»：4 个 piece，4 个 warp -> 一条 TDM 指令覆盖全部 piece。", "116": "«B»Operand B«/B»：8 个 piece，4 个 warp -> 需要两条 TDM 指令。", "118": "图11：让每个 warp 跨越四 tile 条带可得到更大、更少的 piece。颜色同样标记每条指令计算的 tile。Operand A 不变（四个 ？0？ piece，？1？），但 operand B 从八个 ？2？ piece 缩减为两个 ？3？ piece（？4？）。", "119": "这一选择带来两点。其一，TDM 成本下降：四个 warp 下，operand B 的两个 piece 现在只需«B»一条«/B» TDM 指令而非两条。其二，更大的 piece 更好地利用内存系统，B 是 N 连续的，在 fp16 下一条 128 字节 cacheline 容纳 64 个 N 元素，因此 ？0？ piece 恰好是一条 cacheline，而旧的 ？1？ piece 只填满其四分之一。两个 pair 之间的隔离未受影响；只是用 piece 粒度换取了更少、利用率更高的传输，因此 WMMA ？2？ 与 ？3？ 参数必须一起选择。", "120": "从更粗的 tile 读出新参数：operand A 不变，但 operand B 现在只有一个 group（它的两个 piece 是一个 ？0？ cycle）：", "121": "partition 保证如何落实：partition 感知的分配", "122": "linear layout 说明一个 piece 属于«I»哪个«/I» partition；分配器则让这些 partition 落在 LDS 中«I»不同的物理 64 KiB 区域«/I»。Triton 的 shared memory 分配器是来自经典论文“Algorithms for Compile-Time Memory Optimization”的贪心区间放置算法，partitioned 情形是它的一个小扩展（Allocation.cpp）。", "123": "基础算法把每个 buffer 视为 (time x offset) 空间中的一个矩形，沿 time 是其存活区间，沿 offset 是其大小，并贪心地把每个 buffer 放在不与任何同时存活的 buffer 重叠的最低 offset 处。针对 partitioned tensor 的扩展增加两个要素。", "124": "其一，当 ？0？ 使用 ？1？ 时，分配器创建 ？2？ 个 buffer（每个包含该 partition 全部 group 的拼接，因此大小为 ？3？），并把它们全部标记为互为«B»neighbor«/B»。neighbor 必须占用不同的物理 partition。", "125": "其二，放置通过两个阶段实现 partition 感知：", "126": "«B»Stage 1（初始放置）。«/B» 在基础算法中，该步骤从最低 offset 向上扫描开放空闲 slot，并在每个 slot 处选择存活区间适配的«I»第一个«/I»候选，赋予它该 offset，并把剩余空间拆成新 slot。partition 感知在这一选择上增加一道门：候选只有在落入与已放置 neighbor 不同的物理 partition 时才被接受。这会使当前 slot 被阻塞，即所有能放下的候选都会与某个 neighbor 的 partition 冲突。此时分配器在更高的 offset 处重新打开 slot，越过 neighbor 的 partition，使某个候选在那里变得可接受。由于 offset 只会上移，算法持续取得进展。图 12 追踪该 stage-1 放置的四次迭代，不断增大搜索高度，直到每个 partition buffer 找到与其 neighbor 不冲突的 partition。", "127": "图12：Stage-1 放置不断增大搜索高度，直到每个 partition buffer 找到与其已放置 neighbor 不冲突的 partition。", "128": "«B»Stage 2（冲突精化）。«/B» 在基础算法中，初始 offset 仍可能让某些 buffer 重叠，因此该步骤构建 interference graph，即任意两个同时存活且字节范围重叠的 buffer 之间连边，对其着色，并把每个 buffer 推到与其冲突者之上，反复进行直到没有重叠。partition 同样需要该步骤，因为这一阶段执行的 offset 上调本身可能把某个 buffer 滑入与其某个 neighbor 相同的物理 partition，破坏 stage 1 建立的保证。因此 partition 感知再增加一类 interference 边：共享同一物理 partition 的两个 neighbor。解析该边时，buffer 被推过 neighbor 的 partition，而不只是推过其字节范围，从而恢复保证。", "129": "partitioned local load 如何 lower", "130": "？0？ 与分配器在编译期解决一切；最后一个问题是 load 本身在运行期需要计算什么。每个 operand 带有 «B»distributed«/B» layout，把线程的 ？1？ 映射到 tensor 坐标，以及 «B»shared«/B» layout，把 LDS 位置映射到同一坐标。后端生成二者的复合，？2？。partitioned shared layout 只是增加第二个输出维度，因此该转换产生一对 ？3？，即«I»从哪个物理 buffer«/I»读取，以及其中的位置。图 13 展示 64x64 partitioned operand A 的该复合，高亮为新的 ？4？ 坐标提供输入的基。", "131": "图13：partitioned shared layout 增加一个 ？0？ 输出维度。将其与 distributed layout 复合得到 ？1？；高亮的基为新的 ？2？ 坐标提供输入。", "132": "与所有 linear-layout 输出一样，partition index 是各 basis 贡献的 XOR，每一项都能廉价地求解：？0？ 项是编译期常量，？1？ 项是运行期值但«B»循环不变量«/B»，而 ？2？ 通常根本不参与 ？3？。因此 base pointer 的选择完全移出循环。后端一次性求解依赖 warp 的部分，并«B»预计算一个重排后 base pointer 的小数组«/B»，每个 partition 一个，每次 load 用折叠为字面量的常量索引它。", "133": "这与后端在无分区（partitioning）时已有的做法一致：单个动态地址计算被提升到循环之前，循环体保持完全静态。分区只是在其旁增加 base-pointer 数组，因此运行时代价是一次性与分区数成正比的 setup，而非与 load 数量成正比，K 循环内部的地址运算与 baseline 保持相同的纯 XOR 表达式。", "134": "度量：LDS 带宽微基准测试", "135": "以上都是编译期构造。要度量它带来什么，我们用一个微基准测试隔离该效应，其中«I»只«/I»改变 shared layout：相同 shape、相同指令数、相同向量宽度、相同 bank 行为。", "136": "«B»加载什么。«/B» kernel 从 LDS 读取一个 ？0？ fp16 张量，不做其他事。规模缩小到 ？1？ 以便各 base 能在页内展示，其 layout 为：", "137": "由这些 base 可直接推出 kernel 的四个性质：", "138": "«B»每个 warp 一行。«/B» distributed layout 的 warp base 只携带 dim 0，别无其他，因此 warp ？0？ 完整拥有 row ？1？，不触及任何其他 row。", "139": "«B»每次 load 一个 512 字节窗口。«/B» register base 与 lane base 以每个 8 个 fp16 连续覆盖 dim 1，即每 lane 128 位，因此一个 warp 用单个 ？0？ 跨越 512 字节。实测运行使用更长的 row，这只增加沿 dim 1 跨步的 register base，并随之增加被计时的背靠背 load。", "140": "«B»两种 layout 都无 bank conflict。«/B» 同样的连续性意味着各 lane 读取连续的 16 字节块，而 ？0？ 以 16 个 lane 为一组服务该 warp，因此每组恰好覆盖全部 64 个 bank 各一次。", "141": "«B»两种 shared layout 只在一处不同：«/B» 一行最终落在哪个 partition。在 plain layout 中，四行位于一个连续 buffer 中，因此共用同一个 partition。在 partitioned layout 中，各行在两个 partition 之间交替。warp ？0？ 拥有 row ？1？，这就是把它放到 partition ？2？ 的原因。", "142": "张量的尺寸设置使«I»未分区«/I» baseline 能放进单个 64 KiB partition，这是保证存在冲突需要解决的场景。编译后的 LDS 大小是放置实际发生的证据。对 64 KiB 张量，baseline 在 offset 0 分配一个 64 KiB buffer，而 partitioned 版本分配两个 32 KiB buffer，并被迫将第二个推到 64 KiB 边界，共 96 KiB。", "143": "«B»带宽如何计算。«/B» 每个 warp 发出一串背靠背的 ？0？，两端各由一次 ？1？（shader-clock 周期计数器）读取界定。开头的 clock 读取之前设 barrier，使各 warp 对齐从而其执行串重叠；最后一次 load 之后的第二个 barrier 确保所有 load 在结尾 clock 读取前都已退休。各 warp 合计恰好读取张量一次，因此带宽为张量字节数除以记录的 cycle 数。", "144": "«B»稳态带宽。«/B» 该数值除以 clock 计入的所有内容，包括固定的启动与排空开销：两次 clock 读取与 barrier，再加首次访问的一次未被重叠的 LDS 往返。该开销不随 burst 增长，因此会低估带宽，且 kernel 流式处理越快，低估«I»越严重«/I»，因为固定开销在更短运行中占比更大。将一次 burst 建模为", "145": "cycles = fixed + bytes / rate", "146": "并在一定 burst 长度范围内对 bytes 拟合 cycles，可得到 rate 为斜率倒数，固定开销则作为截距得出。", "147": "«B»结果。«/B» 4 个 warp，工作集 64 KiB，每 warp 32 次 load：", "148": "layout", "149": "实测 cycles", "150": "LDS", "151": "raw B/cycle", "152": "steady B/cycle", "153": "peak B/cycle", "154": "plain", "156": "64 KiB", "160": "partitioned", "162": "96 KiB", "166": "解决 partition conflict 带来 «B»1.65x 加速«/B»，代价是额外 32 KiB LDS，且两种 layout 基本都以各自上限的 100% 流式运行：baseline 因冲突被限制在单个 port，partitioned layout 则用上两个 port。", "167": "由此确定的是机制及其上限：partition conflict 的代价是第二个 LDS port，而将各部分分离可以以对齐间隙的代价完全找回它。当然，只读 LDS 的 kernel 是最佳情况。真实 GEMM 会将这些 load 与 WMMA 计算、全局流量重叠，因此一部分串行化被其他工作掩盖，额外的 LDS 还会与 occupancy 竞争。", "168": "关键要点", "169": "«B»partition conflict 需要同时具备共享位置与共享 cycle。«/B» 与 bank conflict 在单次访问内串行化线程不同，partition conflict 要求 cross-pair 访问命中同一个物理 64 KiB LDS partition «I»并且«/I»被一起发射。打破任一条件即可，而由于 Triton 与 Gluon 无法调度发射 cycle，它们攻击位置这一半。", "170": "«B»修复方法是用 ？0？ 做 swizzle，并用 ？1？ 固定各 piece。«/B» distributed layout 设定 piece 形状与 ？2？ / ？3？，而 ？4？ 输出维度让 partition-aware allocator 能把冲突的 piece 放到不同物理区域，因此分离成为编译器推理的结果，而不是手工调优。", "171": "«B»partition 分离在运行时基本零开销。«/B» 因为 partition index 是编译期常量 register 与循环不变量 warp 项的纯 GF(2) 线性 XOR，base-pointer 选择可以完全提升出循环。", "172": "总结", "173": "在 AMD Instinct™ MI450 GPU 上，有两个 LDS 效应会悄然限制 kernel 吞吐，Triton 与 Gluon 分别对其进行了消除。transposed LDS load：？0？ 的 cooperative-transpose 指令让每个 lane 发出一次宽读，由硬件将数据重分布成 matrix core 期望的 layout；编译器不是去模式匹配转置，而是将两侧表示为 linear layout 并通过测试 left division 来判定 eligibility。partition conflict：为什么到达同一物理 LDS partition 的 cross-pair warp 会串行化，对 WMMA ？1？ 做 swizzle 并用 ？2？ 固定 piece 如何将两个 SIMD pair 拉开，以及 partition-aware allocator 配合循环不变的 base-pointer 选择如何以几乎零运行时代价让这种分离成为物理现实。一个只读 LDS 的微基准测试给出了收益数字：冲突解决后 LDS 带宽提升 1.65x。", "174": "LDS 只是从 global memory 到 matrix core 这条路径上的一站，layout 驱动的优化是 AMD GPU 的 Triton 与 Gluon 编译器中的活跃领域。我们计划后续带来更多 gfx1250 kernel 优化深度剖析。", "175": "其他资源", "176": "Linear Layouts: Robust Code Generation of Efficient Tensor Computation Using GF(2)（arXiv:2505.23819）：Triton linear layout 背后的理论。", "177": "Triton Linear Layout: Concept by Lei Zhang：linear layout 的直观介绍。", "178": "Triton Linear Layout: Examples by Lei Zhang：linear-layout 内部机制与操作（product、composition、inversion、？0？、left division）的实例。", "179": "Algorithms for Compile-Time Memory Optimization：Triton 实现的 base shared-memory 分配算法。", "180": "Triton language repository：AMD 后端 lowering（？0？）、shared local load 与 store lowering（？1？）、allocator（？2？）、linear-layout 转换（？3？）。", "181": "From Naive to Near-Peak: Building High-Performance GEMM Kernels with Gluon：在 AMD GPU 上编写 Gluon GEMM kernel 的前置材料。", "182": "免责声明", "183": "本文件所载信息仅供参考，可能包含技术性错误、遗漏及排版错误。此处所含信息可能发生变更，并可能因多种原因而不准确，包括但不限于产品与路线图变更、组件与主板版本变更、新型号和/或产品发布、不同制造商之间的产品差异、软件变更、BIOS 刷新、固件升级等。任何计算机系统都存在无法完全预防或缓解的安全漏洞风险。AMD 无义务更新或以其他方式更正、修订此信息。但 AMD 保留修订此信息并不时更改其内容的权利，且无义务就此类修订或更改通知任何人。此信息按“原样”提供。AMD 对本文件内容不作任何陈述或保证，并对本信息中可能出现的任何不准确、错误或遗漏不承担责任。AMD 明确否认任何关于不侵权、适销性或适用于特定用途的默示保证。在任何情况下，AMD 均不对任何人因使用本文件所含任何信息而产生的依赖、直接、间接、特殊或其他后果性损害承担责任，即使 AMD 已明确告知此类损害的可能性。AMD、AMD Arrow 标识及其组合是 Advanced Micro Devices, Inc. 的商标。本出版物中使用的其他产品名称仅用于标识目的，可能是其各自公司的商标。© 2026 Advanced Micro Devices, Inc. 保留所有权利", "184": "«I»warp«/I» 是在 SIMD 单元上共同执行的一组线程（MI450 上为 32 个）。AMD 文档将同一概念称为 «I»wave«/I» 或 «I»wavefront«/I»；这些术语可互换使用，本文统一使用 «I»warp«/I»。", "185": "在 MI450 上，«I»Workgroup Processor«/I»（WGP）即早期 AMD Instinct 架构所称的 «I»Compute Unit«/I»（CU）。两个名称指代层级中的同一级别；本文统一使用 WGP。", "12": "«B»linear layout«/B» 用一个抽象取代了那些定制 layout：一个在 «I»（二元域，即 XOR 下的比特）上？0？线性«/I»的映射。每个输入（register、lane 或 warp id）都以二进制写成一个 bit-vector，layout 完全由它把每个«I»输入基«/I»（即该输入的 2 的幂 ？1？）映射到何处来定义。线性决定了其余一切：任意索引的像，是它所分解成的各输入基的像的 XOR。等价地，linear layout 就是作用于输入比特的二进制矩阵。", "16": "Linear layouts 用 «B»左除（left division）«/B» 清晰地表达这一点。左除是 layout 矩阵的块对角分解。linear layout 只是基于 ？0？ 的二进制矩阵。矩阵 ？1？ 可被 «I» «/I»左可除？2？，当它能写成块对角形式时。", "53": "图4a：从图2a中的一个 ？0？ 所有权面板（ownership panel）读取 b16 ？1？。每个逻辑元素将一个目标 ？2？ 与其源 ？3？ 配对，对应于编译器维度 ？4？ 和 ？5？。采样五个 lane base 和三个 register base 便足以定义该 layout，因为其余每个位置都是这些 base 的 XOR。", "62": "降级（lowering）随后计算 ？0？。？1？ 将单条指令内的位置 ？2？ 映射到接收该数据的 destination register 和 lane；与 ？3？ 组合后，将该 destination 位置映射到它的 LDS tile base。此处 ？4？ 枚举由一条指令的各 lane 发起的独立 source reads。因此，编译器将 ？5？ 的 ？6？ bases 重命名为 ？7？ 的 ？8？ bases，并加上来自 ？9？ 的 warp bases。以运行时的 lane 和 warp ID 对 ？10？ 求值，即得到每个 lane 必须读取的 LDS 地址。", "63": "图6：？0？ 成功后使用的 layout 流水线。商（quotient）移除 base ？1？，？2？ 将其放置位置提升回原始 offset 空间，？3？ 通过 ？4？ 转换指令的 ？5？ 坐标。最终的 ？6？ 将运行时的 lane 与 warp ID 直接映射到每个 lane 发出的 LDS 地址。", "66": "«B»？0？ 的适用性是一个 layout 匹配问题，而不是转置检测。«/B»由于该指令应用固定的 cross-lane 模式，编译器将两个 layout 都表达为 linear layout，并使用左除（？1？）来检验所需的 ？2？ 映射因子能否落入该指令的固定 tile。如果可以，则该 load 适用。", "109": "«B»？0？«/B» 之所以存在，是因为每个 partition 的数据可以沿 «B» «/B»交错？1？：沿 operand A 向下遍历时，各片段交替 ？2？。一个完整的 ？3？ cycle 构成一个 «I»group«/I»（组），？4？ 表示其重复次数，即图中的 ？5？ 个括号。等价地，它表示 ？6？ tile 沿 ？7？ 重复的次数：A ？8？；B ？9？。", "117": "{\"id\": 0, \"type\": \"text\", \"content\": \"分片数量，以及由此决定的 TDM 开销，取决于所选的 WMMA ？0？。上述 layout 为每个 warp 分配单个 ？1？ tile，这使得分片很小：operand B 最终成为八条 ？2？ strip。若改为选择更粗粒度的 ？3？，让每个 warp 覆盖 ？4？ 的四 tile strip，则分片变大、数量下降。图11 展示了这一替代方案：？5？ 给出一个 ？6？ tile，它以 ？7？ 个块（沿 M 堆叠）对 C 进行分块。\"}", "59": "编译器首先检查 ？0？。为了使 lowering 正常工作，？1？ layout 必须能被 ？2？ 整除。下图将这一检查应用到一个最小的单 warp ？3？示例。它对应 Triton 的 ？4？ 回归测试在 ？5？ 中的 valid、scrambled-valid 和 invalid 用例。"}