#!/usr/bin/env python3
# -*- coding: utf-8 -*-
# article_data_build.py — The Ultra-Scale Playbook 第3篇（张量/序列/上下文并行 + 流水并行 + 环状注意力）
# 命令式组装 top-level dict -> json.dump 到 article_data.json
import json, os, sys

_article_dir = sys.argv[1] if len(sys.argv) > 1 else os.getcwd()

DATA = {
    "summary": [
        {"key": "张量并行", "body": "沿隐藏维把权重与矩阵乘切成行或列分布到多卡，激活也能随之分摊，缺点是通信会落进正向的关键路径。"},
        {"key": "序列与上下文并行", "body": "把每个 token 独立处理的模块沿序列维分摊，长序列下连注意力也用环状注意力轮转 KV，驯服随序列长度疯长的激活。"},
        {"key": "流水并行", "body": "按层把模型分布到多卡，用微批、交错层与 Zero Bubble、DualPipe 层层削平串行带来的气泡空洞。"},
    ],

    "lead": [
        "上一篇把参数、梯度与优化器状态沿数据并行这根轴一路切到了 ZeRO-3，显存被 N_d 除了又除，最后却发现有一块谁都切不动的激活显存横在前面：它既不被跨副本重复、无处可削，还随序列长度与 batch 一路疯长。本篇正是顺着这条线索，正式从共享范式转向切分范式的第一根轴：张量并行沿隐藏维切开同一次矩阵乘，把权重与激活都摊上多卡；再由作为其自然延伸的序列并行，把 LayerNorm 与 Dropout 沿序列维拆小；等序列真的长到 128k 甚至更远，上下文并行配上一圈环状注意力，轮转送 KV 把跨卡显存压住。而当上面这些都不足以驯服一个动辄 70B 往上的权重本身时，真正的重头戏才登场：它不是再切一层数，而是干脆按层把模型劈开、铺上多张卡并出一条流水，用微批、交错层到 Zero Bubble、DualPipe 一连串越发精巧的调度，把串行带来的流水气泡一寸寸填平。",
        "和前面的篇目一致，原文所有交互式动画、时间轴与教学小程序我们都用文字讲清它们想表达的结论，不截取任何静态伪帧；公式与数字尽量按原样保留，方便对照后续篇目。地图式速查图仍用作本篇封面。",
    ],

    "sections": [
        {
            "type": "h2",
            "title": "张量并行：沿隐藏维切开同一次矩阵乘",
            "paras": [
                "到了 ZeRO 这一步，模型参数、梯度与优化器状态都被削得干干净净，可一旦激活占用反过来超过显存预算，ZeRO 就碰了天花板。于是张量并行（Tensor Parallelism，TP）登场：它把权重、梯度、优化器状态连同激活一起切开，而且算之前并不需要先把它们全部收集齐。看似像梦，实现起来其实只需要一行底子：矩阵乘法的两条基本性质。神经网络里最常见的运算写作 X 乘 W，其中 X 是输入也就是激活值，W 是 nn.Linear 层的权重；能否并行，完全取决于我们选择切开哪一边。",
                "第一条性质针对按列切分：A 乘 B 可以等价成 A 分别去乘 B 的每一列，再横向拼接起来，即 A·B 等于 A·[B1 B2 …]，逐块得到 [A·B1 A·B2 …]。第二条性质针对按行切：把 A 按行、B 按行对齐，A·B 就等于 Σᵢ Aᵢ·Bᵢ，各块相乘的结果再加起来。前者启发我们把一个全尺寸矩阵乘劈成若干小块、各块独立算完再合并，后者启发我们让每一块 GPU 各算一部分结果再做归约。决定选哪条路，其实就是在决定后续要动用哪一种通信原语。",
            ],
        },
        {
            "type": "h3",
            "title": "按列切：广播输入、聚集结果",
            "paras": [
                "第一种是沿列切分，也叫 column-linear。做法是把完整的输入矩阵复制到每一位 worker 上，这一步对应的通信原语是 broadcast；然后把权重矩阵切成几块列。每一块 GPU 用手里那部分权重列去和完整输入相乘，各得一块部分输出，最后再用一次 all-gather 把所有部分输出的列并起来，得到完整输出。这里 all-gather 的作用是收集再拼接，正好和被切出去的列对上。示意图画出列并行下输入全量广播、权重分列、输出再聚集这三段，一图看清切列并行需要的两次通信。",
                "若 C 层之间的衔接恰好是段落式的列拼接（比如这里要的是完整隐藏维），all-gather 自然顺理成章。注意列并行里输入并没有被切开，这在理解上是个关键点，因为稍后我们会见到另一个必须切输入的方向。",
            ],
            "fig_after": {
                "1": [{"src": "tp_diagram2.png",
                       "caption": "列并行（column-linear）：把输入广播到各 worker，权重按列切开后分别相乘，再用 all-gather 把各列部分结果并成完整输出"}],
            },
        },
        {
            "type": "h3",
            "title": "按行切：散射输入、归约结果",
            "paras": [
                "第二种是沿行切分，也叫 row-linear：把权重矩阵切成若干行块。细心读者大概已经猜到，行切会带来一个额外约束：要做的不是广播而是把输入本身也切开来分发给各 worker，这一不产生完整副本的操作叫作 scatter，是本书遇到的第四个通信原语。每一块 GPU 拿到的输入分块和权重行块相乘后，结果形状本来就已经对得上，却需要把所有部分结果加起来才是最终答案，因此这次收尾用的是 all-reduce 而不是 all-gather。",
                "到这里，四种分布式通信原语串起了一条线：broadcast、all-reduce、gather 系列与 scatter，正好覆盖了行切、列切以及后面序列并行里的边界情形。示意图把行并行下输入先散射、部分结果再归约的完整流程标了出来。",
            ],
            "fig_after": {
                "1": [{"src": "tp_diagram3.png",
                       "caption": "行并行（row-linear）：把输入沿行 scatter 到各 worker、权重按行切开相乘，最后用 all-reduce 把各 worker 的部分结果归约为最终输出"}],
            },
        },
        {
            "type": "h2",
            "title": "把 TP 拼进一个 Transformer 块",
            "paras": [
                "玩具例子的两块砖拼好，真正的问题来了：这两块基础件该怎么在一个真实的 Transformer 层里有效组合？一个 Transformer 由两大件构成，前馈层（MLP）与多头注意力（MHA），两者都能套用张量并行。前馈部分用一步 column-linear 紧接一步 row-linear 来并行：前向就是一次 broadcast 复制输入、再加一次 all-reduce 收尾。真实训练里 broadcast 往往能被省略，因为我们本来就可以保证 TP 各 rank 的输入早已同步；更妙的是这个列再接行的组合比反过来行后列更划算，因为中间那次本来要做的 all-reduce 被整个省掉，两个切块运算之间不再需要归约对齐。架构示意图把 MLP 里列并行接行并行、通信点只有入口与出口的格局画得很直白。",
            ],
            "fig_after": {
                "1": [{"src": "tp_diagram4.png",
                       "caption": "Transformer 前馈（MLP）的张量并行：列线性在前、行线性在后，输入只需复制一次、结果做一次 all-reduce，中间跳过一次本可避免的归约"}],
            },
        },
        {
            "type": "h3",
            "title": "多头注意力的张量并行：每张卡各认领几个头",
            "paras": [
                "多头注意力大体沿用同一套思路：Q、K、V 三组矩阵用列并行劈开，输出投影则沿行维切开。列并行在多头注意力的语境里有个非常自然的解释：每一块 GPU 恰好负责计算某几个头、甚至某一个头的注意力。这套做法对多头注意力（MHA）成立，对更省的 MQA 与 GQA 同样适用，后两者让若干 query 共享同一组 key 与 value，省去大量重复计算与显存。",
                "这里埋着一个必须守住的限制：张量并行度不能超过 Q、K、V 的头数，否则某一块 GPU 手里的头不完整，就没法各自独立做注意力，还得额外补通信。用 GQA 时更严，TP 度应当小于 K、V 的头数。打个具体的例子：LLaMA-3 8B 有 8 个 Key/Value 头，所以它的 TP 度最好别超过 8；如果非要用 TP=16，就只能在每块 GPU 上把 K、V 头复制一份，并且随时留意让它们保持一致。整块注意力的张量并行全景图，把 QKV 投影片、头划分与输出行切一次串起来展示。",
            ],
            "fig_after": {
                "1": [{"src": "tp_full_diagram.png",
                       "caption": "多头注意力的 TP 全景：Q、K、V 各按列并行切开、每卡认领若干头，输出投影沿行维切回，完整展示一层里的全部切分与通信"}],
            },
        },
        {
            "type": "h3",
            "title": "TP 不是银弹：通信直接压进关键路径",
            "paras": [
                "必须强调，张量并行对训练并不万能。我们直接在模型的计算路径里插进了好几处分布式通信原语，它们不像 ZeRO 那样好藏好叠，最终性能是计算与显存的收益、和新增通信开销之间权衡的结果。按层看它的时间轴：每个 decoder 层的前向都躲不开一个 all-reduce 同步点，它没法跟计算重叠，因为必须等各 TP rank 的部分结果合并好、LayerNorm 才能套用，这成了暴露在外的通信开销。工程上能做的是部分重叠，比如 Megatron-LM 与 Nanotron 前向里会把 FC1 那步的所有-gather 和计算拆开叠：矩阵乘的一部分结果先开始往别的 GPU 送，另一部分还在算。这块研究仍很活跃，近年像 Domino 就在探索把重叠做到极限的新路子。",
                "益处也实在：矩阵乘中间的激活因为是分片存放，内存自然摊薄；但 LayerNorm 这类算子仍要把完整激活收回来，享受不到全部好处，这也是它立刻催生序列并行的理由。顺带几个容易踩的细节被作者单独圈了出来：TP 训练里因为每个 rank 在 all-gather 之后看到同一份激活，LayerNorm 的权重在反向后其实不需要再来一次 all-reduce 就能天然同步；而 Dropout 的随机种子则必须在 TP 各 rank 间保持一致，行为才能确定可复现。",
                "按 TP 尺度把它拉大来权衡，两侧的得失一眼可见：把 TP 度往上调，每块 GPU 的吞吐在降（通信增多），可同时允许喂进更大的 batch（每块显存更松）。作者在星上做了实测：张量并行的通信开销在超过 8 块 GPU 之后开始变得刺眼。单机内的 TP 可以借用极快的 NVLink，一旦跨节点就要落到慢得多的网口上，于是从 TP=8 到 TP=16 能看到明显下滑，TP=16 到 TP=32 的跌势更陡。并行度越高，通信开销越可能吞并计算时间。这批收益对显存相当直观，以 70B 模型举例：把 TP 提高，每块 GPU 上参数、梯度与优化器状态占用的显存一路下降，直到一张 8 卡的机器已经能勉强塞下这个大块头。",
            ],
        },
        {
            "type": "h2",
            "title": "序列并行：把 LayerNorm 与 Dropout 也沿序列切开",
            "paras": [
                "TP 的显存收益并不完整，因为 LayerNorm 与 Dropout 仍要在各自 GPU 上收齐完整激活才能算，等于把省下的显存又吐回去一部分。序列并行（Sequence Parallelism，SP）是 TP 一个小而自然的延伸，专治这两块：凡是 TP 管不到的算子，比如 Dropout 与 LayerNorm，就沿输入序列这一维切开算，而不是沿隐藏维。",
                "先厘清一个容易混淆的术语：本节说的序列并行与张量并行牢牢绑定，只作用于 Dropout 与 LayerNorm。等序列真的拉到很长、注意力本身变成瓶颈时，会有一类叫环状注意力的技巧，有人也把它叫作序列并行，但我们为区分这两种做法，统一称之为上下文并行 Context Parallelism。所以规矩是：看到序列并行就默认它和张量并行一起用，而上下文并行则可以独立上场。",
                "为什么非切不可，因为这两个算子都要访到完整的隐藏维才能算对：LayerNorm 的公式是 γ·((x−μ)/sqrt(σ²+ε))+β，其中均值 μ 与方差 σ² 都要沿着隐藏维 h 归约得到。即便这些算子算起来极便宜，仍要占不菲的激活显存，因为它们要求完整的隐藏维在手。SP 的妙处正是把这份剪不断的内存负担沿序列维切开、摊到各 GPU 头上，让每块只需各自序列片段、就能各自算出彼此的 LayerNorm。",
            ],
        },
        {
            "type": "h3",
            "title": "边界处的算子切换：f 与 g 这对搭档",
            "paras": [
                "SP 与前向的落地，全看 TP 区域与 SP 区域交界处的几次算子切换。作者用一对共轭的标记 f 与 fᐩ、再配上另一对 g 与 gᐩ 来统一描述。前向里，f 是空操作 no-op，因为在这一刻激活跨 rank 早已重复；fᐩ 则是 all-reduce，负责把激活同步正确。反向正好反转：fᐩ 变成空操作（梯度本就重复），f 才做 all-reduce。所谓共轭，指当某个在正向是空操作、它的另一半就在反向变 all-reduce，反之亦然。",
                "SP 区域另用了标作 g 与 gᐩ 的一组操作，刻意避开了 all-reduce：若在这个区域做 all-reduce，就得把完整激活收回来，反而推高峰值显存、违背了 SP 初衷。g 是 all-gather，把切成两半的序列再接回全长；gᐩ 是 reduce-scatter，既为前面的行线性把部分结果正确归约，又顺势沿序列维把它摊回分散形态。把一次完整前向按算子一个个拆开会更清楚：初始 LayerNorm 在 SP 区域，输入 X1 与 X2 为一张形状 (b,s/2,h) 已沿序列切好的片，各卡独立做 LayerNorm 得 Y1、Y2。随后第一次过渡 g 做 all-gather 把 Y1、Y2 拼回全长 Y(b,s,h)，因为前面的列线性需要完整隐藏维。接着进入 TP 区域：A1 是列线性、沿隐藏维切，GeLU 各卡独立算，得 Z1★(b,s,h/2)；B1 是行线性、把隐藏维补回来，得 W1(b,s,h)。最后一次过渡 gᐩ 做 reduce-scatter，把上一步行线性的部分和归约正确的同时沿序列维切开，得到 W1★(b,s/2,h)。这张放大的逐算子图正是按这串步序把每个过渡画的清清楚楚。",
            ],
            "fig_after": {
                "1": [{"src": "tp_sp_diagram_zoomed.png",
                       "caption": "TP 与 SP 交界的前向逐个过渡：SP 区域的 LayerNorm 只管自己的序列片，g 的 all-gather 接回全长进入列线性，gᐩ 的 reduce-scatter 在行线性后沿序列切开回到 SP 区域"}],
            },
        },
        {
            "type": "h3",
            "title": "形状到底谁切成谁：一张对照与 16k 序列的自由",
            "paras": [
                "在各种算子间来回切，很快会迷失隐藏维 h 与序列维 s 各自谁被切。作者做了一张小表帮忙：进入 TP（列线性）时，纯 TP 下 h 被切、s 全长；加了 SP 则 s 先做 all-gather 补满再进层。TP 区域里两者都是 h 被切、s 全长。退出 TP（行线性）时，纯 TP 里 h 补全并做 all-reduce；加了 SP 则是做 reduce-scatter，s 重新被切成段。于是 SP 区域里纯 TP 是 h、s 都全长，加了 SP 则 h 全长、s 保持被切开的一大截。词嵌入层同理：它本质是沿词表切的行线性，退出时 SP 会用 reduce-scatter 把 s 切回。",
                "压一压账就能看见 SP 的杀手锏：它把需要存的最大激活峰值压到了 b·s·h/tp，因为我们总能保证要么沿序列维、要么沿隐藏维有一边是切开的，绝不同时都有全长。于是 batch 与序列长度都能比纯 TP 推得更远。还是那个 70B 例子：挂上 TP/SP=16，单卡能塞下长达 16k token 的序列，比纯 TP 的处境再宽松一截。（TP=16 作为单机方案仍旧偏大，但正好引出下一篇怎么再省。）",
                "一个大家自然想问的问题：TP 加 SP 是不是比纯 TP 更费通信？答案是既对也不对。纯 TP 每个 Transformer 块前向有两次 all-reduce；SP 每个块前向是两次 all-gather 加两次 reduce-scatter，操作次数确实是 TP 的两倍。但 all-reduce 本来就能拆成一次 all-gather 再接一次 reduce-scatter（附录 A0 有专门讲解），所以按通信量算两者其实等价；反向只需把每对操作换成共轭即可。细心就能数出每层正好四处通信点，两块贡献给注意力、两块送给 MLP。",
            ],
        },
        {
            "type": "h3",
            "title": "TP+SP 的边界与它的两道坎",
            "paras": [
                "跟纯 TP 一样，TP 加 SP 的通信在时间轴上不太容易跟计算叠起来，吞吐因此重度依赖带宽。也正因如此，两套做法通常都只在一台机器内部收着用，把 TP 度压到不超过单节点里的 GPU 数，比如 TP 小于等于 8。作者拿一个 3B 模型、4k 序列长度实测：TP 度从 8 爬到 16 时掉得最狠，因为那是单节点内用 NVLink 通信、跳到跨节点之间走 EFA 的分水岭；而 SP 带来的激活显存大头，让它能一口气塞进远比纯 TP 大的 batch。",
                "SP 还顺带改写了一笔小账：因为 SP 区域里的 LayerNorm 各算各的序列片，它们的梯度在 TP 各 rank 间并不相同，前向之后要用一次 all-reduce 把它们的梯度同步回去，让权重保持一致，这跟数据并行那套保持权重同步的路子如出一辙。代价不大，因为 LayerNorm 参数本身很少。",
                "综合下来 TP 与 SP 被两道坎拦着：其一，序列长度一路加长，TP 区域内部的激活显存依旧要爆；其二，模型大到 TP=8 都装不下，就得面对跨节点那低得多的带宽，速度会断崖式下跌。第一道坎交给上下文并行去解决，第二道坎则要动用下一篇的流水并行。先看上下文并行，它把长序列的激活消融又往前推了一步。",
            ],
        },
        {
            "type": "h2",
            "title": "上下文并行：长序列的总攻",
            "paras": [
                "有了 TP 与 SP，权重与激活都摊到了各卡上，单卡显存压力大减。可当序列长到 128k token 甚至更高，即使在 TP 区域内部我们要处理的依然是一整条完整序列，单节点内存仍可能顶不住。更糟的是，就算对所有激活做完整重算（那要背上约 30% 的重算开销），层与层交界处仍要留一些激活，而它们恰是随序列长度线性增长的，短不掉。",
                "上下文并行（Context Parallelism，CP）的核心，是把序列并行那套沿着序列切的思路，照搬到本来已经做了 TP 的那些模块上。于是这些模块同时被沿两个维度切开，序列长度也被顺势拆小它的作用。执行上它与序列并行很像：把输入沿序列维切开，却不再只切残余的 SP 区域，而是从整条模型的第一层一路切到最后一层。这切法对多数模块并不伤筋动骨，比如 MLP 与 LayerNorm 本来就一个 token 一个 token 独立处理，序列切不切它们无所谓，也不需要 TP 那样昂贵的通信，因为被切的是输入、权重矩阵原样没动，所以它跟数据并行一样，算完梯度后用一次 all-reduce 把梯度在上下文并行小组内同步即可。",
                "唯独一张的注意力是例外，必须要格外留神：注意力里每一个 token 都要能访问到序列里所有其他 token 的 key、value，因果注意力则至少要回看它之前每一个 token。因为 CP 是拿整个序列沿 GPU 切开，注意力这一段就躲不开 GPU 之间交换所需 key、value 数据的全额通信。若硬来会异常昂贵，好在有一项叫环状注意力（Ring Attention）的核心技巧，专门高效地处理这笔 KV 交换。",
            ],
        },
        {
            "type": "h3",
            "title": "环状注意力：把 KV 一轮轮传下去",
            "paras": [
                "环状注意力在概念上与后文要讲的 Flash Attention 有亲缘：两者都靠在线 softmax 计算把内存压下来。只是 Flash Attention 专注单卡上怎么把注意力算子本身优化得又快又省，而上下文并行靠的是把序列分发到多块 GPU 上来换内存。若把序列长度定为 L，一个朴素的全局注意力通常要占随 L 平方增长的内存，环状注意力正是要绕开这点。",
                "它的回合是这样转的：每块 GPU 先发起一个异步通信操作，把自己手中的 key、value 送给别的 GPU；在等别人数据回来的间隙，先对手里已经有的那部分数据算注意力分数。理想情况是下一份 key、value 恰好在这份计算结束前到位，于是 GPU 算完立即接上下一轮，几乎不停顿。拿 4 块 GPU、4 个 token 举例最直观：起初序列均匀沿序列维切开，每块 GPU 只持有一个 token 以及它对应的 Q、K、V。整个注意力的计算要花 4 个时间步，每一步每块 GPU 依次做三件事：先把手里当前的 key、value 非阻塞地发送给下一台机器（最后一步除外），转身对当刻已握在手里的 key、value 本地做一次注意力，也就是 softmax(QKᵀ/√d)·V，然后等待接收上一台 GPU 送来的 key、value，接到后把它当作新的当前值回到第一步。这一轮轮把 KV 顺着环传下去的动画，也正是这套方案被作者亲切唤作环状注意力的由来。",
            ],
        },
        {
            "type": "h3",
            "title": "朴素切法的失衡，与它的之字形破解",
            "paras": [
                "环状注意力初版有个大毛病：因为因果注意力矩阵是三角形的，朴素地把前几个 token 分给第一块卡、后几个分给后面，极易造成 GPU 间负载严重失衡。原因在于 softmax 是按行做的，某块卡只有把手头那一行的所有 token 都收齐了才能算完：GPU1 从 token 1 到 4 起步，天然不需要向任何别的卡讨数据、当场就能算；GPU2 却要等第二轮收到 token 1 到 4，才有 token 1 到 8 的全部材料。于是在朴素切分下，GPU1 干的活似乎比其他所有 GPU 都少一截。",
                "要重新摊匀，诀窍是不再让 token 纯粹按顺序堆到各卡上，而是把顺序打散混着给，让每块卡手里同时有一些很靠前的 token、又有一些很靠后的 token。这正是之字形（zig-zag）环状注意力：新排布下注意力掩码的着色分布均匀，数一数各 GPU 负责的色块数就会发现计算已经端平。代价是，为了把每一行补齐，每块卡必须从其余所有 GPU 各取一些信息，谁也逃不掉一次全面的互通。",
                "把计算与通信叠起来，实现上有两条路可选。全收集那条：一个 all-gather 让所有卡同时把别的卡手里完整的 key、value 全部收齐，直观简单，但每张卡都要临时把全套 KV 攥在手里，需要更多工作内存，通信虽只一步开销却是按最大占用堆的。换环上交换那条：卡与卡之间像串珍珠一样一茬一茬地交换 KV 分块，每张卡只多暂存一块，内存省得多，通信被摊开和计算叠起来，代价是多段式通信额外带了点基准时延。两条路的取舍浮出水面：all-to-all 那版内存更省、只是通信形态略复杂，all-gather 那版实现更直接、却要吃掉更多临时内存。",
            ],
        },
        {
            "type": "h2",
            "title": "流水并行：按层切，而不是让整张卡去扛重量",
            "paras": [
                "把 TP、SP、CP 的账一路算下来，我们理清了怎么用 TP 在一台多卡机器里驯服大模型、怎么用 CP 驯服长序列。可 TP 有一个天生软肋：它跨节点扩不好。那么当模型的权重连一台机器都塞不下时该怎么办？第四个、也是最后一个并行维度流水并行（Pipeline Parallelism，PP）抬手应召。",
                "单节点通常摆 4 到 8 张卡，把 TP 推到超过这个数就撞上叫跨节点连接的低带宽网，all-reduce 这类操作实测下来掉速相当刺眼。可如果瓶颈不是序列而干脆是模型本身，比如 70B 往上，光是一副权重就能压垮单节点的全部显存。解决办法朴素到极致：把模型的层拆开摆在多张 GPU 上。拿 8 块 GPU 举例，第 1 到 4 层放 GPU1，第 5 到 8 层放 GPU2，以此类推，每块 GPU 只存与处理自己那一截层，单卡显存需求大幅跳水。",
                "PP 很自然会让人想起 ZeRO-3：两者都是把参数切开跨越 GPU 分发。区别我们留到下篇的 5D 总结里细细比。但首先请看这套切法的一个微妙之处：参数确实被切开了，可每块 GPU 上的激活显存一点没少，因为每块卡都要处理整套 batch、只不过处理的是不同层；一批数据前向走到底，要把一个 GPU 那几层的激活传给下一块 GPU 去接着算。于是出现一种新的通信形态：像 ZeRO-3 那样传参数的角色退场了，变成激活张量沿一条流水线在卡间依序传递。概念简单，真正实现却相当刁钻。",
            ],
        },
        {
            "type": "h3",
            "title": "最老的练法最大的洞：先把前向全做完",
            "paras": [
                "凡是先把层摊给多块设备、再用一步前向把整批数据顺势传完、紧接着一口气跑完反向的，都叫作先全前向再全反向（AFAB）。它的第一个优点是省网：要练的数据只需在模型深度的若干个落脚点传不大不小的激活，比起 TP 每层内部动辄多次往返，带宽需求低得多。第二个优点是把前向与反向的次序保持成熟悉的整体组织，代码改动最小，是几乎最易实现的 PP 变体。",
                "可它的病也一眼可见：前向是严格串行的，GPU1 算的时候 GPU2 只能干瞪眼，于是图表里大片灰色即空闲，作者把这叫流水气泡（bubble）。把这笔账量化清楚，就知道效率漏在哪：记 t_f 与 t_b 分别为一个微批、一个流水段上测得的前向与反向耗时，通常合理假设 t_b 约为 2 倍的 t_f。若无气泡、练得堪称完美，理想总时间 t_id=t_f+t_b；可图上每个额外阶段都平白搭上 (p−1)(t_f+t_b) 的等待，p 是流水并行度，即同一条流水里的 GPU 数。于是气泡占比 r_bubble=((p−1)(t_f+t_b))/(t_f+t_b)=p−1。阶段加得越多、气泡涨得越凶，朴素实现的利用率自然低得可怜。",
                "解法也不复杂，就是把 batch 拆成一份一份能近乎并行的微批，像数据并行那样：GPU2 正忙着手里的微批 1，GPU1 已能抽身去起微批 2。作者给出一个 8 微批的实际排程，微批足够多时气孔就被填上不少。重新算一遍这场账：现在要处理 m 个微批，理想总时间变成 m·(t_f+t_b)，于是气泡占比是 r_bubble=((p−1)(t_f+t_b))/(m·(t_f+t_b))=(p−1)/m，加微批就把漏掉的气泡按 m 的系数压小。",
                "但 AFAB 有个更凶的暗雷：它要把整批数据的激活一直留到反向到来才能释放，显存因此瞬间撑爆。既然如此，何不让一部分反向绕着仍然飘着的前向插空开始，早点把不再需要的激活丢掉？这就引出了下一步更聪明的调度。",
            ],
        },
        {
            "type": "h3",
            "title": "1F1B：让中间稳态一边前向一边反向",
            "paras": [
                "把反向尽量提早展开的调度，因为其中间稳态是前向、反向交替进行，得名一前向一反向（1F1B）。它的思想是趁早开始反向，让不断释放的激活给渐渐涌来的新数据腾地。仔细数一数图上的方格子会发现气泡尺寸并无改善，效率没有显著升高；但它真正救场在内存：现在只需留存 p 个微批的激活（p 是流水并行度），而不是 AFAB 那种要把全部 m 个微批都攒着，激活爆炸被按下去了不少。正因为省了内存，我们才敢把微批数加上去，而微批一多，按 (p−1)/m 算出的气泡占比才真正一路走低。",
                "图里那种前后向不再清净轮流、而是在各设备上彼此穿插交错的形态，是 1F1B 麻烦的地方：每个设备都得各自安排一个从'正在做前向'切到'开始做反向'的切换点，而不是像往常那样套在一个简单统一的主循环里。这正是实现流水并行往往要动训练代码与建模代码大改的原因之一。",
                "1F1B 扩到多节点时表现如何，作者在自家集群上做了实测，放出一张吞吐随微批数变化的基准图。左半边把微批数压到不超过 PP 度减一，也就是 m=p−1，可以看到气泡有多大破坏力：性能低、而且随 PP 规模拉大还在继续往下掉。右半边改用远多于 PP 度的微批，比如 m=32 远远大于 p−1，低 PP 度一侧的吞吐被明显抬起来，但 PP 度极大时仍旧被封顶。现实里没法无限追加微批去长期维持 m 远大于 p−1 的比例，因为我们终究被目标全局 batch 锁死；随着 PP 度一路加上去、微批上限却只有那么多，最终只能回头按 r_bubble=(p−1)/m 承受渐渐胀大的气泡。",
                "值得记住的一组跨节点数字：微批很小时，PP 从单节点 p=8 扩到两节点 p=16，性能只降约 14%，比 TP 同类跨节点场景通常 43% 的掉速好看太多。碰到低带宽的节点间网络时，这种好脾气正是流水并行在多节点分布式训练里格外讨喜的原因。",
            ],
            "fig_after": {
                "1": [{"src": "pp_1f1b_scaling.png",
                       "caption": "1F1B 流水并行随微批数扩缩的吞吐实测：微批数与 PP 度接近（左侧 m=p−1）时气泡拖累显著、随 PP 拉大持续走低；大量超配微批（右侧 m=32）则明显拉起低 PP 度吞吐"}],
            },
        },
        {
            "type": "h3",
            "title": "交错切层：把模型纵向掰成几段再交错上岗",
            "paras": [
                "1F1B 把激活内存修好了，可那个顽固的气泡还躺在原地，它正比于流水阶段数。真要再压，就得花更多通信来换。于是有了交错层（interleaved stages）的主意：此前我们把模型沿深度朴素一刀切，比如第 1 到 4 层给 GPU1、第 5 到 8 层给 GPU2；现在换成把奇偶层拆开，比如让 GPU1 扛第 1、3、5、7 层，GPU2 扛第 2、4、6、8 层。整体看就像一条回环的流水，一个微批在前向里绕着各卡转好几圈，才把整条模型走完。",
                "代价是明显的：以前一次路过的活，现在模型要在每张卡上进进出出好几回，通信相应变多。收益同样清楚：每个前向与反向都被因子 v 除小了，v 是每张卡上切出的模型段块数，因为前后向被更充分地交错开。定量写出来，气泡总时长 t_pb 变为 ((p−1)(t_f+t_b))/v，气泡占比 r_bubble=((p−1)(t_f+t_b))/(m·(t_f+t_b))·(1/v)=(p−1)/(v·m)。于是微批与交错块双管齐下，气泡被压得更扁；但要记住通信量也同步乘上了 v，这是一笔要以吞吐来偿还的账。图里摆了 p=8 的几个典型组合，其中 m=1、v=1 是朴素流水的情形，v=1 对应 AFAB 或 1F1B，v 大于 1 的才是交错组态。",
                "调度也跟着变量的切换变复杂：同一时刻同一张卡，到底是优先照顾那些已经进入较深层的早期微批、好早早把它们送出模型（这叫深度优先 depth-first，追求尽快出队），还是反过来先让后期微批尽快填进流水前段（这叫广度优先 breadth-first，追求把整条流水填得越满越好），两种优先级的选择有专文讨论。此时 Llama 3.1 使用的流水方案正是集大成：一套 1F1B 配上交错层，深度优先与广度优先之间还可调出一个可调档的优先级。作者放出的这张调度图，把 Llama 3.1 前后向如何在 1F1B 加交错的组合里交错落位画了出来。",
            ],
            "fig_after": {
                "1": [{"src": "pp_llama3.1_schedule.png",
                       "caption": "Llama 3.1 的流水调度：1F1B 加交错层的成熟组合，前后向在整条流水上交错穿插，气孔被压缩到远比朴素分配更小"}],
            },
        },
        {
            "type": "h3",
            "title": "把气泡填到近乎归零：Zero Bubble 与 DualPipe",
            "paras": [
                "调度的想象空间还没到顶。近年出现了把气泡压到几乎零的新方法，DeepSeek V3/R1 的实现就是其中代表，作者忍不住点破那份炫耀意味：它宣称做到了近乎为零的 all-to-all 通信开销。这套做法有个前身叫 Zero Bubble，抓住的观察极其犀利：一次矩阵乘的反向其实藏着两个互相独立的算子，用于反向传播到输入那一步的 B 操作，与用于反向传播到权重那一步的 W 操作。B 的输出还要继续往更下层反向去喂，是反向主线必不可少的一环；W 的结果却只跟优化器打交道，整套反向其余部分都不再需要它、只需在优化步骤前算完即可。想通这一层，W 就获得极大的调度自由度：它可以被排在与其同段的那个 B 完成之后的任何时机，专挑流水里那些气泡的空档去填。图上那张计算图正是把 B 与 W 的依赖画得分明，让人一眼看出 W 的弹性。",
                "Zero Bubble 论文里列了多种编排，其中理论上最优、把气泡清零的那个叫 ZB-H2：它在调度图里并排演示，同一条 1F1B 那条对比成粗粒度反向，下方两路则把反向切进 B 与 W 更细的粒度，zb-H2 就走在这条细粒度拆解与高超排程上。DeepSeek 的 DualPipe 则是把这套细分解又推进一步：它让两股数据流从 PP 维的两端相向而行、交错互插，把 GPU 的空闲一再往下挤。图里那张排程图，正是这套两端相向、你追我赶的双流水交错布局。",
                "真要把这类精细调度的气泡压到极致，往往要逐一测量各细粒度算子的耗时，再解一个整数线性规划(ILP)去求最小气泡；Zero Bubble 论文里讨论了做这类排程的启发式与算法。也正因 Zero Bubble 与 DualPipe 的调度过于复杂，作者坦言放不出逐行代码，只留下概念性的图景。截至此处，我们巡完了名为流水气泡的这片江湖：从朴素 AFAB 的巨大空洞一路走到近乎零气泡的 DualPipe，模型并行地图已经铺开可观的画卷。号角随即吹向压轴的角色：专家并行。",
            ],
            "fig_after": {
                "1": [{"src": "pp_zerobubble_compgraph.png",
                       "caption": "Zero Bubble 的计算图：一次反向被拆成去向输入的 B 与去向权重的 W 两步，W 与其余反向无依赖、可被灵活挪去填气泡"}],
                "2": [{"src": "pp_zerobubble_dualpipe.png",
                       "caption": "DualPipe 的双流水排程：两股数据流从 PP 维两端相向行进、交错互插，把 GPU 的空闲进一步压到接近零"}],
            },
        },
    ],

    "conclusion": [
        "这一篇啃下的是把模型装进多机集群的前半段拼图。张量并行沿隐藏维把权重与矩阵乘切成行或列，让激活也能跟着分摊，代价是这些通信落进每个 decoder 层前向的关键路径、躲无可躲；序列并行顺手补上 TP 管不到的 LayerNorm 与 Dropout，把最大激活峰值一路压到 b·s·h/tp，才换来 16k 序列入单机；序列真的拖到 128k 级别，上下文并行配上环状注意力与它的之字形切分，用轮转送 KV 的方式把跨卡内存摊平。当这一切都挡不住一个 70B 权重自己那点吨位时，压轴出场的流水并行按层切分救场，靠微批、交错层直至 Zero Bubble 与 DualPipe 这套越来越精致的前后向交错，把起初 r=p−1 那样触目惊心的气泡空洞一寸寸填到接近归零。",
        "一条条并行轴逐个磨好了刃：切模型副本的数据并行叠加出 ZeRO 的按需取用，张量并行把权重与激活沿隐藏维摊平，序列并行把每步要存的峰值收到只留一份切块，上下文并行靠环状注意力驯服超长序列，而流水并行则用越抠越细的前后向交错填平了跨节点的流水气泡。到这一篇收束时，模型本身从权重到激活的显存，已经几乎都能在各根轴上摊到任意想要的多卡规模。仍有一座高峰等着翻过：当模型为了省算力把前馈层换成一排成千上万的专家（MoE）时，我们还需要一条专门的轴去决定每个 token 该拜访哪位专家。下一篇的专家并行与五维并行总览，将把我们攒下的所有轴拼成一幅完整的全景图。",
    ],

    "reference_url": "https://huggingface.co/spaces/nanotron/ultrascale-playbook",
    "title": "张量并行到流水并行：把装不进单机的模型铺满整个集群",
}

out_path = os.path.join(_article_dir, "article_data.json")
with open(out_path, "w", encoding="utf-8") as f:
    json.dump(DATA, f, ensure_ascii=False, indent=2)
print("OK wrote article_data.json chars=", len(json.dumps(DATA, ensure_ascii=False)), "sections=", len(DATA["sections"]))
